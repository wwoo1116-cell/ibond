# K-Bond 레인 인수인계 프롬프트

작성 2026-08-27, 개정 2026-09-02 (2차~10차 — §7·§8·§9·§10). 다음 세션은 이 문서를 먼저 읽고 시작한다.
아래 «DECIDED» 는 오너가 정했거나 실측으로 닫힌 사안이다. 다시 열지 않는다.

> **급하면 같은 폴더의 `NEXT_PROMPT.md` 한 장만 읽어도 된다.** 붙여 넣을 프롬프트와
> 현재 상태 표, 오너 결정 대기 2건이 거기 있다. 이 문서는 그 뒤의 근거다.
> 이어서 할 자리는 **§10 국고 은어**, 그다음이 **§6 다음에 할 일**이다.

---

## 0. 한 줄

### ★★[OWNER 2026-09-02] 이 데이터의 목적이 정해졌다

> 「이걸로 **장외 Order Book Dynamics** 구현할 생각이야」

**파싱은 목적이 아니라 재료 만들기였다.** 앞으로의 모든 판단(무엇을 더 파고, 무엇을
버리고, 어느 결함을 먼저 고칠지)은 **호가창 동역학을 세울 수 있는가**로 잰다.
자세한 것은 §11.

같은 날 지시 넷도 함께 받았고 전부 집행했다 — 범주 콜 스키마 신설 · `국딱` 종목·민평
조인 · 기계적 수리 · **모르는 낱말 목록을 오너께 보내 뜻을 받아 오기**(§12).



K-Bond(케이본드) 메신저에서 채권 호가를 뽑는 레인이다.
로그 831만 행을 구조화해 놓았고, 평일 16:30 에 그날치를 증분으로 덧대는
예약 태스크가 돈다. 한 메시지에 여러 종목이 든 발언은 `kbond_legs` 로 펼쳐 둔다.

작업 경로 `C:\Users\infomax` (별도 리포 아님, git 아님).

---

## 1. «DECIDED» 다시 열지 않을 것

**D1. 텍스트 추출 경로는 `WM_GETTEXT` 직접 읽기다.**
세 가지를 실측 비교했다.
- pywinauto `print_control_identifiers()` : 구조 파악용으로만. 같은 컨트롤을 512번
  중복 출력해서 무제한으로 돌리면 파일이 421MB 가 된다. 줄당 400자 절단 필요.
- 클립보드(pyautogui Ctrl+A/Ctrl+C) : **버린다.** 0/5 실패.
  포커스를 뺏기고, 더 나쁘게는 **실패하면서 성공한 척한다** (메모장에 열려 있던
  옛 로그를 K-Bond 데이터로 착각해 5,582자를 "성공"으로 보고했다).
  포그라운드 일치 검증 없이 이 경로를 쓰면 안 된다.
- **`WM_GETTEXT` : 5/5 성공.** 포커스도 클립보드도 안 건드린다. 이걸로 간다.

**D2. UI 백엔드는 win32 다. uia 는 무용지물이다.**
K-Bond 는 Delphi/VCL 32비트 앱이라 UIA 를 구현하지 않는다.
uia 백엔드로는 이름 없는 `Pane` 만 보이고 텍스트가 전혀 안 나온다.

**D3. 대상 방은 블커본드, 막무가내 둘이다.** [OWNER]
디렉토리에는 다른 방(니스, 1반, 알프스, M브라더스, 레드본드 등)도 있지만
확장하지 않는다.

**D4. 틱 표기 `+` 는 사자, `-` 는 팔자다.**
표본 대조(`-`+팔자 558건 대 사자 46건, `+`+사자 42건 대 팔자 10건)와
외부 출처가 일치한다. 경향신문 2012-07-20 「12-6 50+, ㅎㅈ, ㄱㅅ... 수수께끼
같은 장외채권 시장」: "플러스 기호(+)는 사자, 마이너스 기호(-)는 팔자".
같은 기사에서 `ㅎㅈ`=확정, `ㄱㅅ`=감사도 확인했다(`MsgType` 컬럼 근거).

**D5. 민평 대비 `원` 은 가격 단위이고 부호가 반대다. 임의 환산하지 않는다.** [OWNER 자료]
만기 1년 내외 채권은 YTM 이 아니라 액면 1만원당 단가로 호가한다.
전일 민평 단가 대비 +1원/-2원 식이고 최소 단위 0.25원, 민평 단가의 소수점
이하는 절사한 뒤 시작한다. 데이터가 이 규칙과 맞는다.
- 0.25 배수 비율 97.63% (890,554건)
- 잔존만기 중앙값 0.71년, 1년 이하 77.3% / 2년 이하 99.5%
- `+n원` 일 때 금리 하락 92.6% (|스프레드|>=1원 구간은 99% 이상)

환산은 실측표 보간으로만 한다. 함의 듀레이션이 잔존만기와 거의 같았다.

| 잔존만기 | 실측 bp/원 | 함의 D | 1/TTM |
|---|---|---|---|
| 0.00~0.25년 | 5.20 | 0.19 | 6.52 |
| 0.25~0.50년 | 2.48 | 0.40 | 2.61 |
| 0.50~0.75년 | 1.57 | 0.64 | 1.58 |
| 0.75~1.00년 | 1.12 | 0.89 | 1.12 |
| 1.00~1.50년 | 0.87 | 1.15 | 0.84 |
| 1.50~2.00년 | 0.63 | 1.58 | 0.61 |
| 2.00~3.00년 | 0.42 | 2.35 | 0.42 |

`bp`/`빕` 표기는 금리 단위이고 부호가 그대로다.

**D6. 섹터를 먼저 판정하고 코드는 나중에 읽는다.** [OWNER 지적]
`YY-N` 표기를 국고 지표물로 볼 수 있는지는 섹터가 정해져야 알 수 있다.
MBS, 국민주택채(국주), 도시철도채(서도철), 발행체 회차(`삼성증권 20-1`)가
같은 표기를 쓴다. 코드부터 뽑고 숫자로 사후에 걸러내면 안 된다.
그 방식은 **본문에 민평이 적힌 행에서만 작동**하는데 830만 중 2천 행뿐이라
나머지는 검사조차 못 하고 통과한다.
25bp 가드는 2차 안전망으로만 남긴다(현재 439행 폐기).

**D7. 실시간 수집기는 폐기했다. 주 경로는 자동저장 로그 증분 파싱이다.** [OWNER 2026-09-01]
셋을 실측하고 내렸다.
- **한 번도 성공한 적이 없었다.** Last Run 2026-08-28 07:50:17, Last Result
  `0x800710E0`("operator or administrator has refused the request")
  = `DisallowStartIfOnBatteries=True` 로 배터리 상태에서 거부. 08-31·09-01 은
  `StartWhenAvailable=False` 라 미실행(NumberOfMissedRuns=2).
- **유일한 산출물이 파손이었다.** `live/kbond_live_20260827.csv` 는 헤더 25열
  아래 26열 행이 12,451개(60.3%) 섞여 pandas 로 안 열린다. 뿌리는 T10 참조.
  당시 "75초에 11,431행" 검증은 행 수만 세고 열을 안 셌다.
- **고유 가치가 0.01% 다.** 자동저장 `.txt` 는 기동 시 한 번 찍고 마는 게 아니라
  **장중 내내 append** 한다(08-28 블커본드 08:45:34 ~ 16:50:55, 565KB).
  수집기만 잡는 구간은 앱 기동 전 버퍼인데 08:45 이전은 830만 중 648행이다.

D1(WM_GETTEXT)이 틀렸다는 뜻이 아니다. 폴링과 append 쓰기가 문제였다.
폐기물은 `kbond_retired/`(README 에 근거 전문). **M4(실시간·과거 병합)는 소멸했다.**

**D8. `RE_MP_YIELD` 에 앞 경계 `(?<![가-힣])` 를 넣지 않는다.** (2026-09-01 실측)
T5 는 한글 약어에 앞뒤 경계를 채우라고 하지만 **민평만은 예외다.**
경계를 넣으면 297건이 잘리는데 열어 보니 전부 진짜 민평이었다.

    월민3.471 / 금민 3.436 / 3사민평 : 3.62% / 팔자민3.887

요일·출처를 민 앞에 붙여 쓰는 표기가 실재한다. 경계는 넣지 않는다.

**D9. 값이 하나도 없는 매매 의사는 `QUOTE` 가 아니라 `AXE` 다.** [OWNER 2026-09-01]
> 「`22-15 사자`나 `국당 사자 관심` 같은 건 호가를 요청하는 콜이고」

가격 없이 종목과 방향만 말하는 발언이 508,630건(전체의 6.1%) 있다.
이걸 QUOTE 로 세면 「호가인데 값이 없다」는 가짜 결손이 생긴다.
`MsgType='AXE'` 로 가른다. 그래서 지금 **QUOTE 중 값 0개는 정의상 0** 이다.
합쳐 보고 싶으면 하류가 `MsgType.isin(['QUOTE','AXE'])` 로 union 한다.

**D10. 부호는 기호만이 아니라 한글로도 적힌다 — `오버`/`언더` 다.** (2026-09-01 실측)
`오버0.5사자` `언더4.5팔자` `오바2.5팔자`. **495,409행(QUOTE 의 7.5%)** 인데
수리 전에는 그중 3,184 행만 스프레드가 잡혀 있었다. 49만 행을 통째로 버리고 있었다.
- **단위는 bp 다.** 회귀 기울기 0.949(상관 0.852)이고 잔존만기 0~1년 0.757 /
  1~2년 0.952 / 3~6년 0.976 / 6년+ 1.004 로 **만기에 무관하게 일정**하다.
  '원'이라면 D5 표대로 0.42~5.20 으로 요동쳐야 한다.
  명시 단위가 붙은 소수 사례도 전부 `빕`/`bp`/`비피` 이고 '원'은 하나도 없다.
- **방향도 분명하다.** 오버는 금리 상승 **98.1%**, 언더는 하락 **96.4%**.
  M2 가 신뢰도 55.3% 로 표시한 것은 `+2bp` 식 **기호** 표기이고 이쪽은 별개다.
  그래서 `SpreadSource` 로 갈라 두었다 — `overunder` 는 검증됐고 `sign` 은 M2 대상.

**D11. 통안 은어는 해독됐다 — 「구」=舊(한 단계 이전) · 「삼」=3년물.** (2026-09-01)

| 은어 | 종목 | 판별검정(가설 vs 인접 순위) |
|---|---|---|
| `통당`·`통딱` | 2년물 지표(최근 발행) | 98.6% vs 85.2% |
| `구통`·`구통당` | 2년물 직전 발행 | 98.6% vs 83.8% |
| `구구통` | 2년물 전전 발행 | 99.1% vs 86.7% |
| `삼통`·`삼통당`·`삼딱` | **3년물** 지표 | 98.4% / 99.8% |
| `구삼통` | 3년물 직전 | 98.3% vs 88.3% |
| `구구삼통` | 3년물 전전 | 96.6% vs 79.3% |
| `1.5년통` | 2년물 전전(= `구구통` 과 같은 종목) | KIS 축 1bp 이내 98.2% |
| `1년통` | 2년물 네 단계 전 | KIS 축 1bp 이내 81.8% |

**「N년통」은 발행만기가 아니라 잔존만기 지칭이다.** 2년물이 분기 발행이라
사다리 한 칸이 3개월 — `통당` 잔존 2.0년 → `구통` 1.75 → `구구통`=`1.5년통` 1.5
→ r4 = 1.0년. 1년물(1Y) 신규가 **아니다**(1Y r1 은 -3.3bp 로 어긋난다).

근거 셋. (1) 한국경제 2014-10-20 「통딱·당팔선사·구딱…」 — **통당=최근월 발행물,
구통당=직전월, 통딱=오늘 발행·내일 인수, 구딱=어제의 통딱, 선네고=결제일 하루 앞당김**.
(2) 로제타석 — 딜러가 `구구통(25.7.2통) 495-` 처럼 은어와 만기를 **병기한** 행 87건.
(3) 판별검정 — 가설 순위 민평으로만 복원했을 때의 5bp 적중률을 인접 순위와 비교.

**D12. `국당`·`국전`·`국전전`·`국전당` 은 국민주택채권 1종의 당월/전월/전전월물,
`국딱` 은 그날 입찰된 국고채다.** (2026-09-02 판별검정 — §10 · `RESULT_ktb_slang.md`)
확실한 건 **통안이 아니라는 것뿐**이었고, 그다음 두 판정은 둘 다 틀렸었다.

| 시점 | 판정 | 왜 틀렸나 |
|---|---|---|
| ~2026-08-31 | 통안 | 통안 사다리 적중 48.6~50.9%. 이건 옳게 걷어냈다 |
| 2026-09-01 | 국고(만기 미정) | 「국고 사다리 적중 92~100%」는 **살아 있는 107종 중 아무거나 맞으면 적중**으로 센 값이었다. 판별력이 없는 자였다 |
| 2026-09-02 | **국민주택 / 입찰 당일물** | 오프셋 분포 R=0.975~0.976 · 중심 +15.4~15.8bp = 문면 `국주` 호가의 +15.7bp 와 일치 |

**⚠ 이 항목은 두 번 뒤집혔다. 세 번째도 의심하려면 §10 의 반증 셋을 먼저 볼 것**
(문면 `국주` 오프셋 일치 · 국전당 교체 상대 100%가 국주 · 회차·만기 병기 22,679행에서
만기−회차 94.6%가 5.0년). **셋을 못 뒤집으면 열지 말 것.**

---

## 2. 지금 상태

### 산출물

2026-09-02 실측(3차 재파싱 후). 행수·열수는 `pyarrow` 로 직접 세었다.

| 파일 | 크기 | 내용 |
|---|---|---|
| `kbond_structured_data.parquet` | 250 MB | **8,185,824행 x 54열** |
| `kbond_structured_data.csv` | 2,7xx MB | 같은 내용, utf-8-sig |
| `kbond_legs.parquet` | 12.7 MB | **486,913 다리 x 42열** |
| `kbond_fills.parquet` | 2.8 MB | **84,011 체결 x 30열** |
| `kbond_kis_curve.parquet` | 0.07 MB | **25,143행 x 9열**. KIS 예상종가 335일(§9 외부 검증축) |
| `kbond_은어_목록.xlsx` | | 오너 기입용 «모르는 낱말» 목록(§12) |
| `kbond_parse_state.json` | 0.2 MB | (방,날짜) 그룹 지문. 증분 판정용 |

기간 2024-02-14 ~ 2026-09-01, 539 영업일.

**⚠ 행수가 8,331,933 → 8,184,985 로 146,948행 줄었다.** 결손이 아니라
**이중 적재를 걷어낸 것**이다(§3 T20 — 같은 대화가 `.rtf`·`.txt` 두 형식으로
저장되며 타임스탬프가 1초 어긋난 것). 옛 숫자를 쓰는 문서가 있으면 이쪽이 맞다.

**⚠ `kbond_legs.parquet` 은 한 번 파손된 적이 있다**(2026-09-01, §3 T16).
산출물을 믿기 전에 `pq.ParquetFile(...).metadata` 로 열리는지부터 볼 것 —
크기가 그럴듯해도 footer 가 없으면 그 파일은 없는 것과 같다.

### 범주 콜 (2026-09-02 신설)

| | |
|---|---|
| 판정된 행 | **159,190** (`SectorCat` 또는 `TenorLo` 가 있는 행) |
| 둘 다 있는 «진짜 범주 콜» | **80,326** |
| `SectorCat` 상위 | 은행채 38,920 · 여전채 20,422 · CD 16,592 · 전단채 9,766 · 공사채 7,487 |
| `TenorLo` 분포 | 중앙 1.50년 · 0~1년 33,664 · 1~3년 60,512 · 3년+ 18,817 |
| `MsgType` | **AXE 100,031** · OTHER 34,164 · QUOTE 18,929 · INQUIRY 5,575 |

**★대부분이 AXE 다** — 구속력 있는 호가가 아니라 «이런 걸 찾는다» 는 관심 표명이다.
§11 에서 이걸 호가로 세면 호가창이 부푼다. 유형으로 갈라 쓸 것.

### 국딱 입찰 조인 (2026-09-02 신설)

`AuctionCode` **6,231행**. 만기 분포 5Y 1,792 · 30Y 1,368 · 3Y 1,198 · 2Y 876 ·
10Y 589 · 20Y 405 · 50Y 3 — **만기가 고르게 퍼진 것 자체가 §10 결론의 재확인**이다
(국딱이 30년물 하나였다면 한 칸에 몰려야 한다).
대상 7,520행 중 1,289행은 `BondCode` 를 이미 가진 «교체 짝» 이라 건드리지 않았다.

### 섹터 분포 (2026-09-02 3차 재파싱 후)

| 섹터 | 행수 |
|---|---|
| 크레딧/기타 | 4,183,424 |
| 국고 | 2,631,725 |
| 통안 | 514,573 |
| (없음) | 430,736 |
| **국민주택** | **148,088** |
| 지방/첨가소화 | 72,509 |
| MBS | 71,824 |
| CD | 68,432 |
| CP/전단 | 53,904 |
| 국고/통안 | 4,737 |
| IRS/스왑 | 1,073 |

국민주택이 216 → 148,088 로 뛴 것이 §10 의 결과다(은어 넷 + 문면 `국주`).
국고는 그만큼 줄었고, 은어 행이 국고 지표코드를 잘못 무는 건도 195 → **0** 이 됐다
(국딱 1,289건은 «국딱 팔고 24-8 사자» 처럼 **짝 종목**의 코드라 정상이다).

### 파이프라인

```
python kbond_daily_update.py          # 증분. 안에서 enrich + legs 까지 부른다
python kbond_daily_update.py --force  # 전 그룹 재파싱(부트스트랩, 113초)
python audit_kbond_parse.py           # 진단. 정밀도/재현율/교차검증 + 표본 500행
```

`kbond_daily_update.py` 가 하는 일은 넷이다.
1. (방,날짜) 그룹별 지문(파일명·크기·mtime)을 `kbond_parse_state.json` 과 대조해
   **더러운 그룹만** 고른다. 파일이 자라기만 해도 지문이 바뀌므로 16:30 실행이
   놓친 장 마감 꼬리는 **다음 실행에서 회수된다**(유실 아니라 하루 지연).
2. 더러운 그룹만 `parse_group` 으로 다시 판다. 중복 제거가 그룹 안에서 닫히므로
   그룹 재파싱은 멱등하다.
3. 기존 parquet 을 **행그룹 단위로 흘려보내며** 더러운 (Room,Date) 행을 빼고
   새 행을 붙여 임시파일에 쓴 뒤 원자적으로 교체한다(830만 행을 한 번에 안 든다, T8).
4. `enrich_kbond_quotes.main()` · `kbond_legs.main()` · `kbond_fills.main()` ·
   `kbond_kis.main()` 을 부른다. 넷 다 멱등하다.

무변경일 때 13초, 하루치 갱신이면 파싱 13초 + enrich 140초 + legs 114초.

DB 접속은 환경변수 `BW_MYSQL_{HOST,PORT,USER,PASSWORD}` 로만 한다.

### 예약 태스크

```
KBondDailyUpdate
  평일 16:30, pythonw.exe(콘솔창 없음), WorkingDirectory C:\Users\infomax
  StartWhenAvailable=True  DisallowStartIfOnBatteries=False
  StopIfGoingOnBatteries=False  ExecutionTimeLimit=PT2H  MultipleInstances=IgnoreNew
```
앞의 셋은 옛 `KBondLiveCollector` 를 죽인 설정이라 명시적으로 뒤집었다(D7).
트리거는 **평일 16:30 하나뿐**이다(`DaysOfWeek=62` = 월~금). 로그에 보이는
15:30·16:00 회차는 사람이 손으로 돌린 것이지 태스크가 아니다.

**2026-09-01 첫 실전 실행 — 절반만 성공했다.**
`LastRunTime` 16:30:01 · `NumberOfMissedRuns` 0 · **`LastTaskResult` 1073807364
= `0x40010004`(강제 종료)**. 본표 증분(+17,132행)까지는 끝냈고 그다음 다리 재생성
도중에 죽었다. **`LastTaskResult` 는 0 이 아니면 반드시 열어 볼 것** — 옛 침묵 실패
(`0x800710E0`)와 달리 이번엔 로그가 「다리 재생성」까지 정상으로 찍혀 있어서
로그만 봐서는 성공처럼 보인다.

### 컬럼

**54열** = parse 39 + enrich 15 (2026-09-02 기준).

- `parse_kbond_logs.COLUMNS` = **39열**. 여기가 파스 쪽 스키마의 **유일한 출처**다
  (`PA_SCHEMA` 도 같은 파일 모듈 레벨에 있고 순서 일치를 assert 한다).
- `enrich_kbond_quotes.DERIVED` = **15열**.
- `kbond_legs.parquet` = **42열**(Room/Date/Time/Sender 를 앞으로 + `LegIndex
  LegCount LegText`). 파생열은 없다.

**⚠ 새 열을 더할 때 반드시 볼 것 (T23).** `kbond_daily_update.rewrite()` 가
한때 **기존 parquet 의 스키마를 물려받아** 파서가 새로 만든 열을 조용히 버렸다.
범주 콜 4열이 그렇게 사라졌고 산출물이 47열로 나온 뒤에야 알았다.
지금은 `PA_SCHEMA` 에 있는데 파일에 없는 열을 이어붙이고 `[스키마] 새 열 N개 추가`
를 찍는다. **그 줄이 안 보이면 새 열은 안 실린 것이다.**

#### 2026-09-02 신설 7열

| 열 | 뜻 |
|---|---|
| `TenorLo`·`TenorHi` | 범주 콜의 잔존만기 밴드(년). «24년 3~4월» 같은 만기 연월 표기도 메시지 날짜를 빼서 여기로 모은다 |
| `SectorCat` | 범주 콜의 섹터. 여럿이면 `\|` 로 잇는다(`은행채\|공사채`) |
| `RatingCat` | 범주 콜의 등급 밴드(`AA-(이상)`). **오너 지시 밖에서 내가 더한 것** — 호가창 바구니에 등급이 없으면 «3년 회사채»가 한 바구니가 아니라서다 |
| `AuctionCode`·`AuctionTenor` | `국딱` 행에 붙인 그날 입찰 종목과 만기(§10) |
| `QuoteConfidence` | `QuoteMethod` 신뢰등급 {`high`, `med`, `identity`, `low`} |
| `FeeNote` | `half` = 수수료 반값(`수반`). **175,264행** [OWNER 회신] |
| `FeeWon` | 브로커 수수료 액수(원). `(수10)`=10전=0.1원, `(수0.1원)`. **32,079행** [외부 리서치+실측] |
| `MaturityDayNote` | `weekday`(249,110) / `holiday`(14,662) = 만기일의 요일 성격 [OWNER 회신] |

**`QuoteConfidence` 를 읽는 법.** 등급은 「민평에 얼마나 가까운가」가 **아니다** —
그 지표는 `at_mp` 에서 항등식이고 복원 계열에서도 자기참조다(T21). 기준은
**무엇을 근거로 값을 정했는가**다.

| 등급 | 방식 | 뜻 |
|---|---|---|
| `high` | handle2·handle3·stated_abs_tilde | 딜러가 소수부/절대금리를 직접 적었다 |
| `med` | stated_abs_pct·over_under·mp_plus_bp·spread_won | 문면 + 우리 규약/환산 모형이 한 겹 들어간다 |
| `identity` | at_mp | **QuoteYield ≡ 민평.** 「민평에 거래」라는 문면을 믿은 값이지 관측이 아니다 |
| `low` | stated_abs_bare | 마커 없는 맨몸 숫자. 25bp 초과 이탈 30.6% — **걸러야 한다** |

- **`SpreadWonAbs`** = 부호 없는 '원' 호가의 **크기만**. 부호는 못 되찾는다(M8).
- **`SpreadSource`** ∈ {`sign`, `overunder`, `swap`, `swap_unsigned`}.
  뒤 둘은 **종목간** 스프레드라 민평 대비로 쓰면 안 된다.
- **`QuoteRawSource`** ∈ {`tick`, `verb`}. `verb` 는 틱 없이 명시어로만 방향을
  말한 호가라 틱판보다 확실치 않다. **하류는 이 열로 가려 쓸 것.**
- `BondCode` 는 `Sector=='국고'` 일 때만 채운다. 다른 섹터의 `YY-N` 은 `SeriesNo` 로.

`Sector` 분포: 크레딧/기타 49.7%, 국고 32.3%, (없음) 9.2%, 통안 4.9%,
지방/첨가소화 0.9%, MBS 0.9%, CD 0.8%, 국민주택 0.7%, CP/전단 0.6%, IRS/스왑.

`MsgType` 분포: QUOTE 6,590,686 (79.25%), OTHER 613,355 (7.38%),
**AXE 508,630 (6.12%)**, INQUIRY 380,257 (4.57%), CONFIRM 166,115 (2.00%),
THANKS 56,896 (0.68%).

`BondCode` 는 `Sector=='국고'` 일 때만 채운다. 다른 섹터의 `YY-N` 은 `SeriesNo` 로 간다.

### 2026-09-01 4차 — 오너 자료(거래단가의 특수성)를 대조하다 나온 것

| 항목 | 규모 | 처리 |
|---|---|---|
| **`오버`/`언더` 한글 부호 표기** | 495,409 (스프레드 잡힌 건 3,184뿐이었다) | 수리 (D10) |
| 오탐A: `끝9원` 을 원 스프레드로 오인 | 11,695 | 수리 |
| 오탐B: 절대금리를 원으로 오인 (`민 3.578 … 3.70 팔자`) | 27,367 | 수리 |

오탐 둘은 **3차에서 내가 만든 회귀**다. 끝전 대 |원| 상관이 부호 없는 군에서만
+0.432 로 튀어 실물을 열어 보고 잡았다. **상관이 한 군에서만 튀면 추출을 의심할 것.**

### 절사 구조 확인 [OWNER 자료 대조]

- **끝전 분포가 균등하다.** `MPPriceFrac` 2,850,960행, 평균 **0.480**,
  10/25/50/75/90% = 0.08/0.22/0.48/0.73/0.89. 절사(내림) 구조가 만드는 모양 그대로다.
- **오너 예시가 재현된다.** 끝전 0.57원, 잔존 0.20년(D≈0.21) -> **금리 +2.71bp**.
  자료의 「2.793% -> 2.82%」(+2.7bp)와 일치한다.
- **비용은 듀레이션으로 나뉜다.** 같은 0.57원이 D=0.21 이면 2.71bp,
  D=0.97 이면 0.59bp, D=4.60 이면 0.12bp. 「만기가 짧을수록 커진다」가 이 뜻이다.
- **매도자가 실제로 보정한다.** 부호 있는 '원' 에서 `+` 비율이
  **SELL 62.0% 대 BUY 46.2%** 다. 파는 쪽이 `+원`을 더 자주 부른다.

### 검증 수치

- 정밀도 99.54% (뽑은 값이 원문에 실재하는가, 2,520만 건 대조)
- 재현율 누락 Amount/MPYield/Position/Maturity 0.00%, Broker 0.21%
- **`QUOTE` 중 값이 하나도 없는 행 = 0** (D9 로 AXE 를 갈랐으므로 정의상)
- **QuoteYield 확정 3,059,217 (QUOTE 의 46.37%)** — 수리 전 1,584,625 (22.22%)
- 방식별 민평 대비 |차이|

  | 방식 | n | 중앙 | 1bp 이내 | 5bp 이내 |
  |---|---|---|---|---|
  | handle2 | 829,217 | +0.00bp | 82.8% | 99.8% |
  | handle3 | 863,738 | +0.00bp | 77.6% | 98.5% |
  | decimal2 | 150,982 | +0.00bp | 61.3% | 91.8% |
  | spread_won | 766,816 | -1.06bp | — | 80.1% |
  | over_under | 492,413 | +1.00bp | 32.0% | 88.1% |

- 축약호가 복원 1,843,937 (축약호가 있는 행 2,406,146 의 76.6%)
- 원 -> bp 환산 904,852건, 0.25원 배수 97.62%
- 다리 표: 식별자 있는 다리 99.91%, 다리 수 == 고유 종목 수 98.78%,
  불필요 분할 0.91%, 아무것도 없는 다리 0건

### 2026-09-01 2차 수리 전후 (QUOTE 기준)

| 항목 | 전 | 후 | 증감 |
|---|---|---|---|
| MPYield 채움 | 4,016,995 (56.33%) | 4,287,591 (60.02%) | **+270,596** |
| QuoteRaw 채움 | 1,810,307 (25.39%) | 2,150,050 (30.10%) | **+339,743** |
| QuoteYield 확정 | 1,584,625 (22.22%) | 1,780,122 (24.92%) | **+195,497** |
| 값 0개 (누락) | 932,227 (13.07%) | 615,402 (8.61%) | **-316,825** |

### 2026-09-01 3차 수리 (오너 질문에서 나온 것)

오너가 「`22-15 사자` 는 호가를 요청하는 콜이고, `당사 4.2` 는 한 번 봐야
알지 않겠냐」고 짚었다. 둘 다 맞았다.

| 결함 | 규모 | 처리 |
|---|---|---|
| 3. 셋/세트와 동사가 벌어진 축약호가 (`335 34 세트 팔자`) | 31,238 | 수리 |
| 4. `RE_INQUIRY` 가 존댓말 둘을 빠뜨림 (`있으신가요` 27,164·`있으실까요` 12,659) | 46,350 | 수리 |
| 5. «민평 + 원 스프레드» 로 금리가 나오는데 비어 있음 | 841,947 | 수리(766,717 확정) |
| — 값 없는 매매 의사를 QUOTE 로 셈 | 508,630 | `AXE` 신설 (D9) |
| — 부호 없는 '원' | 140,592 | 크기만 `SpreadWonAbs`, 부호는 못 함 (M8) |

**`당사 4.2` 의 정체 = 원 단위 호가다.** `24-9 당사 0.8원 100억` 처럼 원을 적기도
하고 `25-4 당팔 1.8` 처럼 생략하기도 한다. 값 중앙 1.0원으로 D5 의 원 분포와 같다.
10 이상은 억을 생략한 수량이므로(`25-10 당팔 100`) 뺀다.

## 3. 함정 (실측으로 데인 것들)

**T1. `.txt` 는 UTF-8 이 아니라 CP949 다.** utf-8 로 열면 5,432개 파일이 전량 깨진다.
`.rtf` 는 `\ansicpg949` + `\'xx` 이스케이프라 `rtf_to_text(encoding='cp949')` 로 넘긴다.

**T2. 한 발언이 여러 줄로 쪼개진다.** `홍길동 (11:15:12) : ` 다음 줄에 본문이 온다.
헤더 정규식만 쓰면 그 본문을 통째로 잃는다. 헤더 없는 줄은 직전 발언에 이어 붙인다.

**T3. 파일명 규칙이 두 가지다.** `YYYYMMDD_HHMMSS_<방>.<ext>` 와
`채권_<방>_YYYYMMDD_HHMMSS.txt`. 둘 다 지원해야 899 그룹이 다 잡힌다.

**T4. 브로커 태그 괄호가 6종에 중첩과 짝불일치까지 있다.**
`[한양 (02-3770-5433)]`, `[KR 매크로투자 2090-4286)`, `<이베스트 3779-8609>`,
`{코리아에셋 02-560-6386}`, `▨ 부국 6118 - 9655 ▨`.
정규식 하나로는 안 된다. 오른쪽에서 괄호 짝을 세고, 대칭 구분자는 따로 처리한다.
전화번호는 공백 구분(`02 2122 9681`)도 있다.

**T5. 한글 약어는 반드시 앞뒤 경계를 채운다.**
`(?<![가-힣])국전(?![가-힣])` 안 하면 「한**국전**력」이 통안으로 잡힌다.
라틴 약어는 대소문자를 안 가려야 한다(`cd` 소문자로도 쓴다).

**T6. Parquet 스키마가 흔들린다.** 전부 결측인 컬럼은 그룹마다 `null` 타입으로
추론된다. `pa.schema` 를 못 박고 매 그룹 `cast` 한다.

**T7. `"0." + NaN` 은 0.0 으로 파싱된다.** `MPPriceFrac` 결측률이 100% 로 나왔던 원인.
`.where(frac.notna())` 로 결측을 결측으로 둔다.

**T8. 830만 행을 한 DataFrame 으로 들면 10GB 를 넘는다.**
(방,날짜) 그룹 단위로 만들어 CSV/Parquet 에 흘려보내고 통계는 증분 카운터로 쌓는다.

**T9. Windows 에서 `schtasks /Run` 을 Git Bash 로 부르면 인자가 깨진다.** PowerShell 로 부른다.

**T10. `to_csv(mode="a", header=not out.exists())` 는 스키마 드리프트를 못 막는다.**
헤더는 첫 append 때 것으로 굳는데 그 뒤 `COLUMNS` 가 바뀌면 이후 행은
**말 없이 열이 밀린 채** 쌓인다. 실측으로 25 -> 26 -> 27 세 판이 한 파일에 섞였다.
행 수만 세는 검증은 이걸 못 잡는다. **열 수를 세야 한다.**

**T11. `pq.ParquetFile` 을 안 닫으면 Windows 가 `os.replace` 를 거부한다.**
`PermissionError: [WinError 5]`. 임시파일 원자 교체 전에 `src.close()`.

**T12. T6(스키마 흔들림)은 표를 새로 만들 때마다 재발한다.**
`kbond_legs` 첫 판이 `Unsupported cast from large_string to null` 로 깨졌다.
행그룹 1에서 전부 결측이던 컬럼이 `null` 로 굳고 행그룹 3의 문자열을 못 받았다.
**추론에 맡기지 말고 `pa.schema` 를 못 박는다.**

**T15. 경로가 둘인데 한 분모로 묶으면 지표가 거짓말한다.**
«민평 + 원 스프레드» 경로를 붙였더니 복원률이 **108.5%** 로 찍혔다.
분모가 「축약호가 있는 행」인데 분자가 그 밖에서도 채워졌기 때문이다.
리포트를 경로별로 갈랐다. **지표가 100% 를 넘으면 분모를 의심할 것.**

**T14. 「행 수가 늘었다」는 품질 검증이 아니다.**
2026-09-01 에 추출 정규식을 넓혀 33.9만 행을 더 건졌는데, **그게 옳은지는
`QuoteVsMP_bp` 분포를 출처별로 갈라 봐야** 알 수 있었다(verb 5bp 이내 98.2%).
넓힌 정규식은 항상 (1) 기존 값을 안 흔드는지(상위집합인지) (2) 새 값이 독립
기준과 맞는지 둘 다 재고 나서 붙인다.

**T13. 예약 태스크 기본값 둘이 조용히 죽인다.**
`DisallowStartIfOnBatteries=True`(기본) 는 배터리에서 실행을 거부하고
(`0x800710E0`), `StartWhenAvailable=False`(기본) 는 놓친 실행을 그냥 버린다.
`Get-ScheduledTaskInfo` 의 `LastTaskResult` 와 `NumberOfMissedRuns` 를 확인할 것.

**T16. 침묵 실패보다 «부분 성공»이 잡기 어렵다.** (2026-09-01 실측)
T13 을 고쳐 태스크가 드디어 돌기 시작했는데, 첫 회차가 본표 증분은 끝내고
다리 재생성 도중 죽었다. 그 결과 **`kbond_legs.parquet` 이 footer 없이 잘렸는데**
겉으로는 아무 이상이 없었다 — 로그 마지막 줄은 「다리 재생성」으로 정상이고,
파일 크기도 6.9MB 로 그럴듯했다. 드러난 자리는 두 곳뿐이었다:
`LastTaskResult` = `0x40010004`(강제 종료), 그리고 `pyarrow` 로 실제 열어 본 것.

두 가지가 따라 나온다.
1. **파일 존재와 크기는 검증이 아니다.** parquet 은 `pq.ParquetFile(p).metadata` 가
   열려야 있는 것이다. footer 는 맨 끝에 쓰이므로 **잘린 파일일수록 앞부분은 멀쩡하다.**
2. **최종 경로에 직접 쓰면 죽었을 때 새 것도 없고 직전 것도 없다.**
   전 스크립트를 훑었더니 **다섯 중 넷이 그랬다.** 전부 임시 경로 → `os.replace`
   (+`except BaseException` 에서 임시본 삭제)로 고쳤다.

   | 스크립트 | 옛 상태 |
   |---|---|
   | `kbond_daily_update.py` | 이미 안전했다(본표 스플라이스가 `.tmp`+`os.replace`) |
   | `kbond_legs.py` | `ParquetWriter(OUT)` — **이번에 터진 것** |
   | `kbond_fills.py` | `ParquetWriter(OUT)` — 같은 구조 |
   | `kbond_kis.py` | `to_parquet(OUT)` |
   | `enrich_kbond_quotes.py` | `to_parquet(OUT)` + **CSV 를 먼저 지우고 청크 append** |
   | `parse_kbond_logs.py` | **부트스트랩이 두 산출물을 먼저 지우고** 흘려 씀 |

   뒤 둘이 특히 나빴다. 250MB 본표와 2.7GB CSV 를 지운 다음에 쓰기 시작하므로,
   그 사이에 죽으면 **되돌릴 것이 없다**. 임시본을 쓰려면 디스크가 잠깐 2배
   필요한데 여유 41GB 라 문제없다.

**T17. 문턱은 두 가설을 함께 떨어뜨릴 수 있다.** (2026-09-02, §10)
통안을 푼 «5bp 적중률» 을 국고 은어에 그대로 들이댔더니 배관검정 98.4% 인데
은어는 어느 칸도 46% 를 못 넘었다. 문턱이 **«만기가 틀렸다» 와 «만기는 맞는데
다른 종목이라 스프레드가 있다» 를 구분하지 못했다** — 15bp 벌어진 물건은 만기를
맞혀도 5bp 안에 못 든다. 통안에서 통한 이유는 통안 은어가 그 종목 «자신» 이라
스프레드가 0 이었기 때문이고, 그건 방법의 일반성이 아니라 그 문제의 성질이었다.
**성공한 방법을 옮길 때는 그 방법이 무엇을 가정했는지 같이 옮길 것.**
바꾼 자 = 부호 있는 **오프셋 분포** + 원형 집중도(0 일 필요 없이 «뭉치기만» 하면 된다).

**T18. 서로 다른 입력이 같은 답을 내면 그건 신호가 아니라 잡음의 모양이다.**
국당·국전·국전전은 뜻이 다른 세 낱말인데 1차 검정에서 **같은 칸(10Y r3)** 에
45.2~45.9% 로 떨어졌다. 그때 「10년물인가」가 아니라 「자가 틀렸나」를 물었어야 했다.

**T19. 한 문맥에서만 재면 선택 효과에 걸린다.** (`국딱` = 30년물 98%)
그 98%는 **교체 문맥 행에서만** 잰 값인데, 국딱 교체 행 1,293개 중 1,265개가
30년 입찰일에 놓여 있었다. **틀린 값이 아니라 틀린 모집단에서 잰 맞는 값**이다.
전량(4,952행)으로 넓히자 국딱은 만기가 아니라 «그날 입찰된 것» 이라는 상태어로
드러났다. **부분집합에서 나온 비율을 결론으로 올리기 전에 그 부분집합이 왜
그 부분집합인지 물을 것.**

**T20. 「1초 어긋난 이중 적재 2,427」은 60배 과소였다.** (2026-09-02)
실측하니 같은 (방,발신자,본문)이 1초 안에 다시 오는 것이 **152,950행**이고,
그중 **148,158행이 서로 다른 파일**(`.rtf` 내보내기 vs `.txt` 자동저장)에서 왔다.
같은 대화가 두 형식으로 저장되면서 타임스탬프가 1초 어긋난 것이다.
분포가 결정적이었다 — **1초 152,950 대 2초 7,153**, 그리고 2~12초는 7~14천으로
완만하게 는다. 1초에만 솟은 봉우리는 사람의 재게시가 아니다.
파서의 중복 키가 `(sender, tm, body)` 로 **정확한 시각**만 봐서 다 새고 있었다.
→ ±1초 창을 더했다. **창을 넓히면 안 된다** — 몇 분 뒤의 재게시는 진짜 시장
행동이고 §11 에서 지워선 안 될 사건이다.

**T21. 등급을 매길 때 항등식을 근거로 쓰면 안 된다.** (`QuoteConfidence` 신설)
방식별 「민평 대비 5bp 이내」 표에서 `at_mp` 가 **100%** 로 1등인데, 그건
`QuoteYield := 민평` 이라 **항등식**이다. 그 표로 등급을 매겼으면 가장 검증이
안 된 방식에 최고 등급이 갔을 것이다. 복원 계열(handle2/3)도 «민평에 가장
가까운 후보» 를 고르므로 그 지표가 부분적으로 자기참조다.
→ 등급 기준을 **「무엇을 근거로 값을 정했는가」**(문면이 말한 것인가, 우리가
모형으로 채운 것인가)로 바꾸고, `at_mp` 는 `identity` 라는 별도 값으로 뺐다.

**T24. 재파싱이 도는 동안 그 parquet 을 읽으면 교체가 거부된다.** (2026-09-02)
별칭 대조 때문에 `kbond_structured_data.parquet` 을 4분쯤 읽고 있었는데, 그 사이
재파싱이 끝나 `os.replace` 를 시도하다 **`PermissionError: [WinError 5]`** 로 죽었다.
T16 의 원자적 교체가 제 몫을 해서 **원본은 무사했고 임시본도 온전했다**
(8,184,985행 53열) — 임시본을 승격하고 하류 단계만 이어 돌려 파싱을 다시 안 했다.
**교훈: 긴 읽기와 재파싱을 겹치지 말 것.** 겹쳤다면 임시본부터 확인할 것 —
버리고 처음부터 다시 도는 것은 대개 낭비다.

**T23. 스플라이스가 옛 스키마를 물려받아 새 열을 조용히 버린다.** (범주 콜 신설 중)
`kbond_daily_update.rewrite()` 가 `schema = src.schema_arrow` 로 **기존 parquet 의**
스키마를 잡고 `df = df[names]` 로 잘라 냈다. 그래서 파서가 새로 만든 4열이
한 톨도 안 실렸는데 **아무 경고도 없었다** — 51열이어야 할 산출물이 47열로
나온 걸 세어 보고서야 알았다. `--force` 도 같은 경로라 전량 재파싱으로도 안 고쳐진다.
→ `PA_SCHEMA` 를 모듈 레벨로 올려 파스 쪽 스키마의 출처를 하나로 만들고,
파일에 없는 열은 이어붙이며 `[스키마] 새 열 N개 추가` 를 찍게 했다.
**교훈: 열을 더했으면 산출물의 «열 수»를 세라. 행 수만 세면 안 보인다**
— 이 레인이 옛날에 겪은 「헤더 25열 아래 26열 행 60.3%」와 같은 자리다.

**T22. 침묵하는 `except` 가 배선 실패를 감춘다.** (범주 콜 신설 중)
범주 콜 추출을 `extract(body)` 안에 넣으면서 `date` 를 참조했는데 그 함수는
`date` 를 **받지도 않았다.** `try/except Exception` 이 `NameError` 를 먹어서
「만기 연월 축이 원래 잘 안 잡히나 보다」로 보였을 것이다.
→ 서명에 `date` 를 더하고 호출부 둘(`parse_group`·`kbond_legs`)을 같이 고쳤다.
**예외를 삼키는 자리에서는 무엇을 삼켰는지 한 번은 봐야 한다.**

---

## 4. 미결

**M1 닫힘 2026-09-01 [OWNER 승인].** `kbond_legs.parquet` 신설.
옛 문서는 439행이라 적었는데 **실제는 220,053 메시지 / 466,456 다리**(QUOTE 의 3.08%)다.
439 는 25bp 가드가 잡은 부분집합일 뿐이었다 — D6 이 말한 그대로다.

다리 경계는 **식별자가 아니라 매매 동사와 겹쳐서** 정한다. 셋 다 실측이다.
- 식별자만 세면 안 된다: 식별자 2개 이상이 28.17% 인데 대부분 한 종목을 두 방식으로
  적은 것이다 (`26.2.2 JB우리캐피탈 502-5(AA-)팔자` = 만기일 + 발행체회차).
- 동사만 세도 안 된다: 동사 2개가 5.43% 인데 대부분 되풀이다
  (`신한캐피탈 ... 팔자...민팔자`).
- 그래서 **새 앵커가 나오되 직전 앵커 이후에 이미 동사가 있었으면** 거기서 끊는다.
- 표기 순서가 두 갈래라 한 번 더 손본다. 이름이 만기일 앞에 오는 표기
  (`한국전력1425 [29.10.2 화 민2.911] 팔자`)를 앵커에서 그냥 자르면 이름이 앞 다리에
  붙고 섹터까지 뒤집힌다(실측: 앞 다리가 '광주광역시지방채' 를 물어 지방채가 됐다).
  직전 동사와 다음 앵커 사이에 **값이 없고 낱말만 있으면** 그 낱말부터 새 다리다.
단위 검정 10건이 `kbond_legs.py` 독스트링에 있다.
본 표는 손대지 않았다. 조인 키는 (Room, Date, Time, Sender).

**M2 닫힘 2026-09-01 [전수조사].** `+2bp` 식 기호 bp 의 부호 규약은 **DECODED**:
`+` = 금리 상승(오버와 동일 규약). clean 부분집합(34,322행) 일치 99.39%,
|절대-민평|≤25bp 조건에서 **99.94%**. 옛 55.3% 는 부호 문제가 아니라 오염 3종의 합이었다 —
(a) FRN/CD 쿠폰 리셋식 `CD91+59bp` 오포착 15,015행(부호가 전량 + 인 이유),
(b) RE_ABS_YIELD 가 쿠폰을 절대금리로 오취해 검증축이 깨진 것 10,482행(97.4%가 쿠폰값),
(c) 다다리 메시지의 행 단위 삼자 짝짓기 12,173행(다리표 단위로 하면 해소).
교체(스위치) 스프레드 11,414행도 별도 해독됨: **bp = 신형(YY-N 큰 쪽) − 구형 수익률차 수준**
(민평 대비 마진 아님 · 매매 방향·언급 순서와 무관 · 판별표본 94.1% · '민스플' = 민평 스프레드
그대로 교체, 잔차 중앙 0.00bp).

**M3 닫힘 2026-09-01 [전수조사].** 옛 84.43% 는 «가짜 틱» 탓이었다. 불일치를 전수
분류하면 — 교체 두 다리 14,418(정당) · 신용등급 괄호 `(A2+)/(A3+)` 가짜 틱 6,035 ·
교체 단틱 583 · 범위/FRN 356 · 양방향 호가 169 · 원부호 79 … 잔여 재분류 후
**진짜 모순은 1행**('국딱 매수 262-'). 틱 자체의 방향 신뢰도는 **99.96%+**.
남은 수리거리: RE_TICK 에 등급 토큰 배제(한 줄), 다리표 Tick 재추출(교체 방향 정보 사장 중).

**M4 소멸 2026-09-01.** 수집기를 내려 소스가 하나가 됐다(D7).

**M5 재진단 2026-09-01. 「크레딧 민평 소스가 없다」는 진단이 절반 틀렸다.**
본문에 민평이 적혀 있는데 정규식이 못 읽던 행이 264,092개였고,
그중 **263,674개(99.8%)가 바로 DB 민평이 없는 종목**이었다(크레딧 252,107).
소스가 없던 게 아니라 딜러가 써 준 걸 버리고 있었다. 수리 후 잔여 0.
남은 복원 실패는 여전히 종목별 민평이 있어야 풀리지만, 모수가 줄었다.

**M8 재해석 2026-09-01 [전수조사] — 부호 질문 자체가 틀렸었다.**
잔여 25,363행의 91.5%가 당팔/당사(선팔·선사) 계열인데, 이 원은 **민평 대비 스프레드가
아니라 결제일 스위치의 가격 조정**이다. 결정 증거 = «23-5 당사선팔 100억 **1일** 1.05»
의 '1일' 명시 + 값/1일경과이자(민평x10000/36500) **중앙 비율 1.04**, 72.9%가 ±30% 밴드
(2일 가설은 5.4%로 기각). 값이 0.7/0.8/1.0/1.05 라운드 관례라 종목별 점상관은 낮음 —
축(원 단위·1일 캐리 스케일)은 확정, 세부 경제내용(경과이자+레포)은 미분해.
→ 방향을 물을 게 아니라 `settle-switch` 로 재분류하는 것이 옳다. 오너 질문 철회.

**M9 닫힘 2026-09-01.** 「`+2bp` 식 기호 bp 로는 QuoteYield 를 채우지 않는다」는
M2 가 부호 신뢰도를 55.3% 로 표시해 둔 동안의 유보였다. M2 가 99.94% 로 닫히면서
같이 풀렸다 — `enrich_kbond_quotes.py` 의 (가) 블록이 `SpreadSource=='sign'` 을
`mp_plus_bp` 로 올린다. `swap`/`swap_unsigned` 는 종목간 스프레드라 계속 제외한다.
**2026-09-02 에 보니 코드는 이미 그렇게 하고 있는데 이 절과 코드 주석만 옛
상태였다 — 둘 다 고쳤다.**
⚠ 숫자 둘을 헷갈리지 말 것: `SpreadSource=='sign'` 인 **행**이 947,617 이고,
그중 QuoteYield 가 비어 있어 **이 경로로 채워진 것**은 37,708 이다(나머지는 앞선
경로가 이미 채웠다). 처음에 이 절에 947,617 을 적었는데 틀린 배치였다.

## 5. 데이터 소스

- 과거 로그 `C:\Users\infomax\Documents\K-Bond Messenger Chat` (8,315파일, 자동 저장분)
- 민평 `infomax.국고통_민평` (일자, 종목코드=ISIN, 민평).
  **접두는 셋뿐이다** — KR1035 국고 166종 · KR3101 통안 183종 · KRC035 분리채 317종.
  **국민주택은 없다**(`Bond Info` 에 2022년 만기 1종, `국채_발행정보` 에 0종).
- 지표코드 -> ISIN `infomax.ontherun_schedule` 의 `종목명` `국고02875-4409(24-10)` 에서 추출.
  218종 매핑, 데이터 커버리지 98.3%. 제로패딩 정규화 필요(`23-01` = `23-1`).
  **⚠ 물가채가 만기 10 에 섞여 있다** — 만기별로 쓸 때는 `종목명 LIKE '국고%'` 로 거른다.
- **`infomax.국채_입찰`** — 입찰일·표준코드·종목명·회차·만기·**낙찰금리**.
  §10 에서 `국딱`을 푼 축이다. 국고채는 **입찰일마다 만기가 하나만** 열린다
  (246일 전부 `nunique=1`). 컬럼명은 `일자` 가 아니라 **`입찰일`** 이고
  `만기` 는 숫자가 아니라 **문자열**이다(`'30'`).

---

## 6. 다음에 할 일 (우선순위 순)

**2026-09-02 [OWNER] 지시 다섯을 받아 넷을 집행했다.** 1번은 방향 선언이라
§11 로 옮겼다.

| # | 오너 지시 | 상태 |
|---|---|---|
| 1 | 「장외 Order Book Dynamics 구현할 생각」 | **§11 신설.** 설계 지시 대기 |
| 2 | 범주 콜 스키마 「신설하세요」 | **집행** — `TenorLo`·`TenorHi`·`SectorCat`(+`RatingCat`) |
| 3 | 국딱에 민평 「붙이세요」 | **집행** — `AuctionCode`·`AuctionTenor` |
| 4 | 「은어 표기로 보내줘」 | **집행** — `kbond_은어_목록.xlsx` |
| 5 | 「기계적 수리 진행」 | **집행** — 넷 중 셋 완료, 다리 괄호짝만 남음 |

### 남은 것

1. **§11 설계 지시 대기.** 호가 수명·바구니 단위·재게시 처리 셋은 **데이터가
   못 정한다.** 오너가 정해 주셔야 시작한다.
2. **은어 목록 회신 대기.** 오너가 뜻을 적어 주시면 파이프라인에 넣는다.
   가장 큰 미해독어는 **`수반` 178,524행**이다.
3. **다리 괄호짝 12,640** — 기계적 수리 넷 중 유일하게 안 한 것. 머리말이
   분리된 탓이라 결합하면 정합한다는 진단까지만 돼 있고 고치지 않았다.
   `kbond_legs._split_two_sided` 와 `split_legs` 의 괄호 깊이 검사가
   **머리말을 뺀 다리 본문만 보고 있어서** 짝이 안 맞는 것으로 보인다.
4. **`kbond_fills` 중복 제거 미구현.** 같은 거래가 양쪽에서 두 번 보고되면
   두 행이다. §11 에서 체결을 세려면 이게 먼저다.
5. 그 다음은 §11 이 정한다.

## 7. 전수조사 2026-09-01 (워크플로 18에이전트 · 9차원 + 반박검증 + 완결성비판)

830만 행을 9개 차원으로 완전 분할했고, 비판자가 커버리지 매트릭스로 구멍 없음을 확인했다
(Date/Time/Room/Sender 결측 0 · 완전중복 0 · 주말 행 0). 결과 전문은 워크플로 산출물,
여기엔 결론만.

### 진짜 불명확 잔여 (해독 시도 후에도 실패 — 전부 합쳐 ~600행)

| 형태 | 행수 | 예시 | 막힌 이유 |
|---|---|---|---|
| 겹친 부호 파편 | ~295 | `+305++` `33+-` `--` | 값·종목은 직전 문맥으로 ~85% 복원되나 방향 판별 신호 없음 |
| NNN로 / 5자리 숫자 | 201 | `917로` `39395` | 체결 레벨 가설이 44.2%로 문턱 미달(135/147이 한 발신자) |
| 축약복원 실패 잔여 | 170 | `23-4 745 팔자 20억`(민 2.314) | 스테일 재호가/오프마켓/오귀속 판별 불가 |
| 단발 불명 | 1 | `국딱 매수 262- 동금리 가능` | n=1, 인접 문맥 없음 |

### 불명확이 아니라 «사전이 좁아서» 새던 것 (수리 대기열, 규모순)

1. **통안 별칭 사전 부재 — 최대 수확.** 구통·삼통·구구통·구구삼통·국통·통딱·국딱·
   91일통·N년통 등이 섹터 사전에 없어 **OTHER 로 181,788행**(틱 132,937 동반), 섹터
   결측으로 193,675행. 은어→만기 매핑은 KIS 참조 대비 97~99.7%로 검증됨.
2. **민팔자 계열 승격 ~198K** — '민평매도/민팔자' 명시행은 QuoteYield=민평
   (오차 중앙 +0.6bp · 쿠폰 필터 후 95.2%가 5bp 이내). 단 확정호가와 구분 표기 필수.
3. **AbsYield 승격 ~98K** — 쿠폰 필터를 '쿠\s?\.?\d' 축약까지 넓히면 오염 9.0%→0.8%.
4. **MP+bp 결합 ~56K** — enrich 가 MPYieldDB 만 쓰고 문면 MPYield 를 안 씀.
5. **공백/슬래시 만기 15,613** — RE_MATURITY 가 점만 받아 '25 05 02'·'29/4/29' 를 놓쳐
   진짜 호가가 OTHER 로. 구분자 [.\s/] 한 줄.
6. **AXE 의 6.1%(31,279)는 실은 값 보유** — 맨몸 수익률 22,350('신한캐 3.741 팔자' 류,
   마커 없음) + 무부호 bp 교체 8,211 + 기타. AXE=값없음 전제의 하류 분석은 이만큼 오염.
7. **RE_TICK 등급 배제** — `(A2+)` 가짜 틱 6,035행(한 줄 수리).
8. **다리표**: Position 메시지 폴백(결손 10,200 전량 회수) · 괄호 짝 인지(12,164) ·
   활용형 동사(삽니다/파는 2,367).
9. **Amount**: '50억*2장' 은 장당 크기로 과소 30,463행 · '50*2장' 무억 표기 전량 유실
   11,942행 · '0.6억'→0 절사 7,499행.
10. **어휘 확장**: ㄳ/고맙(→THANKS ~5,400) · 거래/체결(→CONFIRM 후보 27,422) ·
    추팔/추사(2,280) · 한 자리 회차 '9-5'=09-5(당일 민평 대조 94~100% 확증) ·
    씨디/씨피 한글 CD/CP(7,093) · 점 축약 통안만기 '25.42통'=25.4.2(91.8% 유일해독).
11. **CONFIRM 복합문 42,146행 = 체결 기록 원석**(종목+방향+레벨, 수량 포함 16,854).
    순수 ㅎㅈ 104,931도 직전 QUOTE 링크로 체결가 복원 가능성.
12. **비판자 지적**: QuoteYield 채워진 2.81M 은 이번에 정확도 미감사 — 쿠폰 오염
    후보 ≥11,531행(문면 AbsYield 와 >50bp 불일치) 별도 감사 필요. Broker 결측
    218,799(2.63%)의 성격(무태그 정상 vs 파싱 실패) 미판별. Maturity 달력 불가능 값
    144행(30.40.2 류 오타).

### 6차 수리 집행 2026-09-01 [OWNER 승인: 기계적 수리 전부 + 통안 별칭 + fills]

단위 검정 43/43 통과 후 전량 재생성. 전후 비교:

| 지표 | 전수조사 시점 | 수리 후 |
|---|---|---|
| QUOTE | 6,591,439 | **6,818,056** |
| OTHER | 613,447 | **300,560** (−51%) |
| QuoteYield 확정 | 3,059,436 (46.4%) | **3,379,951 (49.6%)** |
| 틱·명시어 일치 | 84.43% | **99.22%** |
| M2 부호 일치(clean) | 55.3% | **99.88%** (≤25bp 에선 99.94%) |
| Maturity 달력 오류 | 144 | **0** |

붙인 것 — 통안 별칭 사전(구통/삼통/구구통/구구삼통/통딱/국딱/N일통 + `국고/통안`
혼합 섹터 + 접미 결합형) · 크레딧 부문어 폴백 · 씨디/씨피 한글 · 만기 구분자 `[./]`
+ 공백판 + 달력 검증 · 한 자리 회차 `9-5`→`09-5` · RE_TICK 등급 배제 · 동사 확장
(추팔/추사/삽니다/파는) · THANKS 에 ㄳ·고맙 · FILL(체결/거래) · FRN 리셋식 배제
· 쿠폰 마스킹 · 맨몸 수익률 · verb 오탐 앞경계 · Amount 곱표기·소수억·마지막매치
· enrich 승격 3종(`mp_plus_bp` 39,389 · `stated_abs` 135,124 · `at_mp` 194,629).

**교체 스프레드는 `SpreadSource` 로 갈라 뒀다** — `swap`(38,594) / `swap_unsigned`
(7,698). 신형−구형 수익률차 '수준'이라 민평 대비로 쓰면 안 된다. `sign`(947,617)과
`overunder`(499,564)만 민평 대비다.

**집행 중 내가 만든 회귀 하나를 잡았다.** `RE_FILL='체결|거래'` 가 «거래 후 추가팔자»
23,150행(Position 22,801·값 17,912)을 CONFIRM 으로 밀어냈다 — 그건 체결 응답이
아니라 **새 호가**다. `(?!\s*(?:후|뒤))` 를 붙여 32행으로 줄였고, 체결 사실은
`kbond_fills` 가 `FillKind='reported_then_quote'`(22,415)로 따로 담는다.
**한 메시지가 두 역할을 하면 MsgType 은 주된 행위, 사실은 전용 표로.**

### 범주 콜 111,091행 — 데이터 성질상 현 스키마 밖

'2년 특은 사자' '1년 언저리 카드채 매수관심' — 종목 식별자가 원문에 없어 영원히 OTHER.
잡으려면 별도 스키마(만기구간+섹터범주+방향) 신설이 필요하다. [OWNER 결정 대상]

## 8. 전면 감사와 8차 수리 2026-09-01

6차 수리를 끝내고 «QuoteYield 채워진 3.38M 은 이번에도 감사하지 않았다» 고 적어
두었다가 [OWNER] 「무슨 소리니 그게,, 다 감사해라」 를 들었다. 옳은 지적이다 —
공백을 지목해 놓고 넘어간 것이 잘못이었다.

### 감사 설계 (워크플로 21에이전트)

10개 면을 **적대적으로** 감사했다(「내가 붙인 수리가 틀렸을 가능성을 겨냥해 파라」).
critical/major 주장마다 반박 전담이 붙어 독립 재계산했고, 마지막에 비판자가
«모든 검증축이 같은 파이프라인의 파생열이면 순환논증» 을 따졌다.
판정: CONFIRMED 32 · UNDERSTATED 4 · OVERSTATED 9 · REFUTED 2.

### ★내가 6차에서 만든 회귀 (전부 CONFIRMED)

| 결함 | 규모 | 뿌리 |
|---|---|---|
| `RE_BOND_CODE_1` 앞경계 누락 | 51,811 | 두 자리 규칙엔 있는 `(?<![가-힣A-Za-z])` 를 한 자리에 안 넣었다. `네이버4-2`→국고 `04-2`. 제로패딩(`4-2` vs `04-2`) 탓에 `BondName` 가드가 **구조적으로 도달 불가**였다 |
| **`동통`을 통안 은어로 오인** | 70,777 | 말미 **전송/통보 표식**이다. 국고 지표물 27,591행의 BondCode 까지 빼앗았다 |
| `RE_ABS_BARE` 문두 만기일 | 1,016 | `6.28 스탠차CD 팔자` → QuoteYield 6.28%(그날 중앙 3.56%) |
| `mp_plus_bp` 스위치 오독 | 2,535 | `RE_SWAP` 이 「교체」 낱말만 봐서 `팔고…사자` 형을 못 잡았다 |
| `at_mp` 가 `over_under` 선점 | 1,492 | SELL 행에 BID 레벨이 실렸다 |
| 동사 확장이 교체 방향 뒤집음 | 1,269 | `사고…파는` 이 단일 SELL 로 |
| `RE_FILL` 이 토큰 기반 | 3,368 | 「후/뒤」 **글자**에만 반응 |
| `RE_AMOUNT` 좌측 경계 | 83 | `민3.845.100억` → 845.1억 |
| AXE 에 QuoteYield | 15,353 | D9 계약 파기 |

**내 측정도 틀렸다** — 「틱 일치율 99.22%」는 분모에 200만 행을 넣어 희석한 값이고
정확히는 **94.26%**(잔여 833행)였다. 「M2 clean 99.88%」도 census 정의를 그대로
복사해야 나오는 수치인데 내가 다른 정의로 재서 88.19% 를 보고했다.
**남의 수치를 재현할 땐 정의를 그대로 복사할 것.**

### 원래 있던 것 중 처음 드러난 것

- **`spread_won` 772,236행 전량 계통 저평가** — 원 오프셋 중앙 0.434원, 잔존만기
  8구간에서 평평(매수/매도·부호 무관). **오너 자료가 말한 「민평 단가 절사」** 를
  환산식에 안 넣은 것이다. 내가 절사 구조를 확인해 놓고 흘렸다.
- **`decimal2` 는 존재하지 않는 관행** 150,110행. 세트 표기 `NNN/NN` 을 독립축으로
  삼으면 2자리 짝을 `handle2` 로 읽을 때 앵커와 정확히 0.5bp 인 비율 99.6%,
  `decimal2` 로 읽으면 0.78%. 3자리 토큰 끝자리는 99.82%가 '5'(0.5bp 격자)인데
  `decimal2` 산출값만 격자를 벗어난다.
- **정수부 후보가 `floor` 하나** — 핸들 경계에서 `decimal2` 가 낚아채 10,080행이
  ~10bp 틀렸고, 25bp 가드에 버려진 3자리 5,735행은 `floor±1` 로 구제된다.
- **`MPPriceFrac` `끝전0.4`→0.0** 53,595행(범위검사를 통과하는 틀린 값).
- **Broker 결측 21.8만의 95.6%가 파싱 실패** — `(bgc 2033 0966)` 두 덩이 공백.
- **다리표 Broker 470,120 전량 유실** · **교체 다리 방향 88,888 뭉개짐**.
- **`QuoteVsMP_bp` 파생 경로에서 전멸** 1,244,729행(문면 민평을 썼는데 DB 민평으로 뺐다).

### 8차 수리 결과 [OWNER 승인: P0+P1+P2 전부]

수리 41건 · 단위 검정 통과 후 전량 재생성 · 검증 **34/34 PASS**.

| 지표 | 감사 시점 | 수리 후 |
|---|---|---|
| 한 자리 회차 오염 | 51,811 | **1,952** |
| `동통` 단독 통안 배정 | 70,777 | **0** |
| `decimal2` | 150,110 | **0** (handle2 로 흡수) |
| 축약복원 | 1,688,762 (77.8%) | **1,865,391 (84.0%)** |
| `spread_won` 오차 중앙 | -1.06bp | **+0.05bp** |
| `끝전0.4`→0.0 | 49,696 | **18** |
| `stated_abs_bare` | 22,461 | **5,937** |
| `at_mp` 선점 | 3,156 | **0** |
| Broker 결측 | 218,839 | **28,245** |
| `QuoteVsMP_bp` 확보 | 약 130만 | **3,439,337** |
| 다리 SWAP 뭉갬 | 88,888 | **2,729** |
| 다리 Broker 결측 | 470,120 | **2,335** |
| AXE 에 QuoteYield | 15,353 | **0** |

### 신설 열 (38 -> 42열)

- **`BrokerKey`** — 전화번호 정규화 키. 894개 데스크가 1,506개 문자열로 흩어져
  있었다(`흥국증권 FICC SALES 6742-3574` 71,537 / `흥국 FICC 6742-3574` 29,525).
  **하류 groupby 는 반드시 이걸 쓴다.** 표시용 이름은 키별 최빈 문자열.
- **`SenderKey`** — 말미 마침표·회사접사 제거(`이정훈` 과 `이정훈.` 은 동일인).
  1,072 표기 -> 1,012 키.
- **`MPBase`** — QuoteYield 를 만들 때 **실제로 쓴 민평**(문면 우선, 없으면 DB).
  `QuoteVsMP_bp` 는 이 기준이다.
- **`QuoteBlockReason`** ∈ {`no_mp` 535,550 · `unresolved` 117,888 · `no_ttm` 56,479}
  — 「호가가 없는 행」과 「호가는 있는데 못 푼 행」을 가른다.
- `QuoteMethod` 의 `stated_abs` 가 셋으로 갈렸다: `_pct`(105,373, 25bp 초과 이탈 5.8%)
  / `_tilde`(7,788, 0.5%) / **`_bare`(5,937, 30.6% — 하류는 이걸 걸러야 한다)**.
- 체결표에 `FillAmount`(체결어 인접 억만) · `RepostSeq`(재게시 순번).

### D9 계약 개정

「AXE = 값 없음」의 «값» 에 **민평 명시**(`민평 팔자`·`민 사자`)를 포함한다.
문면이 「민평 수준에 거래」라고 **수준을 말한 것**이므로 호가다. 그래서 AXE 에
QuoteYield 가 실리는 모순이 사라졌다(파서가 `RE_AT_MP_MARK` 로 값을 인정).

### 남은 것

- 양방 토큰 `405.41` 복원 불가 22,265 · 다리 괄호 짝 10,500 · 한 자리 오염 잔여 1,952
- **양방향 호가 62,395행은 스키마가 못 담는다** — 한 행에 bid/ask 가 둘 다 있는데
  `Position` 이 스칼라다. 다리표로 넘겨야 하고, 그건 구조 변경이다.
- 통안 407,926행 축약호가는 민평이 안 붙어 영구 미복원(통안 ISIN + 은어->만기 매핑
  테이블이 있어야 풀린다 — **데이터 조달 필요**).
- 겹치는 로그 두 벌의 1초 어긋난 이중 적재 2,427행.
## 9. 통안 해독과 조인 2026-09-01 (9차)

8차를 끝내며 「통안 407,926행은 통안 ISIN + 은어→만기 매핑 테이블 조달이 필요하다」고
적었다가 [OWNER] 「구조적으로 못 고칠 이유가 없다. 외부 리서치해서 잘 찾아보셈」 을
들었다. 옳았다 — **조달이 아니라 조인을 안 한 것**이었다.

### DB 에 이미 다 있었다

| 테이블 | 내용 |
|---|---|
| **`통안채_발행정보`** | 183종 · `표준코드`·**`종목명`(`25.07.02통`)**·발행일·만기일·만기구분 |
| `국고통_민평` 의 `KR31…` | 그 183종의 민평, 2013-02 ~ 2026-08 |
| `통안채_입찰`, `matrix_통안증권` | 입찰 결과 · 만기구간별 금리 |

`load_mp()` 가 `ontherun_schedule`(국고 212 + 물가 11, **통안 0종**)로만 조인해서
통안에 민평이 한 행도 안 붙고 있었다.

### 종목명 뒤 두 자리는 만기일이 아니라 만기 년수다

마스터 `25.07.02통` 의 `02` 는 **2년물**이라는 뜻이다(01=1년·02=2년·03=3년).
그런데 메신저는 1년물을 **만기일**로도 부른다 — `25.05.09통`(2025-05-09 만기).
1Y 만기일이 8/9/10일이라 마스터 종목명 `25.05.01통` 과 안 맞는다.
그래서 조인을 **두 갈래로** 시도한다(종목명 직결 + 만기일 역인덱스). 이걸 안 하면
직접 표기 90,799행 중 31.8%가 조인에 실패한다.

### 결과

| 지표 | 8차 | 9차 |
|---|---|---|
| 통안 민평 확보 | **0행** | **476,639행 (90.8%)** |
| 통안 MSBCode 확보 | 없음 | **488,654 (93.1%)** — 은어 406,570 · 종목표기 82,084 |
| 통안 QuoteYield | 0 | **350,895 (66.9%)** |
| 축약호가 복원 | 84.0% | **96.8%** |
| QuoteYield 확정(QUOTE) | 49.9% | **54.9%** |
| `no_mp` 미확정 | 535,550 | **192,291** |

**품질** — 통안 QuoteVsMP_bp 중앙 **+0.00bp**, 1bp 이내 65.9%, 5bp 이내 **97.9%**.
우연이 아니다: 은어를 인접 순위로 바꾸면 적중률이 13~19%p 떨어진다.

### 신설 열 (42 -> 44)

- **`MSBCode`** — 통안 표준코드(ISIN). 국고의 `BondCode` 에 대응한다.
- **`MSBSource`** ∈ {`item`(종목 직접 표기), `slang`(은어 사다리)}. `item` 이 더 구체적이라
  우선한다. 신뢰도가 다르므로 **하류는 이 열로 갈라 쓸 것.**

### 아직 못 붙이는 통안

**할인채(`91일통`·`364일통`·`1년통`·`1.5년통`) 2,878행** — `통안채_발행정보` 183종이
전부 이표채(1Y 101 · 2.0Y 72 · 3.0Y 10)이고 할인채는 0종이다. [OWNER 결정]
섹터만 주고 값은 포기한다(`QuoteBlockReason='no_mp'`). 규모가 0.03% 라 억지로
만들면 틀린 값이 생긴다. ※ `91일통 낙찰단가` 는 입찰 결과라 민평과 다른 축이다.

### KIS 예상종가 표 — 외부 검증축 [OWNER 승인, 구현 완료]

KIS자산평가가 메신저에 뿌리는 표가 **424행·335일** 있다. 은어별 금리와 국고 지표물
금리가 한 표에 들어 있어 은어→발행순위 배정을 **매일 독립 검증**할 수 있다.

```
: KIS 자산평가 예상 종가입니다.
1년통 3.385 ( 2.0 )  1.5년통 3.490 ( 6.0 )  구통 3.487 ( 6.5 )
통당  3.462 ( 6.5 )  삼통당  3.495 ( 6.8 )
===================== 14-2 3.450 ( -0.5 )  19-1 3.450 ( -0.5 ) …
```

`kbond_kis.py` 가 파싱해 `kbond_kis_curve.parquet`(25,061행 · 335일)로 남긴다.

**이게 D11 을 파이프라인 밖에서 확증했다.** 우리가 배정한 종목의 민평과
KIS 가 적은 은어 금리를 날마다 대조한 결과:

| 은어 | 우리 배정 | 중앙 | 1bp 이내 | 대안 순위 중앙 |
|---|---|---|---|---|
| `통당` | 2.0Y r0 | **+0.00bp** | **97.9%** | r1 -2.0 · r2 -1.9 |
| `구통` | 2.0Y r1 | **+0.00bp** | **98.2%** | r0 +2.2 · r3 +1.7 |
| `삼통당` | 3.0Y r0 | **+0.00bp** | **99.7%** | r1 +0.2 · r3 +5.4 |
| `1.5년통` | 2.0Y r2 | **+0.00bp** | **98.2%** | r1 -0.1 · r3 +1.3 |
| `1년통` | 2.0Y r4 | **+0.00bp** | **81.8%** | 1Y r1 -3.3 |

**⚠ 파싱 함정** — 은어를 얼개(`[가-힣]{2,5}통`)로 잡으면 안 된다. `구통`은 통 앞이
한 자라 걸리지 않고 `통당`·`삼통당`은 **당으로 끝난다**. 낱말 목록으로 못 박고
긴 낱말을 앞에 둘 것.

값 복원에는 아직 안 쓴다(검증 전용). 쓰려면 `QuoteMethod='kis_est'` 로 갈라
출처를 남길 것 — '예상' 종가이고 335일치라 640 영업일의 52% 만 덮는다.

### 양방 토큰과 양방향 호가 (10차)

**양방 토큰 22,265행 회수.** `405.41+` 같은 세트 표기를 `RE_QUOTE_RAW` 가 한 덩이로
잡는데 `restore()` 가 점을 지우면 4~6자리가 되어 어느 후보에도 안 걸리고 조용히
NULL 이 됐다. 점을 다리 구분자로 보고 **뒤 다리**를 쓴다(`405.41`→`41`→2.41).
두 다리가 다 필요하면 `kbond_legs` 몫이다.

**양방향 호가 62,395행을 다리로.** `한국전력(…) 3.70% 팔자 / 3.75% 사자` 처럼
종목이 하나여도 한 줄에 매수·매도가 다 적힌 줄이 본표에서는 단일 SELL 로 접혔다.
「Position 이 스칼라라 구조적으로 못 고친다」고 적었는데 틀렸다 — **다리표가
이미 있고**, 앵커가 없거나 다리가 하나로 끝날 때 «값+동사 쌍이 둘 이상이고
양쪽 방향이 다 있으면» 쪼개면 된다. 종목명은 머리말로 보존한다.

**⚠ 분해에도 괄호 깊이 검사가 필요하다.** 처음엔 안 넣어서 `AA-) 팔자` 처럼 괄호 안에서
잘려 다리 16,301개의 짝이 깨졌다(10,500 → 22,620 으로 늘었다). 깊이 검사를 넣어
**12,640** 으로 내렸다(수리 전 33,286 대비 62% 감소). 잔여는 머리말이 분리된 탓이라
머리말을 되붙이면 정합한다.

결과: 다리 457,598 → **492,245**(양방향 분해분 +34,647) · 축약복원 96.8 → **97.9%** ·
`QuoteYield` 3,850,006 · `unresolved` 122,897 → **97,586**.

### 오너에게 물을 것 (남은 셋) — ~~미해결~~ **§10 에서 데이터로 닫혔다**

이 셋(`국딱`·`국전`·`국전전`)은 2026-09-02 판별검정으로 풀렸다. 아래는 그때 무엇을
어떻게 물으려 했는지의 기록이고, **답은 §10 에 있다**. 물으러 갈 필요 없다.

1. `국딱` — 「통딱과 같은 문법인가」 → **문법은 같고**(딱=당일) **대상이 다르다.**
   통딱은 2년 통안 한 종목이지만 국딱은 만기를 안 가린다.
2. `국전` — 「국고 전물인가」 → 아니다. **국민주택 전월물**이다.
3. `국전전` — 「국전의 전물인가」 → **사다리 관계는 맞았고 물건이 틀렸다**(국민주택 전전월물).

**교훈: 「국고냐 통안이냐」로 물어본 것 자체가 선택지를 둘로 좁힌 질문이었다.**
답은 셋째 후보에 있었다.

---

## 10. 국고 은어 — 닫힘 2026-09-02

[OWNER] 「국당 근데 만기 표시 진짜 안 되어 있더라」에서 시작해 판별검정으로 닫았다.
전문은 `RESULT_ktb_slang.md`. 재현 = `slang_ktb_offset.py`.

| 은어 | 행수 | 09-01 판정 | **09-02 확정** |
|---|---|---|---|
| `국당` | 55,285 | 국고(만기 미정) | **국민주택채권 1종 당월물** |
| `국전` | 32,327 | 국고(만기 미정) | **국민주택채권 1종 전월물** |
| `국전전` | 3,722 | 국고(만기 미정) | **국민주택채권 1종 전전월물** |
| `국전당` | 211 | 미정 | **국민주택채권 1종**(사다리 위치는 n 부족) |

`국전당` 의 칸은 **추론으로는 국전과 같은 r1** 이다 — 통안 사전에서 `구통` 과
`구통당` 이 둘 다 2Y r1 이었듯 「당」이 발행 계열을 가리키는 접미이고 앞의
「전」이 칸을 민다. **다만 이건 유비이지 측정이 아니다**(n=98 이라 인접 칸이
1bp 차인 이 사다리에서는 갈라낼 수 없다). 트레이더 확인 대상으로 남긴다.
| `국딱` | 7,649 | 국고 30년물 | **그날 입찰된 국고채 — 만기가 아니라 상태** |

### ★첫 검정은 틀린 자를 들이댔다

통안을 푼 방법(만기 x 순위 격자 · 5bp 적중률)을 그대로 썼더니 배관검정은 **98.4%**
인데 은어는 어느 칸도 **46%** 를 못 넘었고, **국당·국전·국전전이 전부 같은 칸**에
떨어졌다. 셋이 같은 답이면 신호가 아니라 잡음의 모양이다.

**5bp 문턱은 «만기가 틀렸다» 와 «만기는 맞는데 다른 종목이라 스프레드가 있다» 를
구분하지 못한다.** 국고 대비 15bp 벌어진 물건은 만기를 맞혀도 떨어진다.
→ 적중률을 버리고 **오프셋 분포**로 갔다(축약호가 소수부 − 기준 민평 소수부).
100bp 로 감기는 자료라 **원형 집중도 R** 이 맞는 통계다.

### 답

국당 5Y r0 **R=0.975 중심 +15.4bp** · 국전 0.976 **+15.8** · 국전전 0.976 **+15.7**.
그리고 **문면에 `국주` 라고 적힌 호가 3,689행의 중심이 +15.7bp**(R=0.937) —
딜러가 종목을 명시한 독립 표본이 같은 자리를 가리킨다.

뒷받침 셋: `국전당` 21행 **전부**가 `국주 23-12, 24-01, 24-02 팔고 국전당 사자` 로
국주와 짝 · 셋이 자기들끼리 70~100% 굴리고 인접 칸이 1bp 차(`국전전 93 팔고 국당 94
사자`) · **월 단위 사다리를 도는 물건은 국민주택 1종뿐이다**(국고 지표물은 분기마다
바뀌어 한 분기 안에서 당월=전월이라 서로 교체 자체가 성립 안 한다 → 가설 A 붕괴).

**★「5년물·매월」도 로그가 직접 말한다.** 딜러가 회차와 만기를 병기한 행 22,679개
(`28.8.31 국주23-08 민3.571 67 팔자`)에서 **(만기−회차) 94.6%가 정확히 5.0년**이고
회차의 월 부분이 1~12 에 모두 나온다. **시장 상식을 가져다 붙인 게 아니라 자료
안에서 확인했다** — 통안 때의 로제타석과 같은 종류의 증거다.

### ★`국딱` = 상태어. 그리고 앞선 98%는 선택 효과였다

국딱은 4,952행인데 **고유일자가 24일뿐이고 그 24일이 100% 국채 입찰일**이다.
국고채는 입찰일마다 만기가 하나만 열리므로(246일 전부 `nunique=1`) 표본이 날짜로
완전 분할되는데 **각 조각이 그날 낙찰금리에 붙는다** — 2Y R=0.981 · 3Y 0.992 ·
5Y 0.998 · 10Y 0.989 · 20Y 0.984 · 30Y 0.986, 중심 전부 ±1bp, 표본 합이 정확히 4,952.

앞선 «국딱 = 30년물 98%» 는 **교체 문맥 행에서만** 잰 값인데, 국딱 교체 행 1,293개
중 **1,265개(97.8%)가 30년 입찰일에 놓여 있다.** 30년이라서가 아니라 교체 거래가
30년 입찰일에 쏠려서다. **틀린 값이 아니라 틀린 모집단에서 잰 맞는 값이다.**
한 문맥만 보고 정하면 이렇게 된다.

### 파서 반영

`SECTOR_RULES` — 넷을 국민주택으로 옮기고(97,156행 오배정 시정), 국딱은 국고에
두되 **고정 만기를 주지 않는다**. 그리고 **뒤 경계 `(?![가-힣])` 를 뺐다**
(`국당팔고`·`국전전팔자` 처럼 동사가 바로 붙는 진짜 호가 약 950행이 잘렸다).
**앞 경계는 필수로 남긴다** — 없으면 `한국전력`의 「국전」이 걸려 151,023 중
4분의 3이 가짜였다. 교대 순서는 길이 내림차순으로 못 박았다(안 그러면 `국전`이
`국전전`을 삼킨다, 4,700행).

### 남은 것

**국주 민평이 이 DB 에 없다** — `국고통_민평` 은 KR10 국고 166 · KR31 통안 183 ·
KRC0 분리채 317뿐, `Bond Info` 의 국민주택은 2022년 만기 1종, `국채_발행정보` 0종.
그래서 국민주택 은어 91,545행은 **섹터까지만** 간다. 근사가 필요하면
**국고 5Y 지표물 +15.7bp**(R 0.976 · 중심 ±5bp 안 87~89%)를 쓸 수 있다.

**`국딱`은 값을 붙일 수 있다(미구현).** `국채_입찰` 의 (입찰일 → 표준코드) 로 그날
종목을 특정하고 `국고통_민평` 에서 민평을 가져오면 7,649행이 종목까지 확정된다.
낙찰금리 대비 R 0.98~1.00 이 이미 그 조인이 맞다는 증거다.

### 트레이더 질문지

https://claude.ai/code/artifact/d45eafb5-d485-4358-9ebe-2c7261d38b3d

소스는 `artifacts/kbond_slang_questions.html`. 고칠 때는 그 파일을 고치고 위 URL 을
`Artifact` 의 `url` 인자로 넘겨야 같은 링크가 유지된다.
**국고 은어 다섯은 이제 데이터로 풀렸으므로 질문지의 B 절은 «확인만» 받으면 된다.**
가장 큰 미해독어는 여전히 **`수반` 176,893행**이다.

---

## 11. 장외 Order Book Dynamics — 목적 [OWNER 2026-09-02]

> 「이걸로 장외 Order Book Dynamics 구현할 생각이야」

**파싱은 목적이 아니라 재료였다.** 앞으로 무엇을 더 파고 무엇을 버릴지는
「호가창 동역학을 세울 수 있는가」로 잰다. 아직 설계 지시는 없으므로
**여기 적는 건 이 레인이 이미 가진 것과 없는 것뿐이다. 임의로 시작하지 않는다.**

### 이 데이터가 장외 호가창으로서 가진 성질

장내 호가창과 다른 점을 먼저 못 박아야 한다. 이건 **주문서가 아니라 대화록**이다.

| 장내 호가창 | 이 데이터 |
|---|---|
| 주문 제출·취소·체결이 사건으로 찍힘 | **제출만 보이고 취소가 안 보인다** — 호가를 거둬들이는 메시지가 따로 없다 |
| 호가가 살아 있는 기간이 명시 | **수명 미상.** 같은 딜러의 재게시로 추정해야 한다 |
| 수량이 항상 있음 | `Amount` 는 일부만(명시 안 하면 관행 수량) |
| 종목이 항상 특정됨 | **범주 콜이 있다**(만기대·섹터로만 부름) — 새로 낸 `TenorLo/Hi·SectorCat` |
| 익명 | **발신자·데스크가 보인다**(`SenderKey`·`BrokerKey`) — 장내에 없는 축이다 |
| 양방 호가가 한 주문서에 | 한 줄에 양쪽이 다 오기도 한다 → `kbond_legs` 로 폄 |

### 이미 있는 재료

- **시각** `Timestamp`(초 단위) · 방 두 개(블커본드·막무가내)
- **방향** `Position` {BUY, SELL, SWAP} · `PositionSource`
- **레벨** `QuoteYield` 385만행 + **`QuoteConfidence`**(2026-09-02 신설)
- **호가 대 관심** `MsgType` {QUOTE, AXE, INQUIRY, CONFIRM, OTHER}
  — **AXE 대 QUOTE 비가 곧 «구속력 없는 관심 대 실제 호가»** 다
- **체결** `kbond_fills` 8.6만행(`FillKind` 로 사후보고/복합문 구분)
- **참가자** `BrokerKey` 894 데스크 · `SenderKey`
- **다리** `kbond_legs` 49만 — 교체(스위치)를 두 다리로 폄

### 없는 것 (이게 설계를 정한다)

1. **취소가 없다.** 호가 소멸을 관측할 수 없다. 수명을 «다음 재게시까지» 로
   가정하든 «고정 창» 으로 자르든 **가정이고, 그 가정이 결과를 만든다.**
2. **체결 확인이 없다.** `kbond_fills` 는 체결 «보고» 이지 «확인» 이 아니다.
   같은 거래가 양쪽에서 두 번 보고되면 두 행이 된다(중복 제거 미구현).
3. **호가창 깊이가 없다.** 한 시점에 한 종목의 매수·매도가 동시에 관측되는
   일이 드물다 — 스프레드를 재려면 **시간 창을 잡아야 하고 그게 자유도**다.
4. **국민주택 91,545행에 레벨이 없다**(민평 소스 부재, §10).

### 참조 모형 — Coinbase 호가창 해부 [OWNER 2026-09-02 「Coinbase Advanced 에서 한 번 보고」]

Advanced Trade(리테일)는 한국에서 시세 스트림이 안 와 스켈레톤만 봤고(레이아웃은
확인: 집계 증분 드롭다운·표시통화 토글·Price/Amount/Total 3열·매도 위/매수 아래),
**실제 흐르는 책은 Coinbase Exchange(기관용, exchange.coinbase.com)에서 봤다.**
캡처 = `artifacts/coinbase_orderbook.png` · `coinbase_depthchart.png`.

**화면 구성 (BTC-USD 실측):**

| 요소 | 내용 |
|---|---|
| 사다리 | 3열 `Market Size | Price | My Size`. **매도가 위**(최우선이 가운데 쪽), **매수가 아래**(최우선이 맨 위), 가운데에 **스프레드 줄**(`USD Spread 0.01`) |
| 레벨 바 | 행 왼쪽 가장자리에 레벨별 잔량 막대(색 = 방향) |
| 집계 | 증분 선택($0.01→$1…). **매수는 구간 최소가·매도는 구간 최대가로 붙인다** — 집계 표시를 보고 지정가를 내도 반드시 닿게 하는 보수 규칙 |
| 심도 차트 | 중간가(Mid Market Price)를 축 중심에 놓고 좌(매수)·우(매도) **누적** 잔량 계단. ± 로 가격 범위 확대/축소 |
| Trade History | 별도 패널: 체결 크기·가격·시각 + **틱 방향 화살표** |
| My Size | **내 주문을 시장 책 위에 겹쳐** 보여주는 열 |
| 갱신 | 행이 통째로 다시 그려지지 않고 **셀 단위 비동기 갱신**(빠른 장에서 순간적으로 어긋나 보이는 프레임이 있다 — 캡처에도 잡힘) |

**데이터 모형 (Advanced Trade API `level2` 채널, 문서 실독):**

```
구독: {type:subscribe, channel:level2, product_ids:[...]}
수신: snapshot 1회 + update 스트림
  update = {side, event_time, price_level, new_quantity}
  ★ new_quantity 는 델타가 아니라 절대값 교체. 0 이면 그 레벨 삭제.
  sequence_num 으로 유실·중복 감지. timestamp(발송)와 event_time(엔진 기록) 분리.
```

즉 Coinbase 의 책은 **`(side, price_level) → quantity` 맵 + 절대값 교체 + 명시적
0(삭제) + 순번**이 전부다. 화면(집계·누적·심도)은 전부 이 맵의 파생이다.

### 우리 데이터로의 번역 — 무엇이 옮겨지고 무엇이 안 옮겨지나

| Coinbase | 우리 (K-Bond 장외) | 판정 |
|---|---|---|
| 키 `(side, price_level)` | **`(바구니, side, 딜러)`** — 장외 호가는 익명 풀이 아니라 **딜러의 소유물**이고, 같은 레벨에 딜러 여럿이 설 수 있다 | 키 축이 다르다. 가격이 키가 아니라 **딜러가 키** |
| `new_quantity` 절대값 교체 | **재게시가 정확히 이것이다** — 같은 (딜러,종목,side)의 새 메시지 = 그 딜러 레벨의 교체 | **§11 질문 3의 답이 여기서 나온다**: 책 «상태»에서는 갱신(교체), «이벤트 로그»에서는 사건 — Coinbase 도 snapshot(상태)과 update(사건)를 둘 다 준다. **양자택일이 아니라 두 층이다** |
| `new_quantity = 0` (명시적 삭제) | **없다.** 대체 후보 셋: (a) 같은 키의 새 레벨 = 교체, (b) 체결 보고(fills) = 제거, (c) **TTL 만료 = 삭제** — §11 질문 1(호가 수명)이 곧 「0 이벤트를 무엇으로 정의하나」다 | 설계 결정 필요 (오너) |
| `sequence_num` | Timestamp + 메시지 순서. 유실 감지는 «로그 없는 날» 경고가 그 역할 | 있음(약함) |
| 가격축 집계($0.01→$1) | (i) 크레딧은 **민평 대비 bp(`QuoteVsMP_bp`)로 정규화**하면 종목 간 한 축에 놓인다 — Mid = 민평(0bp), 스프레드 줄 = 오버/언더 · (ii) 범주 콜의 `TenorLo/Hi·SectorCat·RatingCat` 이 이미 **만기·섹터축 바구니**다 | 축이 둘(스프레드축 + 바구니축) |
| 집계의 보수 규칙(매수=구간 최소가) | 금리 세계는 방향이 뒤집힌다 — 매수 관심은 바구니의 **높은 금리** 쪽, 매도는 **낮은 금리** 쪽으로 보수 표시 | 옮겨진다(부호 반전) |
| Trade History | `kbond_fills`(84,011) + `Tick` 이 방향 화살표 | 있음 |
| My Size (내 주문 겹침) | **`BrokerKey`/`SenderKey` — 우리는 남의 것까지 전부 보인다.** 장내에 없는 축: 딜러별 책, 딜러별 지속성 | 우리가 더 풍부 |
| 심도 차트(누적 잔량) | 만들 수 있으나 `Amount` 결측 행은 관행 수량 가정 필요, AXE(관심)를 잔량에 넣을지 별도 결정 | 조건부 |

**요약: Coinbase 모형에서 «절대값 교체 + 명시적 삭제 + 상태/이벤트 이층» 을
가져오면 §11 질문 3은 해소되고(두 층), 질문 1은 «삭제 이벤트의 정의»로
좁혀진다.** 남는 진짜 선택지는 (a) 교체+체결만으로 지우고 TTL 없음, (b) TTL 을
더해 조용한 소멸을 흉내 — 이 둘 중 하나다.

### 가능한가 — 실측 답 [OWNER 2026-09-02 「가능하다에요?」]

**가능하다. 단, 한 가지 모양이 아니라 두 가지 모양으로.**

**(1) 국고·통안 = Coinbase 식 양면 사다리가 그대로 선다.**

| 실측 | 값 |
|---|---|
| 단면 호가(QUOTE, BUY/SELL) | 6,579,751행 |
| 국고 QUOTE 방향 | BUY 46.4% / SELL 53.6% — 균형 |
| 국고 (일, 종목) 칸에 양면이 다 있는 비율 | **66.6%** (28,586칸) |
| 최활발 종목(25-4)의 30분 창 양면율 | 호가 있는 창의 **84.1%** · 전체 장중 창의 33.9% |
| 통안 QUOTE 방향 | BUY 44.1% / SELL 55.9% |

**(2) 크레딧 = 양면 사다리가 아니다. 애초에 그런 시장이 아니었다.**

| 실측 | 값 |
|---|---|
| 크레딧 QUOTE(종목 찍고 레벨 있는 호가) | **BUY 0.1% / SELL 99.9%** — 사실상 전부 매도 |
| 크레딧 AXE | **BUY 79.8%** / SELL 20.2% |
| 범주 콜(TenorLo/SectorCat 있는 행) | **BUY 78.1%** / SELL 21.9% |

크레딧 장외에서 매도는 「딜러가 재고를 종목·레벨 찍어 파는 것」이고, **매수는
종목을 안 찍는다** — 「2~3년 AA- 이상 회사채 사자」 처럼 바구니로 온다.
그래서 크레딧 책의 옳은 모양은:

    매도면 = 종목별 사다리 (QuoteVsMP_bp 축, 딜러 키)
    매수면 = 바구니별 수요판 (TenorLo/Hi x SectorCat x RatingCat)
    가운데 = 어느 매도 종목이 어느 수요 바구니에 «들어가는가» 의 매칭

**2번 지시(범주 콜 스키마 신설)가 정확히 크레딧 책의 매수면이었다** — 이제 와서
맞물린다.

**정직한 제약 셋:**
1. **삭제 관측 불가** — 수명은 가정이다(교체+체결 vs TTL, 위 택일 대기).
2. **이 두 방이 시장 전체가 아니다** — «보이는 것만의 책». 깊이의 절대값보다
   변화·비대칭이 믿을 만한 신호다.
3. **지금은 하루 1회(16:30) 증분** — 과거 재생(replay)은 즉시 가능. 장중
   실시간판은 같은 증분 파이프라인을 장중에 자주 돌리면 분 단위 지연으로 가능
   (자동저장 로그가 장중 내내 append 되는 것은 이미 실측돼 있다).

### 실시간 가능한가 — 실측 답 [OWNER 2026-09-02 「실시간으로도 가능해?」]

**가능하다. 장중에 직접 쟀다(2026-09-02 14:24):**

| 실측 | 값 |
|---|---|
| 오늘 로그 파일 mtime | 벽시계 −1초 (막무가내·블커본드 동시) |
| **마지막 메시지 vs 벽시계** | **블커본드 0초 · 막무가내 3초** — 메신저가 버퍼 없이 즉시 flush |
| 오늘 14:24 까지 메시지 | 두 방 합계 약 8,600건 (분당 ~25건) |
| 증분 파싱 비용 | `extract()` 는 정규식이라 초당 수천 건 — 꼬리 몇 줄은 밀리초 |

**아키텍처 (얇은 라이브 레인 — 본표 배치와 분리):**

```
감시자 (상주 스크립트, 파일별 바이트 오프셋 기억)
  → N초마다 오늘 파일 꼬리만 읽어 extract()
  → (딜러,방향[,종목]) 절대값 교체로 오늘 책 접기  (목업과 같은 규약)
  → 스냅샷 JSON 을 localhost 로 서빙
뷰어 = 목업 페이지 그대로, 데이터 소스만 라이브 JSON 폴링으로 교체
```

끝단 지연 = **폴링 주기 + 1초 미만** — 폴링 5초면 체감 실시간이다.

**설계 원칙 셋:**
1. **본표(16:30 배치)는 안 건드린다.** 라이브 레인은 자기 스냅샷 JSON 만 쓴다 —
   낮에 본표를 다시 쓰다 T24(읽기와 교체 충돌)를 또 만들 이유가 없다.
2. **민평 기준은 아침에 한 번 로드**(전일 민평). 딜러 호가 관행 자체가 전일 민평
   대비라 장중 기준으로 옳다.
3. **뷰어는 로컬이어야 한다.** 아티팩트는 CSP 로 외부·로컬 fetch 가 다 막힌다 —
   sauron/rateslab 처럼 localhost 서버 + 로컬 페이지 패턴.

**제약:** 이 PC 에서 메신저 앱이 켜져 있어야 한다(꺼지면 스트림 죽음 — 2026-08-31
결측이 그 사례). **덤 발견: 오늘 로그에 «1반·니스·알프스» 방도 있다** — 현재
파이프라인은 D3 로 두 방만 파싸는데, 방 추가는 오너 결정 사안.

### 목업 v1 [OWNER 2026-09-02 「일단 목업 띄워볼래?」]

https://claude.ai/code/artifact/ba9dc24a-fc9c-484f-9902-13a595042e6d
소스 `artifacts/kbond_orderbook_mockup.html` (524KB, 데이터 내장).
**가짜 데이터가 아니라 2026-01-08 실데이터 재생**이다 — 25-4 양면 최활발일
(매수 587·매도 501). 국고 25-4 이벤트 1,088 · 크레딧 매도 4,464 · 수요 바구니 350
· 체결 25 를 내장하고 브라우저에서 접는다.

구성: **시간 슬라이더+재생(×60/×300/×1200)** · 국고 25-4 **DOM 3열 사다리**
(매도|수익률|매수 — Coinbase 의 상하 분리 대신 DOM 을 쓴 이유: 장외는 유령 호가로
같은 레벨에 양면이 서거나 크로스가 나는데 상하 분리로는 그걸 못 그린다.
크로스가 나면 스프레드 줄이 음수+«유령 호가» 로 표시된다) · 체결 테이프 ·
크레딧 **vs민평 bp 사다리**(잔존 필터) · **수요 바구니판**(범주 콜) ·
**호가 수명 토글 [교체만|30분|2시간]** — §11 의 미결 설계 선택을 오너가 직접
눌러 보고 고르게 만든 것이 목업의 핵심 목적이다. 오래된 호가는 3시간에 걸쳐 바랜다.

책 접기 규약은 Coinbase level2 번역 그대로: (딜러,방향[,종목]) 키의 절대값 교체,
TTL 은 옵션. 잔량 없는 호가는 딜러 수로만 센다.

### 라이브 레인 v1 — 가동 중 [OWNER 2026-09-02 「지으세요」]

**`kbond_live.py` + `kbond_live.html` → http://127.0.0.1:8301/**
예약 태스크 **`KBondLive`**(평일 08:20, pythonw, 시간제한 10h, IgnoreNew).
당일 장중에 등록·기동해 실측 확인: 오늘 메시지 9,321건 접힘 · 끝단 신선도 10초.

- 오늘 로그 꼬리를 5초마다 바이트 오프셋 증분으로 읽어 `extract()` → 접기 →
  `/book.json`. **본표를 읽지도 쓰지도 않는다**(T24 원천 차단).
- 민평은 기동 시 1회(최신 일자 = 전일). 호가 관행이 전일 민평 대비라 옳다.
- **TTL 기본값 = 리서치 결과: 국고 30분 · 크레딧 2시간**(`RESEARCH_microstructure.md`).
  근거 = 재게시 간격 실측(국고 p90 17분→30분이 93% 포괄 · 크레딧 p95 124분→2시간이
  95% 포괄) + CBBT «최신 executable» 원칙 + runs 는 indicative 라 soft decay 병행.
- v1 범위: 국고(BondCode+축약호가 복원+절대금리) · 크레딧(문면 bp 만) ·
  수요 바구니 · CONFIRM 테이프. **v2 로 미룬 것**: 통안 은어 사다리 ·
  크레딧 «원» 호가(TTM 환산) · 체결의 호가 귀속 · pythonw 라 로그 무출력(로그
  파일 추가 여지) · 새 방(1반·니스·알프스) 포함 여부[OWNER].

### ★v1 판단 적합성 평가 [OWNER 2026-09-02 「트레이더가 보고 판단하기 편할까?」]

**아직 아니다 — 데이터는 맞지만 판단 도구로는 세 군데가 어긋난다.** 장중 실증:

> 14:43 화면의 25-10 최우선 = 매도 3.835(**8분 전**) / 매수 3.835 → **0.0bp 잠김**으로 표시.
> 그러나 «최신 호가» 기준 = 매도 3.830(0분 전) / 매수 3.835(1분 전) → **+0.5bp 정상**.
> **8분 묵은 매도 극단이 화면의 최우선을 오염** — 트레이더가 잠긴 시장으로 오판할 자리.

1. **최우선이 스테일 극단에 오염된다.** v1 은 «생존 전체의 min/max» 로 최우선을
   구한다. CBBT 교훈 그대로: **최신성 우선**으로 바꿔야 한다(딜러별 최신 중
   방향별 최신 상위로 best 산출) + 레벨에 «나이(분)» 명시.
2. **기준선·동태가 없다.** Harris(호가가 최고의 실시간 가치 신호)의 함의는 그
   신호의 **시계열**을 보여주라는 것 — 트레이더의 첫 질문은 「지금 어디고 아침보다
   얼마나 움직였나」다. mid 스파크라인·전일 민평 대비 Δ·갱신 플래시가 없어
   5초마다 화면 전체를 다시 읽어야 한다.
3. **크레딧 매수면-매도면이 매칭되지 않는다.** 이 시장 구조의 핵심(매도=종목,
   매수=바구니)의 쓸모는 「이 매도가 어느 수요에 꽂히나」인데 화면은 둘을
   나란히 둘 뿐이다. 바구니에 맞는 매도 수 배지 + 매도 행에 수요 하이라이트 필요.

부차: 딜러가 익명 숫자키(트레이더는 데스크명을 안다 — Broker 원문으로) ·
크레딧 TTM/섹터 필터 누락 · 테이프 미파싱 · 셀 단위 갱신 플래시 없음.

### v1.1 — 판단 결함 셋 수리 + 준실시간 전환 [OWNER 2026-09-02 「고치면서 거의 동시에」]

**지연: 파일 폴링 5초 → 0.4초, 뷰어 폴링 → SSE 푸시.** 책이 바뀔 때만 서버가
스냅샷을 민다(`/events`, 15초 유휴 핑, 끊기면 3초 재접속 + `/book.json` 폴백).
끝단 = 폴링 0.4초 + 푸시 ~0 ≈ **0.5초 미만**.

1. **최우선 = 방향별 «최신» 호가**(15분 창, CBBT 원칙)로 교체. 헤더에
   최우선 매수/매도·나이·데스크·스프레드·**전일 민평 대비 Δbp** 를 상시 표시,
   종목 탭에도 Δ 배지. 사다리 레벨에 나이(분)와 ★(최신 최우선) 마커.
2. **mid 스파크라인**(10초 표본, 서버 `hist`) + 전일 민평 점선 + **셀 단위
   갱신 플래시**(변경 시그니처 비교, reduced-motion 존중).
3. **크레딧 수요↔매도 매칭**(서버 `_match`): 잔존 ±0.25년 · 섹터 겹침 ·
   등급(둘 다 있을 때만, «이상» 은 서열 비교). 매도 행에 «수요 N» 배지,
   바구니 카드에 «맞는 매도 N건». 매도 엔트리에 잔존·섹터·등급을 붙이는데
   **섹터·등급은 본문에서 직접 읽는다**(발행체명이 범주 규칙에 걸림 —
   '하나캐피탈'→여전채). 잔존 탭 부활. 딜러는 **데스크 실명**(Broker 원문에서
   전화번호 꼬리 제거), 사다리 셀 title 에 딜러 명단.

가동 확인(14:56): 매칭 «수요 붙은 매도 109/160 · 매도 붙은 바구니 16/62»,
화면에 «0~2년 은행채 50억 — 맞는 매도 3건» 식으로 표시. 25-10 최신 최우선
3.835/3.835(진짜 잠김) · **전일 민평 대비 +4.5bp** 가 헤더에 바로 읽힌다.

**★이번에 데인 것(T25): 문자열 치환 속 `역슬래시+개행` 은 라인 연속으로 접혀
매칭이 조용히 불발된다.** 게다가 그 replace 에만 assert 를 빼먹어 「완료」가
찍혔고, pythonw 태스크는 콘솔이 없어 기동 크래시(KeyError)가 침묵했다.
줄 인덱스 치환으로 고침. **replace 에는 항상 assert, 태스크 스크립트는
포그라운드 스모크 테스트 후 등록할 것.**

### 검증 2026-09-02 [OWNER 「잘 작동하는건지 확인하기」] — 그리고 검증이 찾아낸 큰 구멍

**끝단 지연 실측**: 파일 증가 → 책 반영, 20초 창 17사건 — **중앙 0.20초 · 최대 0.42초.**

**내용 대조**: 원본 로그 최근 호가를 `extract` 로 따로 파싱해 책과 맞춤 —
**32/32 정합**(정확 일치 25 + 같은 딜러 재게시로 옳게 교체됨 7 + 진짜 누락 0).

**★검증이 잡아낸 결함 둘 (즉시 수리):**

1. **민평 매핑 구멍 — 그날 최활발 종목이 통째로 안 보이고 있었다.**
   `load_mp` 가 `ontherun_schedule`(지표지정 이력, 마지막 2026-06-10)만 쓰므로
   그 뒤 발행된 **26-7(당일 국고 1위, 50건)·26-9(3위, 34건)** 의 ISIN 이 없어
   민평이 안 붙고 호가가 버려졌다. → `국채_발행정보`(종목명 'YY-N' 꼴)와
   **합집합**(155종 추가, 매핑 218→222종). 라이브·배치 둘 다 수리 —
   **배치는 매일 enrich 가 전 표를 다시 돌므로 오늘 16:30 회차가 전 이력을 채운다.**
   ⚠ 이 구멍은 6월 이후 본표의 국고 신규물 민평 결측으로 이미 존재했었다.
2. **`토지(용지)24-12` 가 국고 24-12 로 오인** → 지방/첨가 규칙에 `토지(용지)` 보강.
   본표 반영은 다음 `--force` 재파싱 때(소수 행).

**교훈: «잘 도는 것처럼 보인다» 와 «원본과 대조했다» 는 다르다.** 화면·지연·매칭이
다 정상으로 보이는 상태에서, 원본 대조만이 「가장 활발한 종목이 없다」를 잡았다.

### v2 — 외부 서비스 기준 초과 + rateslab 문법 + 준실시간 안정화 [OWNER 2026-09-02]

> 「기능은 Coinbase Advanced, 디자인 문법은 Rateslab · 실시간성 불안정 보완 ·
> (외부 서비스 화면 2장 제시) 이거보다는 나아야 해」

**오너가 준 외부 참조 2장이 기준을 정했다** — 크레딧은 «건별 블로터»(종별·발행사·
등급·잔존·전일민평·호가·YTM·종목명), 국채는 «전 종목 보드»(CODE·만기·Yest·
Sell/Buy 메시지 스택·Traded). v2 는 그 정보 구성을 채우고 우리만의 축을 얹었다:

| 외부 서비스 | 우리 v2 |
|---|---|
| 국채: 종목별 원문 메시지 3개 나열 | **접힌 최신 최우선 + 나이** (딜러 title 로 명단) + 행 클릭 → 상세(사다리·mid 차트·심도) |
| Traded: 체결가 + 전일 대비 | 동일 + **stats 스트립·당일 고저** |
| 크레딧: 건별 블로터 | 동일 열 구성 + **수요 매칭 배지** + TTL·나이 바램 |
| (없음) | 집계 토글 · 수명 토글 · 심도 차트 · 수요 바구니판 · 스프레드(최신) |

**디자인 = rateslab 터미널 문법 그대로** (`sauron-v2/src/theme` 실측값 이식):
`--sr-term-*` 팔레트(#111418/#1c2127…) · 행 20px `0 8px` 12px tabular · 라벨 11px
700 .08em 대문자 `{ }` · 행 경계 흰 알파 inset(“border 아님”) · 호버 gray1 30% 워시 ·
프레임 반경 0/컨트롤 4 · 격자 = 가는 선 + 교점 점 · **매수 시안 #68c1ee / 매도 호박
#ec9a3c**(등락 빨/파와 분리된 제3쌍 — sauron 이 한국 독자 때문에 이미 내린 결론) ·
등락 = #e86c76/#5794f0 · Pretendard 단일 face.

**실시간 불안정의 뿌리와 수리:**
1. **★SSE 핑을 주석(`: ping`)으로 보내 EventSource 가 못 봤다** — 조용한 구간마다
   클라이언트가 «연결 끊김» 을 깜빡이고 폴백 fetch 가 발동. → **실제 이벤트
   `event: hb`**(5초)로 교체. 상태는 3단(LIVE/대기/끊김)으로 정직하게.
2. 큰 시계가 서버 스냅샷 시각이라 조용하면 멈춤 → **클라이언트 1초 시계** +
   «마지막 호가 N초 전» 분리 표시.
3. pythonw 는 콘솔이 없어 크래시가 침묵 → **파일 로그**(`kbond_live_server.log`) +
   poll 루프 try/except + 민평 로드 5회 재시도(실패 시 민평 없이 기동) +
   `/health` + **태스크 RestartCount=10/1분**(크래시 자동 재기동).

서버 확장: `kfills`(국고 종목별 당일 체결 — CONFIRM 에서 복원) · `mats`(만기일 166종,
보드 정렬용) · 크레딧 `mp`(문면 전일민평)+`ytm`(=민평+bp) 계산.

### v2.1 — 연물·지표물·통안 분리·데스크 용어 [OWNER 2026-09-02]

> 「국고 종목 옆 연물 · 크레딧에 만기+원만기 · 용어를 금융언어로 · 지표물 표기 ·
> 통안과 국고 분리」

- **연물 열**: `국채_발행정보.인포맥스소분류`(166종)로 2y/3y/…/50y. 선택 종목
  stats 에도 «26-1 2Y 지표» 로 표기.
- **지표물 배지**: `ontherun_schedule` 만기별 마지막 지표지정 7종에 [지표] 배지.
- **통안 MSB 보드 분리**: 은어(통당·구통…)를 **배치와 같은 사다리 규약**으로
  종목 확정해 만기·잔존·전일민평·오퍼/비드·체결 표시. ★통안 최신물은 민평
  적재가 며칠 늦는다(28.07.02통이 최신일에 없음) → **ISIN 별 최근 7일 마지막
  민평 폴백**. 결과 57/58 엔트리에 레벨.
- **크레딧 만기(잔존)**: `29-08-24 (3.0Y)` 형식. **원만기(발행 당시 몇 년물)는
  DB 에 소스가 없다** — `Bond Info` 는 미만기 크레딧 0건. 발행일 소스가 생기면
  잔존과 함께 «3y 발행/2.5y 잔존» 으로 확장(정직한 미구현).
- **데스크 용어 교체**: 매도/매수(최신)→**오퍼/비드(최신)** · 딜러 N→**호가 참여
  N개사** · 수요→**실수요** · 맞는 매도 N건→**매칭 오퍼 N건** · 수요 바구니→
  **매수 관심 실수요 인콰이어리** · 체결 확정→**체결·ㅎㅈ**.

### v2.2 — 정식표기·통안 지표·라벨 수리·행동형 니즈 [OWNER 2026-09-02]

> 「종목 정식표기 · 통안 지표물 · 데이터 확인 · 크레딧 «29.8.31» 라벨 수정 ·
> 전부 트레이딩 시선으로」

- **정식표기**: 국고 = `국고03000-2803(26-1)`(ontherun 명 우선, 없으면 발행정보
  쿠폰·만기로 구성 — 222종 전부) · 통안 = `28.07.02통` + **은어 병기**(통당·구통…).
- **통안 지표**: 만기구분별 최근월(rank 0)에 [지표] 배지(통당·삼통·1년물).
- **크레딧 라벨 «29.8.31» 뿌리 = M6**(BondName 이 회차 있는 이름만 잡음) →
  `issuer_guess`(머리의 날짜·요일괄호·별표를 걷고 첫 낱말) 폴백. 잡은 함정 둘:
  닫히지 않은 요일 괄호(`28.8.28(월 우리금융…`)와 선행 `*`. **날짜형 라벨 0/194.**
- **행동형 니즈 카드**: «매칭 오퍼 21건 · **베스트 +2bp 하나카드306**» — 니즈를
  보고 바로 칠 수 있게. 패널명 «매수 니즈».
- **데이터 검증(3차)**: 원본 로그 최근 호가 대조 — 국고+크레딧 **68/68 정합**
  (일치 53·재게시 교체 15·누락 0) · 통안 3/3.

### v2.3 — 종별·등급·통안 연물·사다리 정밀화·rateslab 절제 [OWNER 2026-09-02 7건]

1. **정식명칭 전면**(v2.2 완성분 유지) + 크레딧 별칭도 정식화(한전→한국전력 등).
2. **종별 분류기**: 발행사 키워드 규칙(여전>은행>공사>지방>금융지주>증권>회사 순).
   당일 194/194 분류(여전 80·은행 40·회사 34·공사 23·지방 7·금융지주 5·증권 5).
3. **등급**: ★외부를 종목별로 긁는 대신 **본표 833만 행에서 딜러가 문면에 적은
   등급을 발행사별 집계**(표본 20+·지배 70%+ → 563개 사전 `issuer_ratings.json`)
   + 공사·은행 AAA 규칙. 문면 등급 우선, 집계 추정은 흐리게 표시. 185/194 커버.
4. **통안 연물 열**(1y/2y/3y — 마스터 소분류).
5. **mid 이력 차트 존치 논거**: 트레이더의 둘째 질문 «오늘 어떻게 왔나»(강세/약세
   전환 시점)에 답하는 유일한 시계열 + Coinbase 가격차트의 대응물. **체결 마커**를
   얹어 «지금 레벨이 당일 체결 대비 어디인가» 까지 답하게 강화.
6. **사다리 정밀화(Coinbase 기준)**: 최우선 사이 **스프레드 줄**(오퍼·비드·나이
   병기) + **누적 Σ**(Coinbase Total — 최우선에서 바깥으로) + 바 폭 = 누적 기준.
7. **rateslab 절제**: 보드 값은 기본 잉크(색은 방향 워시 그라데이션만) ·
   패널 간격 6px · 등락색은 델타에만.

**[OWNER 추가] 집계 토글은 자유** — 유지. **수명 토글 재정의**: 최우선·스프레드는
토글과 무관하게 «항상 최신 호가» 로 고정돼 있으므로(숫자의 정본), 토글은 **사다리
표시 범위 필터**로만 작동 — 라벨을 [활성만|타이트|세션 전체] 로 교체.

### v2.4 — Coinbase Advanced 실물 대조 후 사다리 재설계 [OWNER 2026-09-02]

> 「금융지주=은행채 · 오더북이 여전히 이해 안 되는 디자인 — Advanced 실제로
> 열어보고 판단해서 고치기」

**Advanced 를 실제로 열어 실측했다**(이번엔 시세까지 살아서 옴). 관찰:
한 행 = 한 방향(위 매도 빨강 · 아래 매수 초록) · 열 = `Price | Amount | Total(누적)` ·
**색은 가격에만** · 깊이는 행 배경 워시(오른쪽 기준, 폭 ∝ Total) · 가운데
스프레드 줄(`0.01 (0.0013 bps) USD Spread`) · 최우선이 스프레드 줄에 인접.

**우리 사다리의 결함 = 양방향을 같은 행에 우겨넣은 것**(오퍼|수익률|비드 3열 DOM).
Coinbase 문법으로 재설계:

```
수익률 % | 수량(억) | Σ 누적(억)     ← 가격/수량/Total 대응
[오퍼 rows]  호박, 수익률 오름차순, 최우선이 스프레드 바로 위
[스프레드 줄]  0.5bp · 오퍼/비드 레벨·나이 병기
[비드 rows]  시안, 최우선이 스프레드 바로 아래
```
수익률 오름차순 = 가격 내림차순이라 Coinbase 의 상하 배치가 그대로 성립한다.
깊이 워시는 행 전체(오른쪽 기준, CSS 변수 `--d` 그라데이션). Σ 는 최우선에서
바깥으로 누적. **금융지주 → 은행채** 분류 반영.

### v3 — Coinbase Advanced 단독 문법 [OWNER 「그냥 rateslab 무시하고, coinbase advanced를 따라간다고 생각하고 디자인하고 구성해봐」]

rateslab 토큰을 전부 걷어내고 **디자인·구성 둘 다 Coinbase Advanced 로** 전면 재작성
(`kbond_live.html` 통째, 이전 판은 `kbond_live_rateslab.bak.html` 백업).

**구성(Advanced 실물 그대로 이식)**:
- 상단 종목 바 = 종목 선택 드롭다운(정식명·연물·지표칩·Δbp) + 인라인 스탯
  (최신 mid · 전일 민평 대비 · 스프레드 · 당일 고/저 · 당일 체결 · 오늘 메시지) + LIVE/시계
- 중앙 대형 차트, «시세 차트 | 심도 차트» 탭 (Advanced 의 Price/Depth chart)
- 오른쪽 호가책 패널, «호가 | 체결» 탭 + 집계/표시 셀렉트 (Advanced 의 Order book/Trade history)
- 사이드(주문폼 자리) = 통안 미니보드 + 매수 니즈
- 하단 탭 스트립(Orders/Positions 자리) = «국고 전 종목 | 크레딧 블로터»
- **뷰포트 고정 앱 레이아웃** — 페이지 스크롤 없음, 패널 안쪽만 스크롤(처음 놓쳐서
  클릭이 스크롤에 빗나가는 것으로 발견, body flex-column + 패널별 overflow 로 수리)

**디자인 토큰(Advanced 재실측)**: bg #0B0D10 거의 검정 · 패널 = 같은 바탕 + 1px 경계
· 반경은 컨트롤에만 · 본문 12~13px/책 행 19px · 매도 빨강 #EB4E5C / 매수 초록 #2EBD85
(가격·증감에만 색) · 액센트 = 코인베이스 블루 #3773F5(활성 탭 밑줄) · 깊이 워시 오른쪽
기준 누적 비례 · 증감 색 = 가격 기준(금리 하락 = 초록, Coinbase 규약; 국내 상승 빨강과
반대이므로 오너 확인 필요할 수 있음)

**★재작성 중 방향 실수 1건 자가 검출**: 새로 쓴 uncross/bestOf 가 오퍼·비드 금리
방향을 뒤집었었다(최우선 오퍼 = «최고» 금리가 정본 — 원화 관행 오퍼금리<비드금리,
검증된 v2.5 방향). 통째 재작성은 검증된 불변식도 다시 깨뜨릴 수 있다 — 이식 시
불변식 목록을 먼저 적고 대조할 것.

### v2.5 — 재방문에서 잡힌 것: 책의 단조성 [OWNER 「진짜 제대로 보고온거 맞아? 다시 보고 와」]

**오너가 맞았다.** 다시 열어 뜯어보니 첫 방문에서 열 구성·색·워시는 옮겼지만
**Coinbase 책의 가장 근본적인 성질을 놓쳤다: 사다리 전체가 위→아래 단조이고
크로스가 절대 없다**(가격이 한 방향으로만 흐르고, 최우선이 스프레드 줄과 항상
인접·일치). 우리 화면은 묵은 지표 호가가 크로스를 만들어 «오퍼 3.755 가 스프레드
줄 위, 비드 3.755 가 아래» 같은 모순을 그리고 있었다.

**수리 = `uncross` 규칙**: 표시 책에서 크로스를 만드는 쌍 중 **더 오래된 쪽을
제거**한다 — 더 새로운 반대편 호가가 «그 레벨은 사라졌다» 는 시장 증거다.
(오너의 미결 질문 «체결이 오퍼를 지우나» 의 표시-층 부분해이기도 하다.)
보드·통안 보드·stats·사다리 **전부 이 한 책으로 일원화** — 스프레드 줄·◈·보드
최우선이 서로 절대 모순되지 않는다. 재실측 추가 확인: 워시는 오른쪽 기준(Total
비례) ✓ · Total 은 최우선에서 바깥으로 ✓ · 색은 가격 열에만 ✓.

**교훈: 참조물을 «구성요소» 로만 보면 «불변식» 을 놓친다.** 열·색·배치는 베꼈어도
«책은 절대 크로스되지 않는다» 는 불변식이 빠지면 다른 물건이다.

**v2 알려진 한계**: 블로터 종별이 비는 행(종목명 안의 «카드» 가 한글 경계에 막힘 —
발행사→섹터 매핑 테이블로 풀 것, v3) · 통안 보드 부재 · 크레딧 «원» 호가 미접힘.

### 남은 설계 결정

- ~~재게시 사건/갱신~~ 두 층으로 해소(Coinbase) · ~~TTL~~ 리서치로 확정.
- **바구니 단위**(§11 지표를 종목 말고 바구니로 접을 때)와 **v2 범위 우선순위**만 남음.

---

## 12. 모르는 낱말 — 오너 회신과 그 뒤 [OWNER 2026-09-02]

목록 산출물 `Projects\data\kbond\kbond_은어_목록.xlsx` · 재현 `slang_inventory.py`.

### 오너가 답한 일곱

| 낱말 | 행수 | 뜻 | 처리 |
|---|---|---|---|
| `수반` | 176,706 | **수수료 반값** | **신설 `FeeNote='half'`** — 체결가에 얹히는 조건이라 §11 유효가격의 일부 |
| `평일` | 249,000 | **만기일이 평일** | **신설 `MaturityDayNote='weekday'`** |
| `휴일` | 14,650 | **만기일이 휴일** | **신설 `MaturityDayNote='holiday'`** |
| `오바` | 44,945 | 오버 | 이미 처리 중이었다(`RE_OVER_UNDER` 에 있음, SpreadValue 100%) |
| `표리` | 29,213 | 표면이자율 | 이미 처리 중(`RE_COUPON_CTX` 가 쿠폰 문맥으로 배제) |
| `농중` | 12,580 | 농협중앙회 | 발행체 별칭 — 적용은 M6 대기(아래) |
| `국은채` | 12,454 | 국민은행채 | 같음 |

**★평일/휴일은 데이터로 되짚어 확인했다.** `평일` 행의 만기일이 주중인 비율
**99.6%**(표기 없는 행 82.5%). `휴일` 행은 주중이 **95.8%** 라 처음엔 어긋나
보였는데, 그 날짜가 설(01-30·02-09)·추석(09-24·10-05)·대체공휴일(03-03·05-06)에
정확히 몰린다 — **주중 공휴일**이라 맞는 값이다. 만기일이 휴일이면 지급일이 밀려
경과이자·결제가 달라지므로 딜러가 표시한다.

### 내가 데이터로 확인한 발행체 별칭 14

오너를 더 번거롭게 하지 않으려고, 별칭과 정식명의 **같은 (일자, 만기) 본문 민평**을
대조했다. 같은 발행체면 0bp 여야 한다.

| 별칭 | 정식명 | 공통칸 | \|차이\| 중앙 | 1bp 이내 |
|---|---|---|---|---|
| `한전` | 한국전력 | 회차 450종 겹침(94.9%) | **0.00bp** | 92~100% |
| `인도공` | 인천도시공사 | 618 | 0.00 | 98.5% |
| `인국공` | 인천국제공항공사 | 79 | 0.00 | 100.0% |
| `삼카`·`국카`·`신카`·`우카` | 삼성/국민/신한/우리카드 | 172~415 | 0.00 | 97.2~99.4% |
| `신한채`·`하나채`·`우리채` | 신한/하나/우리은행채 | 35~108 | 0.00 | 95.1~99.1% |
| `농중`·`농중회` | 농협중앙회 | 19~103 | 0.00 | 89.5~99.0% |
| `수금은행` | 수협은행 | 41 | 0.00 | 100.0% |
| `철도공사` | 한국철도공사 | 196 | 0.00 | 99.0% |
| `수출입채` | 수출입은행 | 42 | 0.00 | 100.0% |

`도공` 은 공통칸이 1개뿐이라 판정하지 않았다.

**⚠ 확인했지만 아직 «적용» 은 못 한다.** `RE_BOND_NAME` 이 `발행체명+숫자-숫자`
(하이픈 필수)를 요구해서 `한국전력1299` 조차 종목명으로 안 잡힌다 — 정식명도 못
잡는 마당이라 별칭만 바꿔 봐야 소용이 없다. **기존 미결 M6 를 먼저 풀어야 한다.**
지금 이 별칭들의 종목명 인식률은 0~1.4% 다.

### 괄호가 종류를 가른다 — 농금·수금 계열

    농금(은행) · 농금(중앙회) · 농금채(중앙회)
    수금(경제) · 수금채(일반) · 수산금(신특)

`농금`=농업금융채권, `수금`=수산금융채권으로 읽히고 **괄호 안이 발행 주체나
종류를 가른다.** 합쳐 11만여 행인데 그 구분의 뜻은 미확정이라 오너께 물었다.

### ★목록을 만들며 두 번 헛돌았다

**1차: 발신자 서명에 완전히 오염됐다.** 상위 20개가 전부 데스크 이름이었다
(`신한` 494,422 · `채권투자팀` 205,521 …). 서명은 **매 줄 끝에 붙으므로 빈도
상위를 통째로 먹는다.** → `split_broker` 로 떼고 본문만 세게 고쳤다.

**2차: 순위와 표시를 서로 다른 자로 쟀다.** 순위는 **토큰** 빈도인데 행수·예문은
**부분문자열**로 골라 `도공` 에 「철**도공**단」 예문이 붙었다.
→ 표시도 같은 앞뒤 경계를 쓰게 했다.

**교훈: 사람에게 보내기 전에 상위 20줄을 눈으로 읽을 것.**
두 번 다 숫자로는 정상이었고 눈으로 봐야 보였다.

### 외부 리서치 + 데이터 검증 (2026-09-02 [OWNER] 「외부 리서치 해보기」)

여섯 중 넷을 리서치로 닫았고, **남은 둘도 오너가 닫아 주었다(2026-09-02) — 목록 완결.** 방법은 통안 때와 같다 — 웹에서 실마리를
얻고 **데이터가 스스로 말하게** 한다.

| 낱말 | 판정 | 결정 증거 |
|---|---|---|
| `수N` | **닫힘 = 브로커 수수료 액수** → **`FeeWon` 신설**(원 단위, **32,079행** — 0.1원이 31,044) | 단위를 명시한 행이 있었다 — `(수수10전)` 53건 · `(수0.1원)` 628건 · `총수수료0.1` 154건. **10전=0.1원**이라 «수10»과 «수0.1»은 같은 값의 두 표기(값 분포도 거울: 10↔0.1, 20↔0.2, 5↔0.05). 규약: N≥1 은 전, N<1 은 원 |
| `변고정` | **닫힘 = FRN이 마지막 이자 리셋을 지나 쿠폰이 사실상 고정된 상태** | 22,122행의 잔존이 **중앙 1.5개월, 83%가 3개월 이내**(분기 리셋 주기 안). `중금채FRN`과 같은 문맥에 나옴 |
| `재정`·`재정채`·`재정증권` | **닫힘 = 재정증권**(국고금관리법상 1년 이내 할인 단기국채) | 잔존 **중앙 1.4개월 · 99.8%가 1년 이내** |
| `신특` | **닫힘 = 신용사업특별회계** | 수산업협동조합법이 「수협중앙회에 신용사업특별회계 설치」를 명시. 괄호 계열 분포: 수금(은행 5,612·중앙회 2,264·일반 2,090·**신특 1,402**·경제 1,111) · 농금(중앙회 19,608·은행 17,630) — **괄호는 조합 그룹 안의 발행 주체·회계 구분**이다 |
| `상단` | **닫힘 [OWNER] = 민평 3사 상단값** | 문면 민평이 4사 평균이 아니라 **3사 범위의 최고값**. 별도 열은 안 만들되 이 행들의 `MPYield` 는 DB 민평보다 계통적으로 높을 수 있다(«민 상단» 으로 걸러진다) |
| `화정` | **닫힘 [OWNER] = ㅎㅈ(확정)의 오타** | 확정 타이핑에서 받침 ㄱ 이 빠진 꼴, DS투자증권 CM팀 전용. **`RE_CONFIRM` 에 편입** — 2,664행이 OTHER → CONFIRM 으로, 값 딸린 행은 `kbond_fills` 로도 흘러든다 |

**`FeeWon` 쉼표 소수점 57행(`(수0,1)`, 메리츠 한 데스크)은 같은 날 재파싱으로
해소됐다** — 57/57 회수, FeeWon==0 인 행 0.

**덤 확정: 만기 요일 계열.** `일만기`(93.1% 일요일)·`토요일`(92.5%)·`일욜`(94.3%)·
`금욜`(96.7%)·`토욜`(85.2%) — 전부 **만기일의 요일** 표기다. **파서 확장은 안 한다**:
요일은 `Maturity` 날짜에서 유도되는 중복 정보다. `평일`/`휴일`만 열로 남긴 이유는
음력 공휴일이 날짜만으로 유도가 안 되기 때문이다.

**참고: 경향 2012-07-20 기사 링크가 살아났다** —
https://www.khan.co.kr/article/201207202133595 (옛 기록엔 링크 사망으로 적혀 있다).
내용은 기본 문법(`12-6 50+`·`ㅎㅈ`·`ㄱㅅ`)까지라 새 은어 해독에는 안 보태졌다.
아시아경제 2017-04-25 기사에서 **«뻑»**(확정된 거래가 어긋남)을 얻었으나 **이 두 방
로그에는 0건**(cp949 전수 grep) — 기사 속 관행이 이 방 문화에는 없다.

### 남은 한계

- **빈도 300회 이상 · 상위 200개**만 낸다. 꼬리는 안 봤다.
- 채용 공고처럼 브로커 서명이 없는 글은 서명 제거가 안 돼 본문 토큰이 샌다(소수).
- 어미 조각을 `FURNITURE` 로 빼고 있는데 손으로 관리한다 — 새로 나오면 또 샌다.
