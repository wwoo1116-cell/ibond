# 발행체명 레인 → 미해석 레인 인계 (2026-09-03 17:0x)

`PROMPT_unparsed_2026-09-03.md` 를 읽었다. 좋은 문서다 — §2 는 전부 원장에서
잰 숫자라 그대로 쓸 수 있고, §2.3 `당사` 는 여기서 독립으로 재현했다
(`RE_SELL` 에 `선팔` 이 없고 `RE_BUY` 에 `당사` 가 있어 `당사선팔` 이 BUY 로
떨어진다 — 진단 그대로다).

이 문서는 **그 레인이 모르는 것 다섯**을 넘긴다. 셋은 경고고 둘은 새 결함이다.

---

## 0. ★먼저 — `parse_kbond_logs.py` 에 열이 둘 늘었다

2026-09-03 16:4x 에 파서를 고쳤다. **stale 사본으로 편집하면 이 둘이 조용히
사라진다.** P1·P3 은 같은 파일(`RE_TICK`·`QuoteRaw`)을 건드리니 **지금 파일을
다시 읽고 시작할 것.**

| 열 | 값 | 어디서 |
|---|---|---|
| `IssuerCanon` | 발행체 정본. 못 접으면 **원문 그대로** | `kbond_issuer.canon_issuer` |
| `IssuerSource` | `power5` / `ambiguous` / `raw` | 〃 |

- 새 모듈 `kbond_issuer.py` · 시험 `test_issuer.py` (PASS 48/48)
- `parse_kbond_logs.py` : `import kbond_issuer` · `COLUMNS`/`PA_SCHEMA` 끝에 2열 ·
  `extract()` 끝에 한 줄
- `kbond_live.py` : `canon_label()` 을 표시 3자리에 물림(별칭 치환보다 **앞**)
- `kbond_legs.py` 는 `PA_SCHEMA` 에서 열을 유도하므로 자동으로 따라온다(확인함)

**원장은 아직 이 열이 없다.** `--force` 재파싱은 끝났는데(8,214,398행)
`os.replace` 가 **라이브 서버(:8301, PID 27640)가 parquet 을 물고 있어** 막혔다.
`kbond_structured_data.parquet.tmp`(232MB, 16:52)가 그 결과물이다.
→ 원장을 갈려면 **KBondLive 를 내렸다가 올려야 한다.** 오너 결정 대기 중.

> ### ★정정 (2026-09-04 08:50) — 위 두 문장은 틀렸다
>
> 상세는 `RESULT_bondname_fix_2026-09-04.md`. 요지만:
>
> 1. **라이브 서버는 잠금 주체가 아니다.** `kbond_live.py` 는 원장을
>    읽지도 쓰지도 않는다(13행 주석이 그렇게 말한다 — T24 회피가 설계 의도).
>    실제로 물고 있던 것은 **같이 돌던 16:30 `KBondDailyUpdate`** 다.
>    `enrich_kbond_quotes.py:70-71` 이 같은 경로를 읽으면서 쓴다.
>    **공개 서비스를 내릴 이유가 없었다.**
>
> 2. **그 `.tmp` 는 승격하면 안 되는 물건이었다.** 열은 상위집합(57+2)이지만
>    enrich 열이 전부 비어 있었다(QuoteYield 46.14% → **0.00%** ·
>    MPBase 89.78% → **0.00%**). 파싱 단계 산출물이 enrich 열 이름만 빈 채
>    달고 있던 것. 갈아끼웠으면 호가·스프레드 층이 통째로 날아갔다.
>
> 둘을 묶어 `--force` 전체 1회로 해결했다(1,182초). 원장은 지금
> **8,214,400행 · 59열 · IssuerCanon 21.77%** 이고 enrich 채움률은 전부 정상이다.

⚠ 그리고 **16:30 예약 태스크가 돌고 있을 때 `--force` 를 같이 돌렸다(내 잘못).**
둘 다 같은 `.tmp` 경로를 쓴다. 재파싱 전에 `KBondDailyUpdate` 가 도는지 먼저 볼 것.

---

## 1. ★P7 경고 — 오타 복원을 발행체명에 켜면 발전 5사가 무너진다

P7 이 `ㅅ마통`→`삼통`, `샂`→`사자` 같은 오타 복원을 편집거리로 열려고 한다.
**종목·낱말에는 맞지만 발행체명에는 켜면 안 된다.**

```
한국서부발전 · 한국중부발전 · 한국남부발전 · 한국남동발전 · 한국동서발전
```

다섯이 서로 **한 글자씩만** 다르다. 실측(2026-09-03, 원장 전수):

| 짝 | 관계 | 행 A | 행 B | Δ민평 | 회차겹침 |
|---|---|---|---|---|---|
| 한국서부발전 / 한국중부발전 | 1글자 | 17,189 | 6,393 | 28bp | **51%** |
| 남동발전 / 남부발전 | 1글자 | 5,353 | 6,601 | 25bp | 32% |
| 한국남부발전 / 한국중부발전 | 1글자 | 7,611 | 6,393 | 16bp | 38% |

**회차가 51% 겹치는데 다른 회사다.** 「같은 회차면 같은 종목」 규칙으로도 못 가른다.
가른 것은 «같은 회차·같은 날의 민평이 12~28bp 다르다» 는 쪽이었다.

[OWNER 2026-09-03] 「서부, 중부, 남부, 남동, 동서로 파싱하면?」 —
공통부(한국…발전)를 버리고 **변별부를 키로 잡았다.** `kbond_issuer.RE_POWER5` 가
그것이고, 변별 토큰 **뒤에 «발전» 어간이 올 때만** 받는다. 이 조건이
`아이에스동서`(37행, IS동서 — AAA 자산관리공사 지급보증)를 막는다.

P7 을 켤 때 **발행체명은 `kbond_issuer` 를 거치게 하고 편집거리를 태우지 마라.**

---

## 2. ★§1.3 「발행체 사전을 넓히는 순간 오배정이 생긴다」 — 그 자리 측정치

그 문장이 맞다. 얼마나 위험한지 숫자로 재 뒀다. 크레딧 419만행 중 `BondName`
있는 **1,748,085행 · 서로 다른 발행체명 1,735개** 기준.

순진한 규칙(접두·접미·1글자)이 접을 후보쌍 **451**:

```
✓ 검정 통과(같은 종목)   158
✗ 검정 탈락(다른 발행체) 293   접두 102 · 1글자 116 · 접미 75
-> 규칙만 믿고 접으면 65% 가 오접
```

검정 = **같은 회차·같은 날 민평이 1bp 안이면 같은 종목.** 통과분은 중앙
|Δ민평| 이 `0.00bp` 로 떨어진다(예: 지역난방공사/한국지역난방공사 145쌍,
하나캐/하나캐피탈 1,058쌍, 케이비캐피탈/KB캐피탈 623쌍).

**제일 위험한 짝** — 이름이 «아이» 하나 차이:

```
엠캐피탈339-2    민 6.129  A-      <- M캐피탈(구 효성캐피탈), KOFIA 67종
아이엠캐피탈       민 3.5%대 AA-급   <- iM캐피탈(구 DGB캐피탈), KOFIA 94종
                                    Δ민평 243bp · 회차겹침 0%
```

그 밖에 접으면 안 되는 것: `SK`(㈜, KOFIA 71종 — `SK온` 과 156bp) · `한진`(24종) ·
`LS`(6종) · `케이씨씨`≠`케이씨씨글라스` · `하이트진로`≠`하이트진로홀딩스` ·
`이마트`≠`롯데하이마트` · `KT`≠`SKT` · `한화토탈에너지스`≠`한화토탈에너지서비스`(AA-).

**한국 지주회사는 이름이 두 글자라 «축약처럼 생겼지만 실명»이다.**
포스코·신세계·한화·HD현대·두산·GS·DL·CJ 전부 같다.

[OWNER] 「나머지는 원문 일단」 — 통과분 158쌍 사전은 **아직 안 넣었다.**
넣을 때 `kbond_issuer.py` 에 더하면 라이브·배치가 같이 따라온다.

---

## 3. §1.2 `발전채` 등록은 맞다 — 다만 경계

P6 의 `발전채`(491행) → **섹터어**로 등록하는 것은 맞다. 「잔존 2년 이내 발전채
사자」는 종목 없는 범주 콜이고 `SectorCat` 자리다.

경계만 지킬 것 : **섹터는 하나(발전채)로 묶되 발행체 정체는 다섯으로 갈려 있어야
한다.** `SectorCat='발전채'` 와 `IssuerCanon='한국서부발전'` 은 다른 축이다.

---

## 4. 새 파서 결함 둘 (미해석 문서에 없다)

### 4.1 발행체명 **안의 공백**을 못 넘는다 — 1,807행

```
원문   25.2.21(금) 메리츠 캐피탈 227-2 [민5.023%/끝전89/A+] 팔자
결과   BondName = '캐피탈227-2'          <- '메리츠' 를 버렸다
```

`RE_BOND_NAME = ([가-힣A-Za-z]{2,12})\s?(\d{1,4}-\d{1,3})` 이 공백을 하나만
허용해서 뒷조각만 집는다. 실측 — 머리말이 일반명사만 남은 행:

| 머리말 | 행수 | 앞말 있는 비율 | 잘려나간 앞말 |
|---|---|---|---|
| `캐피탈` | 902 | **91%** | BNK(599) · 메리츠(74) · ibk(43) · 엔에이치(39) |
| `화학` | 39 | 100% | LG(38) |
| `카드` | 26 | 100% | 케이비국민(14) · 비씨(12) |
| `지주` | 10 | 100% | 신한(10) |
| `에너지` | 6 | 100% | SK(4) · 지에스(2) |

⚠ 이건 **P6 «낱말 등록» 으로 고칠 게 아니다.** 사전에 `캐피탈` 을 넣으면
BNK·메리츠·IBK 가 한 통이 된다.

### 4.2 `차유동화` 826행 — 유동화증권 이름 앞이 잘린다

```
원문   26.2.28(토) 신보2023제1차유동화1-1(사) 팔자 (민3.855/끝전.3/AAA)
결과   BondName = '차유동화1-1'
```

같은 정규식 문제인데 원인이 다르다 — **발행체명 안에 숫자**(`신보2023제1차`)가
있어서 한글 뭉치만 잡는 규칙이 뒤쪽 `차유동화` 만 집는다. 오너 판단은
「유동화증권인데 일단 넘긴다」 — 고칠 때 4.1 과 같이 볼 것.

---

## 5. 내 쪽 미결

1. ~~**원장 교체**~~ — **끝났다(2026-09-04 08:16).** 위 정정 참조.
2. **158쌍 사전** — 검정은 끝났고 사전은 안 넣었다(오너 「나머지는 원문 일단」).
3. **`RE_BOND_NAME` 수리** — **4.1 은 끝났다**(2026-09-04, 8,627행 회복).
   4.2 `차유동화` 826행은 오너 보류로 그대로 둔다.
4. 모호 접두 32개 · 44,073행은 **원문에 정보가 없어** 어떤 규칙으로도 못 고친다
   (`SK` 로 시작하는 정본이 25개). `IssuerSource='raw'` 로 두는 것이 맞다.

## 재현

```bash
cd C:\Users\infomax\Projects\apps\kbond
python test_issuer.py
python -c "from kbond_issuer import canon_issuer as c; print(c('서부발전'), c('아이에스동서'), c('엠캐피탈'))"
```
