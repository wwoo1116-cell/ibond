# 미해석 레인 P4~P7 (2026-09-04)

[OWNER 2026-09-04] 「할 수 있는 거 해야지」 — 넷 중 셋을 넣고 하나는 NO-GO 로 닫았다.

## 요약

| | 판정 | 규모 |
|---|---|---|
| P4 종목 승계 | **NO-GO** — 사양의 95% 문턱에 못 닿는다(천장 94.6%) | — |
| P5 상태어 | 파서 절반만 | `State` 8,859행 |
| P6 낱말 등록 | 넣음 | OTHER 291,628 → **264,042** |
| P7 하이픈 없는 코드·오타 | 넣음 | 코드 152행 · `ㅇㄱ` 75행 |

재파싱 2,300초 · 원장 **8,225,391행 × 63열** · `replay_verify` [A]~[H] 전건 통과(크로스 0).

## P4 — NO-GO

사양의 배관검정을 그대로 돌렸다. 문면에 종목이 적힌 국고 축약호가에서 **종목을
가리고** 같은 레벨을 말한 종목으로 복원해 적중률을 잰다(후보 둘 이상이면 안 붙인다).

| 창 | 판정 | 적중 | 귀속률 |
|---|---:|---:|---:|
| 30초 | 805,997 | 91.7% | 45.4% |
| 60초 | 946,978 | 93.0% | 53.3% |
| 120초 | 1,034,116 | 93.9% | 58.2% |
| 300초 | 1,033,085 | 94.4% | 58.1% |
| **600초** | 958,011 | **94.6%** | 53.9% |
| 1800초 | 782,016 | 94.5% | 44.0% |

★**사양의 처방이 방향이 반대다.** 「95% 아래면 창을 좁혀라」인데 좁힐수록 나빠진다
(30초 91.7%). 넓히면 나아지지만 600초에서 천장을 치고 그 뒤로는 귀속률만 잃는다.
「모호하면 가장 최근을 쓴다」로 풀면 85.7~86.2% 로 더 나쁘다.

600초로 붙이면 약 103만 건 중 **5.6만 건이 틀린 종목**에 간다. 안 붙이는 것보다
나쁘므로 넣지 않는다.

⚠ 이 검정은 «코드가 적힌» 행으로 잰다. 맨 행은 문맥이 뻔해서 코드를 안 적었을
수도 있어 검정이 과소평가일 여지는 있다. 그러나 그건 검정할 수 없는 희망이고,
사양이 이 설계를 정했다.

## P5 — 파서 절반만

`State` 열(보류 5,002 · 저매도 1,954 · 제가매수 1,509 · 제가매도 1,409 · 저매수
1,358 · 마감 61)과 `STATE` 유형(8,859행)을 넣었다.

★**사양의 나머지 절반은 파서가 할 수 없다.** 실측하면 «저매수»·«제가매도»
3,404행 중 종목이 붙은 것은 **59행(1.7%)** 뿐이다 — 대부분 «직전 호가» 를 가리키는
맨 말이라, 어느 호가에 붙는지는 **방 문맥을 가진 엔진**(`kbond_live`)이 정해야 한다.
파서는 방을 가로질러 보지 않는다. 그래서 «무슨 말인지» 만 싣는다.

## P6 — 낱말 등록

- 섹터어에 `발전채`·`특수채` — «1.5~2년 발전채 사자» 같은 **종목 없는 범주 콜**이
  `Position` 만 붙고 `Sector` 가 비어 OTHER 로 떨어지고 있었다.
- `RegCap` 열(RWA0 243행) — 섹터가 아니라 «어떤 물건을 찾는가» 의 규제자본 축 속성.
- `NOTICE`(채용 공고 1,508행) · `EVAL`(평가사 예상종가 방송 394행) 유형 신설.
  ★EVAL 은 호가가 아니라 공시인데 그 숫자가 물가26-4 로 오태그되고 있었다.
- ⚠«마감» 은 채용공고의 «마감임박» 이 대부분이라 NOTICE 가 먼저 걸러야 한다
  (순서를 바꾸면 P5 로 샌다).

## P7 — 하이픈 없는 코드 · 오타

★**맨 수만 보면 안 된다.** 처음 넓게 세니 212,023행이 걸렸는데 대부분
`삼성카드2523` 같은 **크레딧 회차**였다. 사양의 예(서명 XXXX-XXXX)는 «코드 + 두 수
축약호가» 꼴이므로 **그 모양에서만** 받는다 — 그러면 152행이고, 사양이 「좁게 세도
225행」이라 한 자리와 같다.

자릿수: 3자리 `YYN` → YY-N · 4자리 `YYNN` → YY-NN(두 자리 달 먼저).
`2510`→25-10 · `2412`→24-12 · `237`→23-7 · `242`→24-2.

★복원의 옳고 그름은 파서가 못 판정한다(민평이 enrich 에 있다). 틀리면 enrich 의
`MAX_DIFF` 가드가 값을 안 채워 «코드는 있고 금리는 빈» 행이 된다.

`ㅇㄱ`(억 자판 오타) 75행을 수량 단위에 넣었다. `악` 은 이미 다른 갈래가 받는다.

## 재현

```bash
cd C:\Users\infomax\Projects\apps\kbond
python -c "import sys;sys.path.insert(0,'.');from parse_kbond_logs import extract as e; \
  print(e('2510 785 79-','2026-09-04')['BondCode'], \
        e('보류 [DS]','2026-09-04')['MsgType'], \
        e('1.5~2년 발전채 사자','2026-09-04')['Sector'])"
# -> 25-10 STATE 크레딧/기타
```
