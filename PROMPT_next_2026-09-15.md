# 다음 세션 인계 — K-Bond, 2026-09-15 · **레인 얼림**

## §0. 붙여 넣을 블록

```
K-Bond 레인. ★이 레인은 2026-09-15 에 [OWNER] 「락만 고치고 얼린다」로 **얼렸다.**
개발을 이어서 하지 마라. 내가 지정하는 것만 한다.

정본 —
  Projects\apps\kbond\PROMPT_next_2026-09-15.md   (이 파일 · 얼린 상태 · 유지보수 목록)
  Projects\apps\kbond\PROMPT_next_2026-09-11.md   (화면 둘 · 지문 대조 · 배관 함정)
  Projects\apps\kbond\PROMPT_next_2026-09-10.md   (레인 전체 · 규율 다섯)

얼린 뒤 이 레인에서 할 일은 셋뿐이다(§8):
  1. 아침 확인(§7-[C])  2. 고장 나면 고치기  3. 내가 지정하는 것

오너 판정이 하나 열려 있다 — [A] CD·CP 「3.72/73 팔자」의 뜻(§4).

손대게 되면 규율:
- 백엔드를 만졌으면 반드시 셋 다 — verify_v4 [A]~[I5] · gate_meaning [J][K][L] · pytest.
- 화면을 고쳤으면 지문 대조(node compare_screens.mjs)를 **두 번** 돌리고(첫 실행은 거짓
  양성이 난다) 눈으로 한 장(node shot_screen.mjs ... 국고 26-7).
- 재기동은  powershell -NoProfile -File restart_kbond.ps1
- 불변식은 «오퍼 금리 <= 비드 금리» 다. 락(간격 0)은 걷지 않는다 — 09-15 판정(§2-C).
- 귀속 규칙과 설명 변수가 축을 공유하면 그 상관은 검정이 아니다(§3).
- ★본표 `MPYieldDB`·`QuoteVsMP_bp` 는 «그날 종가» 민평이다. 장중·예측 문맥에선 선견이다.
```

---

## §1. 레인 방향과 얼림 [OWNER 2026-09-15]

**결론: 이 책은 알파 소스가 아니라 데스크의 «값 진실 층» 이고, 그 층은 이미 서 있다.
그래서 락만 고치고 얼린다.**

오너 질문 「so what?」 세 번을 거쳐 정리됐고 검정으로 크기까지 쟀다.

1. **체결 알파가 구조적으로 없다.** 거래가 나는 순간 두 면은 **같은 레벨에 있다**(체결의
   84.2%가 락). 먹을 스프레드가 아예 없다. 손잡이는 「얼마에」가 아니라 **「되느냐」**다.
2. **예측도 없다.** 살아남은 유일한 예측력(불균형 → 공격 방향 AUC 0.571, 위약 0.501)은
   하루 단위로 세 번 중 한 번 뒤집힌다.
3. **값 진실은 선다, 다만 얇다.** 장중 마킹에서 어제 민평보다 2bp 낫지만 그 대부분은
   «오늘 대 어제» 다. 만들 수 있는 최선의 대용(민평+커브이동)보다는 **0.3bp** 낫고, 그건
   RV 레인의 비용 경계(0.713bp)보다 작다. 게다가 **양면이 있을 때만**(15분 기준 53%).
4. **그래서 더 지을 값이 적다.** [OWNER] 락만 고치고 얼린다.

⚠**다시 열 만한 자리는 §8-B 에 적어 뒀다.** 큰 수는 전부 **딜러 축**에서 나왔다는 것이
오늘의 패턴이다(가격 축은 전부 작았다).

---

## §2. 오늘 한 것 — 셋

### A. 「K-Orderbook+++」 §11 지표 1차

정본 `RESULT_book_dynamics_2026-09-15.md` **(⚠수치 일부는 C 가 대체했다 — 머리말 참조)** ·
연구 `kbond_dyn_study.py`(3분) · `data\kbond\kbond_dyn_study.json`(락 수리 뒤 값으로 교체됨).

- 최우선 «레벨» 체결률 **84.3%** (v12 의 37% 는 «엔트리» 를 센 것이라 다른 물건)
- 불균형 → 공격 방향 **AUC 0.571**(그룹내 0.620 · 위약 0.501): 비드 우세 → 사 감 62.9% ·
  오퍼 우세 39.8%. ⚠하루 단위로는 3분의 1이 뒤집힌다
- **Hendershott 2025(JF) 재현** — 호가 품질 상위 3분위의 «체결/호가» 가 하위의 **1.31배**
  (위약 평평). 많이 내는 것과 잘 내는 것이 따로 기여
- 부산물: 체결 순간의 책(`_book_ctx`) · 딜러별 귀속 체결 · 동향 탭 넉 줄 · verify [I3~I5]

### B. 값 진실 층 검정 + 나이 밴드

정본 `RESULT_mark_and_age_2026-09-15.md` · `kbond_mark_test.py`(1분).

- **★민평은 종가 평가다.** 배치 `enrich.load_mp` 가 같은 날짜로 조인해서 본표
  `MPYieldDB`·`QuoteVsMP_bp` 가 **그날 종가**에 붙어 있다. 라이브는 전일이라 안전.
- 마킹: 우리 mid(−15분) 중앙 **0.25bp** 대 민평_전일 2.20bp, 530일 중 **99%의 날** 승.
  ★그러나 더 센 상대(민평+커브이동)엔 0.55 대 0.25 · 결판 중 64.7% = **0.3bp**.
- **나이 밴드 벼랑은 1분**(~1분 90.6% · 1~5분 61.2% · 5~15분 29.6%) → `AGE_STEPS (60,300)`,
  바램 깊이 `.82/.60`. React 가 문턱을 코드에 박아 둔 것도 서버 `age_steps` 로 모았다.

### C. ★락은 걷는 것이 아니다 [OWNER 「양면을 다 세운다」]

정본 **`RESULT_lock_2026-09-15.md`**. **불변식이 «오퍼 < 비드» → «오퍼 <= 비드» 로 바뀌었다.**

- 귀속 체결의 **23.5%가 uncross 가 걷어낸 호가에서 났고**, 그 레벨이 통째로 사라진 건
  0.2%뿐 = 거의 전부 **락**. 화면은 「3.710 사자」인데 실제는 「3.710 팔자가 맞았다」였다.
- **★그 표시 규칙이 측정까지 오염시키고 있었다.** 같은 스크립트를 uncross 한 줄만 바꿔
  다시 돌리자 아침 헤드라인 넷 중 **셋이 움직였다**:

| | 락 걷던 때 | 지금 |
|---|---:|---:|
| 걷어낸 호가에서 난 체결 | 23.5% | **0.7%** |
| 체결 시점 스프레드 0(락) | 0.3% | **84.2%** |
| 유효 반스프레드 중앙 | 0.25bp | **0.00bp** |
| 최우선 «레벨» 체결률 | 81.2% | **84.3%** |
| 불균형→15분 뒤 mid 그룹내 | +0.188 | **−0.038**(부호 뒤집힘) |
| 딜러 품질 상/하 | 1.60배 | **1.31배** |
| 불균형→공격 AUC | 0.578 | 0.571 (산다) |

- 같은 규칙이 **여섯 곳**에 있었다(엔진·`_book_ctx`·뷰·연구·옛 화면 JS·**검증기 안의 독립
  구현**). verify [C] 는 이제 «간격 있는 크로스 없음» 을 보고 **락 종목 수를 따로 센다.**
- 화면은 **한 행에 양면**을 그린다. 안 그리면 잔량 합에는 드는데 행이 없다(26-7 실측
  870억 대 750억). 스프레드 줄 「락 3.790 · 같은 레벨에 양면」 · 배지 «락» · 락 칸은 바램 없음.
- **라이브 확인(15:41)**: 양면 12종 중 **락 9종**. 어제까지 그 아홉은 전부 한 쪽이 지워져
  있었다. 그중 23-7 은 **같은 데스크가 양쪽에** 서 있다(H20-2).

---

## §3. 규율 — 오늘 값을 치른 것 다섯

▎**같은 축을 두 표에서 쓸 때는 «같은 함수» 를 지나게 하라.** 체결표에 `BrokerKey` 가 없어
서명 앞부분으로 키를 지었더니 본표와 **겹침 0%** 였다.

▎**★★★귀속 규칙과 설명 변수가 축을 공유하면 그 상관은 검정이 아니다.** 세 번 나왔다 —
잠금 동점(부호가 통째로 뒤집혔다) · 맨 «ㅎㅈ» 귀속 · 마킹 검정의 h=0.

▎**위약이 0 이 아니면 추정량부터 의심하라.** R2b 위약 −0.108 은 그룹 «간» 성분이 샌 것.

▎**★이기는 쪽이 크게 이기면 상대를 의심하라.** 민평을 89% 로 이긴 것의 대부분이 «오늘 대
어제» 였다. 시시한 상대를 이긴 수를 성과로 적지 말 것.

▎**★★★표시 규칙이 측정을 오염시킬 수 있다.** uncross 는 «화면을 어떻게 그릴까» 로 들어온
규칙인데 연구도 같은 함수를 썼다. **한 줄이 헤드라인 넷 중 셋을 움직였다.**

▎(계속 유효) 완료 줄 없는 로그는 실패한 로그다 · 판정을 고쳤으면 쓰는 자리를 전부 세라.

---

## §4. 오너 판정 대기 — 하나

### [A] CD·CP 「3.72/73 팔자」의 뜻 (09-10 부터 그대로)

전제 셋이 실측으로 깨졌다(양방 아님 SELL 18,917 대 BUY 15 · 범위 아님 내림 1,167 ·
어두운 행 아님). 판정은 **이미 책에 올라간 18,142행의 값이 맞나** 다. 뜻을 받기 전에는
짝 19,751행을 손대지 않는다.

### 닫힌 것

- ~~[B] 프로덕션 반영~~ → [OWNER] 「지금 바로」. **15:16 · 15:41 두 번 재기동 완료**,
  Vercel 프로덕션 배포 완료.
- ~~[C] 락~~ → [OWNER] 「양면을 다 세운다」. §2-C.
- ~~[D] 레인 방향~~ → [OWNER] 「일단 맞다고 가정하고 쭉 갑시다」 → 검정 뒤 **「락만 고치고
  얼린다」**. §1.

---

## §5. 지금 상태 (얼린 시점)

| | |
|---|---|
| 코드 | `Projects\apps\kbond` · `origin/kbond-backend` 에 전부 푸시됨 |
| 화면 | `Projects\apps\kbond-web`(원격 없음) · `out`·`out-kbond` 새로 구움 |
| **프로덕션** | `:8301` **새 코드로 돌고 있다**(15:41 재기동) · Vercel `kbond-web.vercel.app` 배포됨 |
| 본표 | `kbond_structured_data.parquet` 8,338,580행 · 63열 |
| 다리 · 체결 | `kbond_legs` 515,707행 · `kbond_fills` 86,228행 |
| 연구 산출 | `kbond_dyn_study.json`(락 수리 뒤 값) · `_lock.json` · `_fills.parquet` · `kbond_mark_test.json` · `kbond_age_bands.json` |
| 예약 | `KBondDailyUpdate` 평일 **15:50 + 08:05** · `KBondLive` **08:20** · `KBondAppBuild` **08:00** |
| 주소 | Funnel `/kbond`=옛 화면 · `/kbond/app`=React · 로컬 `:8301/app/` · Vercel `kbond-web.vercel.app` |

⚠`main` 과 `kbond-backend` 는 공통 조상이 없다. **머지 금지.**

---

## §6. 재현 — 검증 배터리

```bash
cd C:\Users\infomax\Projects\apps\kbond

# 동결 리플레이 (정본 검증일 20260903 · ★--at 안 걸면 동결이 아니다 · 기동 60~95초)
set KBOND_REPLAY=20260903&& set KBOND_AT=15:30:00&& ^
  python -X utf8 -m uvicorn kbond_api:app --host 127.0.0.1 --port 8303

python verify_v4.py --port 8303 --at 15:30:00 --day 20260903     # [A]~[I5]
python gate_meaning.py                                            # [J][K][L]
python -m pytest -q test_frac.py test_cors_token.py               # 55
python test_issuer.py                                             # 138/138

# 화면 (★지문 대조는 두 번 — 첫 실행은 화면이 아직 안 받아와 거짓 양성이 난다)
cd ..\kbond-web && npm run build && python -m http.server 3400 --directory out
chrome --headless=new --remote-debugging-port=9222 --user-data-dir=%TEMP%\kbchrome about:blank
cd ..\kbond
node compare_screens.mjs http://127.0.0.1:8303/ http://127.0.0.1:3400/ http://127.0.0.1:8303
node shot_screen.mjs http://127.0.0.1:3400/ http://127.0.0.1:8303 ob.png 국고 26-7

# 연구 (다시 돌릴 일이 있으면)
python kbond_dyn_study.py     # 3분   python kbond_mark_test.py   # 1분
```

**09-15 결과**: verify_v4 [A]~[I5] 전건 통과([C] 락 3종 · [I5] 90/90) · gate_meaning 전건 통과 ·
pytest 55 · test_issuer 138/138 · 지문 대조 다섯 영역 0건 · 동향 탭과 26-7 락 사다리 눈으로 확인.

---

## §7. 배관 — 얼린 뒤에도 남는 것

### [C] 아침 확인 (이게 얼린 레인의 일상이다)

```bash
powershell -c "Get-ScheduledTaskInfo -TaskName KBondLive | fl LastRunTime,LastTaskResult"
#   ⚠ 0x40010004 는 고장이 아니다 — ExecutionTimeLimit PT10H 로 스스로 죽은 것
curl -s http://127.0.0.1:8301/health
```

### 함정 — 데인 것만

1. **`Stop-ScheduledTask` 는 파이썬을 안 죽인다**(conhost 래퍼) → `restart_kbond.ps1`.
2. **기동 되감기 60~95초**(장 끝나갈수록 5분). 안 뜬다고 다시 죽이지 마라.
3. **Miniconda 파이썬이 Funnel 인증서를 거절한다** → 로컬이나 `curl`.
4. **재파싱 중 사전·코드 수정 금지.** 원장 동시 읽기도 막힌다.
5. **`kbond_sector.json` 을 안 구우면 유동화가 조용히 회사채로 접힌다.**
6. **지문 대조는 동결로만, 그리고 두 번.** 장중 실황은 폴링 시차, 첫 실행은 콜드 스타트.
7. **Next `basePath` 는 CSS 의 `url(/fonts/...)` 를 안 고친다** → `build_app.ps1`.
8. **화면 HTML 은 `no-store`**, 자산은 해시. 열려 있던 탭은 한 번 Ctrl+Shift+R.
9. **★문자열 치환에서 역슬래시가 접힌다 — 오늘도 두 번.** 셸을 지나는 패치를 쓰지 말고
   파일 도구로 고쳐라. `replace` 마다 `assert`.
10. **윈도우 경로를 문서에 스크립트로 쓰면 `\a` 가 BEL 로 들어간다.**
11. **★본표 `MPYieldDB`·`QuoteVsMP_bp` 는 «그날 종가» 민평**(§2-B).

---

## §8. 얼린 뒤 — 할 일과 안 할 일

### A. 할 일 (유지보수)

1. **아침 확인**(§7-[C]). 16:30 갱신이 끊기면 다음 실행이 소리 내어 말한다.
2. **고장 나면 고친다.** 파싱이 새 문형에 걸리면 `gate_meaning` 이 [K] 로 센다.
3. **[A] 판정이 오면** CD·CP 짝 19,751행을 본다.

### B. 다시 열 만한 자리 (지시가 있을 때만)

**★오늘의 패턴: 큰 수는 전부 «딜러 축» 에서 나왔고 작은 수는 전부 «가격 축» 에서 나왔다.**

| 축 | 오늘 나온 것 |
|---|---|
| 가격 | 스프레드 0(락) · 유효=0 · 불균형 AUC 0.571 · 마크 우위 0.3bp |
| 딜러 | 귀속 68.5%(양성대조 93.8%) · 품질→흐름 1.31배 · 하루 364 데스크 · 최우선 체류 시간 |

가격 축은 한 틱 시장이라 배울 게 없고 민평이 이미 종가에서 한 틱 안이다. 딜러 축은 국내에
**상대방이 찍힌 체결 테이프가 없어서** 벤더도 거래소도 못 파는 자료다. 다시 연다면 거기다.

- **딜러 축** — 「누구를 불러야 하나」. 자료가 제일 세다.
- **크레딧 소싱** — 시장 가치는 제일 크지만 자료가 얇다(체결에 종목명 4.6%). 먼저 재야 한다.
- 커버리지 53% 넓히기 · 마킹 배관 붙이기 — 오늘 결과로는 값이 작다.
- [B1] 판정 대기열 2회차(`kbond_issuer_todo.py 30`) — 제일 싸지만 접을 게 별로 없다.

### C. 원리적으로 못 하는 것 (건드리지 말 것)

범주 콜 약 84,000행(발행체가 문면에 없다) · 사모 유동화 SPC ~15,000행(원장에 없다) ·
계열 없는 39.9만행(내용 없는 확인) · 이름이 이미 맞는 것 ~5,000행 ·
크레딧 양면 사다리(종목 찍고 레벨 있는 QUOTE 가 BUY 0.1%) · 취소 관측(삭제는 TTL 뿐).

---

## §9. 오늘 커밋

```
kbond (origin/kbond-backend · 전부 푸시함)
  f9e8943  책이 말하는 것 — 체결 귀속이 열어 준 지표 셋 [OWNER 「K-Orderbook+++」]
  067b779  다음 세션 인계문 — 09-15 (1판)
  3cc581c  인계문 상세판 — 레인 방향까지 적는다 · shot_screen.mjs
  db1b006  인계문 §5·§10 해시를 맞춘다
  98dd239  값 진실 층 첫 검정 + 나이 밴드 재보정 [OWNER 「쭉 갑시다」]
  6c1433e  인계문 — 후반부까지 반영
  5b53f90  락은 걷는 것이 아니다 — 불변식을 «오퍼 <= 비드» 로 [OWNER]
  (+ 이 인계문을 «얼림» 으로 다시 쓴 커밋 하나)

kbond-web (원격 없음)
  9ac1326  책이 말하는 것 — 동향 탭 넉 줄 + 리더보드 «귀속» 열
  23f003f  나이 문턱은 서버가 정한다 + 바램 깊이를 같이 맞춘다
  d1b26eb  락 칸은 한 행에 양면을 그린다 [OWNER]
```

프로덕션 `:8301` 재기동 2회(15:16 · 15:41) · Vercel 프로덕션 배포 2회. `main` 은 안 건드렸다.
