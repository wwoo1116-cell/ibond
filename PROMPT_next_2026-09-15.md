# 다음 세션 인계 — 2026-09-15

## §0. 붙여 넣을 블록

```
K-Bond 레인. 정본 셋을 먼저 읽어라 —
  Projects\apps\kbond\PROMPT_next_2026-09-15.md   (이 파일 · 오늘 달라진 것)
  Projects\apps\kbond\PROMPT_next_2026-09-11.md   (화면 둘 · 지문 대조 · 배관 함정)
  Projects\apps\kbond\PROMPT_next_2026-09-10.md   (레인 전체 · 규율 · 여전히 유효)

오늘 할 것은 §6 후보에서 지정한다. 오너 판정 둘이 열려 있다(§3).

규율:
- 백엔드를 만졌으면 반드시 셋 다 — verify_v4 [A]~[I5] · gate_meaning [J][K][L] · pytest.
- 재기동은 손으로 하지 말고  powershell -NoProfile -File restart_kbond.ps1
  (태스크 Stop 은 파이썬을 안 죽인다. 09-11 §5)
- 화면을 고쳤으면 동결 리플레이로 지문 대조 — node compare_screens.mjs
- 책의 «값»을 새로 만들었으면 배치로도 한 번 재서 라이브와 체결 단위로 맞대라.
  둘은 같은 책이 아니다(다리·AtMP·방 중복) — 뜻은 «같은 수» 가 아니라 «같은 규약».
- [H2] 가 빨간불이면 먼저 09-11 §3-3 을 읽어라. 놀랄 일이 아닐 수 있다.
```

## §1. 앞 인계문 둘은 그대로 유효하다

09-11(화면 둘·지문 대조·배관 함정 일곱)과 09-10(규율 다섯·사전 굽는 순서·판정 대기열)에서
바뀐 것이 없다. 여기에는 **09-15 에 달라진 것만** 적는다.

정본: **`RESULT_book_dynamics_2026-09-15.md`** (오늘 한 일 전부 · 근거 · 한계).

## §2. 오늘 한 것 — [OWNER] 「K-Orderbook+++」

§11 의 다음 칸을 닫았다. ++(09-07)가 «체결은 삭제가 아니라 확인» 을 세우면서 남긴 부산물
(공격 방향 · 딜러별 체결)이 있어야만 잴 수 있던 지표를 **국고 전 이력 547영업일**에서 위약
대조로 쟀고, 살아남은 것을 동향 탭에 올렸다. 스크립트는 리포에 둔다(`kbond_dyn_study.py`).

1. **책은 한 틱이다.** 체결 시점 호가 스프레드가 **0.5bp 93.6%** → 유효 반스프레드가 호가
   반스프레드와 같다(0.25bp). 여기서 새로 나온 것은 **음수 19.7%** 이고 정체는
   **«크로스한 호가가 맞은 것» 23.5%** — 국고 체결의 넷 중 하나는 서 있는 호가가 아니라
   **치는 호가**다. v8 이 크로스를 「사실상 치는 호가」라 부른 것이 실측으로 섰다.
2. **최우선 «레벨» 체결률 81.2%.** ⚠v12 의 「37%」와 다른 수인데 틀린 것이 아니라 **다른
   것을 센 것**이다(v12 = 최우선 «엔트리», 여기 = 최우선 «레벨»). 트레이더에게 뜻이 있는
   것은 레벨이다.
3. **불균형은 «누가 칠지» 를 조금 안다.** 비드 우세 → 사 감 59.5% · 오퍼 우세 39.1% ·
   AUC 0.578 대 **위약 0.511±0.003**. 연도별로도 같은 자리(2024·25·26 전부).
   ⚠하루 단위로는 흔들린다(183일 중 35.5%가 50% 아래).
4. **Hendershott 2025 가 이 방에서도 선다.** 최우선 게시 비율 상위 3분위의 «체결/호가» 가
   하위의 **1.6배**(위약은 세 칸이 평평). 호가를 **많이** 내는 것과 **잘** 내는 것이 따로
   기여한다(날짜 FE 회귀 0.138 · 0.155). → 리더보드 «귀속» 열.
5. **R2b(불균형 → 15분 뒤 mid)는 화면에 안 올렸다.** 그룹내 +0.188 로 살아 있지만 부호가
   R2 와 반대 방향이고 정체가 **호가 mid 의 되돌림**이다. 거래 신호로 읽히면 안 된다.

### 새 규율 셋 — 오늘 값을 치른 것

▎**같은 축을 두 표에서 쓸 때는 같은 함수를 지나게 하라.** 체결표에는 `BrokerKey` 가 없어
서명 앞부분으로 키를 지었더니 본표와 **겹침 0%** 였다. 「딜러+레벨 귀속 0%」라는 못 믿을
숫자가 아니었으면 그냥 지나갔다. `parse_kbond_logs.broker_key` 를 지나게 하니 99.8%.

▎**★귀속 규칙과 설명 변수가 축을 공유하면 그 상관은 검정이 아니다.** 두 번 나왔다.
(a) **잠금 동점** — 락에서 자기 호가가 없으면 귀속은 «최신 것» 을 고르는데, 최신 호가는
사람이 많은 쪽일 확률이 높다. 안 빼면 부호가 **통째로 뒤집힌다**(오퍼 우세 → 사 감 67%).
(b) **맨 «ㅎㅈ» 귀속**(`csrc=prev`) — «그 딜러의 가장 최근 호가» 로 방향을 정하는데, 누가
최근에 냈는가는 불균형이 재는 것과 같은 것이다. → 화면의 불균형은 **종목을 문면에 적은
체결만** 센다(뺀 수는 괄호로 같이 보인다).

▎**위약이 0 이 아니면 추정량부터 의심하라.** R2b 위약이 −0.108 이었다 — 그룹 «간» 성분이
샌 것이었고, 종목-일 안에서 순위를 매기니 위약이 −0.001 로 앉았다.

## §3. 오너 판정 대기 — 둘

### [A] CD·CP 「3.72/73 팔자」의 뜻 (09-10 부터 그대로)

09-10 §5 를 그대로 읽어라. 전제 셋이 이미 실측으로 깨졌고(양방 아님·범위 아님·어두운 행
아님), 판정은 「어두운 행을 여는 것」이 아니라 **이미 책에 올라간 18,142행의 값이 맞나** 다.
뜻을 받기 전에는 짝 19,751행을 손대지 않는다.

### [B] ★프로덕션에 오늘 반영할 것인가 — 안 했다

**장중이라 백엔드를 재기동하지 않았다**(레인 규약 「장중이면 오너 확인 후」). 그래서 지금:

| | 상태 |
|---|---|
| 코드 | 디스크에 있고 커밋·푸시까지 끝났다 |
| `:8301` 백엔드 | **옛 코드가 돈다** — `book_info` 를 안 낸다 |
| Funnel `/kbond/app` 화면 | **새 판이 이미 서빙된다**(out-kbond 를 다시 구웠고 정적 파일이라 재기동 불필요) |
| 그래서 보이는 것 | 새 칸 넷은 **스스로 숨는다**(`book_info` 가 없으면 안 그린다) · 리더보드 «귀속» 열은 빈칸 · ▲▼ 없음 |
| Vercel 판 | **배포 안 했다** — 화면만 새로 가면 볼 것이 없다. 백엔드가 새 판이 된 뒤에 올리는 것이 맞다 |

**내일 아침 자동으로 맞춰진다**(08:00 `KBondAppBuild` → 08:20 `KBondLive`). 오늘 보고
싶으면 장 끝난 뒤 `powershell -NoProfile -File restart_kbond.ps1` 하나면 된다.
Vercel 은 그 뒤 `cd ..\kbond-web && vercel deploy --prod` (CLI 로그인 되어 있다).

## §4. 새로 생긴 것

| | |
|---|---|
| `kbond_dyn_study.py` | 국고 전 이력 연구. `--since`·`--perm`. 3분(적재 4s·접기 96s) |
| `data\kbond\kbond_dyn_study.json` | 수치 전부 |
| `data\kbond\kbond_dyn_study_fills.parquet` | 체결 39,889행 × 책 문맥(라이브 대조용) |
| `kbond_live.Book._book_ctx` | 체결 순간의 책 — `eff`·`qs`·`ab`·`imb`·`amb`. **연구와 같은 규약** |
| `kbond_view.book_info` | 동향 탭 넉 줄. 책을 **다시 접지 않는다**(엔진이 그때 잰 값을 모은다) |
| `verify_v4` [I3][I4][I5] | 뷰=재계산 · Σ딜러 f ≤ 귀속 수 · **최우선이면 유효=호가/2 항등식** |
| `LeaderRow.f` · `CurveTodayRow.imb` · `BookInfo` | 스키마 → `openapi.json` → `api-types.d.ts` |

## §5. 검증 (20260903 15:30 동결 리플레이)

```
verify_v4 [A]~[I5]  전건 통과      gate_meaning  전건 통과
pytest 55            test_issuer 138/138        compare_screens 다섯 영역 0건
```

그날 값: 귀속 104 · 유효 0.25 = 호가 0.25 · 최우선 82.5%(n=97) · 불균형 21건(+50 은 맨 ㅎㅈ).
동향 탭은 스크린샷으로 눈으로도 봤다 — **표현 계층은 verify 도 지문 대조도 안 덮는다**(v11·v12).

## §6. 다음 후보

1. **크레딧 책의 동학** — 오늘 것은 전부 국고다. 크레딧은 매도 일변도(99.9%)라 양면 불균형이
   없다. 물어야 할 것은 «수요 바구니 ↔ 매도 매칭» 쪽이고, 재료(`_match`)는 이미 서 있다.
2. **[B1] 판정 대기열 2회차** — `python kbond_issuer_todo.py 30`. 2,746종·43,643행.
3. **지문 대조를 아침에 자동으로** — 동결 리플레이로 매일 한 번, 0건이 아니면 로그에.
4. **딜러 품질 축을 늘린다** — 지금은 «게시 순간 최우선 여부» 하나다. 나이·양면 여부·재게시
   빈도를 넣으면 R3 가 «어떤 호가가 흐름을 가져오나» 로 좁혀진다.
5. **기본 화면(`/`)을 새 화면으로 돌릴지** — 쓰면서 정한다. 되돌리기는 한 줄.

## §7. 오늘 커밋

```
kbond (origin/kbond-backend · f9e8943 푸시함)
  f9e8943  책이 말하는 것 — 체결 귀속이 열어 준 지표 셋 [OWNER 「K-Orderbook+++」]

kbond-web (원격 없음)
  9ac1326  책이 말하는 것 — 동향 탭 넉 줄 + 리더보드 «귀속» 열
```

`main` 은 안 건드렸다. 프로덕션 재기동·Vercel 배포는 §3-[B] 대로 안 했다.
