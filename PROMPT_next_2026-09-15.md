# 다음 세션 인계 — K-Bond, 2026-09-15

## §0. 붙여 넣을 블록

```
K-Bond 레인. 정본 셋을 순서대로 읽어라 —
  Projects\apps\kbond\PROMPT_next_2026-09-15.md   (이 파일 · 오늘 달라진 것 · 레인 방향)
  Projects\apps\kbond\PROMPT_next_2026-09-11.md   (화면 둘 · 지문 대조 · 배관 함정)
  Projects\apps\kbond\PROMPT_next_2026-09-10.md   (레인 전체 · 규율 다섯 · 여전히 유효)

내가 지정하는 것만 한다. 임의로 분석을 시작하지 않는다.
오늘 할 것은 §8 후보에서 지정한다. 오너 판정 셋이 열려 있다(§4) — [C] 가 제일 값나간다.

규율:
- 백엔드를 만졌으면 반드시 셋 다 — verify_v4 [A]~[I5] · gate_meaning [J][K][L] · pytest.
- 화면을 고쳤으면 동결 리플레이로 지문 대조(node compare_screens.mjs)와
  눈으로 한 장(node shot_screen.mjs ... 국고 26-7). 지문 대조는 «색» 도 «새 칸» 도 안 본다.
- 재기동은 손으로 하지 말고  powershell -NoProfile -File restart_kbond.ps1
- 책의 «값»을 새로 만들었으면 배치로도 한 번 재서 라이브와 체결 단위로 맞대라.
  둘은 같은 책이 아니다(다리·AtMP·방 중복) — 뜻은 «같은 수» 가 아니라 «같은 규약».
- 귀속 규칙과 설명 변수가 축을 공유하면 그 상관은 검정이 아니다(§3).
- ★본표 `MPYieldDB`·`QuoteVsMP_bp` 는 «그날 종가» 민평이다. 장중·예측 문맥에서는 선견이다(§3).
- [H2] 가 빨간불이면 먼저 09-11 §3-3 을 읽어라. 놀랄 일이 아닐 수 있다.
```

---

## §1. ★레인 방향 [OWNER 2026-09-15 「일단 맞다고 가정하고 쭉 갑시다」 = 잠정 채택]

**이 책은 알파 소스가 아니다. 데스크의 «값 진실 층» 이다.**

오너 질문 「so what?」 에 답하면서 정리됐고, 그날 오후에 **검정으로 확인까지 했다**(§2-B).
근거 넷:

1. **체결 알파가 구조적으로 없다.** 폭 전체가 0.5bp(체결 시점 호가 스프레드의 93.6%)이고
   81%는 이미 최우선에서 난다. 잘해야 반 틱 = 5년물 100억에 125만원. **손잡이는 「얼마에」가
   아니라 「되느냐」다.**
2. **예측도 없다.** 살아남은 유일한 예측력(불균형 → 공격 방향 AUC 0.578, 위약 0.511)은
   하루 단위로 세 번 중 한 번 뒤집힌다.
3. **대신 값 진실이 선다** — 장중 마킹에서 우리 mid(15분 전 책)가 손에 잡히는 유일한 대안
   (어제 민평)보다 **2bp** 낫고(530일 중 99%의 날), 만들 수 있는 최선의 대용
   (민평+커브이동)보다 **0.3bp** 낫다.
4. ⚠**다만 좁다.** 값은 «더 똑똑한 추정» 이 아니라 **«하루 묵은 마크를 분 단위로 바꾸는 것»**
   이고, **양면 책이 있을 때만**이다(15분 기준 체결의 53%). 0.3bp 는 RV 레인이 잰 비용
   경계(0.713bp)보다 작다 — **마킹이 좋아졌다고 RV 손익이 따라오지는 않는다.**

**그래서 다음을 어디에 쓰나.** 신호 사냥은 접는다. 이 층을 ①옳게 그리고(§4-[C] 락) ②넓히고
(커버리지 53%가 진짜 한계) ③마킹·민평 대비·RV 가 기대게 붙인다.

---

## §2. 오늘 한 것

### A. 「K-Orderbook+++」 — §11 지표 1차

정본 **`RESULT_book_dynamics_2026-09-15.md`** · 연구 `kbond_dyn_study.py`(3분) ·
`data\kbond\kbond_dyn_study.json` · `kbond_dyn_study_fills.parquet`(39,889행 × 책 문맥).
국고 전 이력 **547영업일**(호가 175만 · 체결 39,889)에서 위약 대조.

| | 값 | 위약 |
|---|---:|---:|
| 체결 시점 호가 스프레드 0.5bp 비중 | 93.6% | |
| 유효 반스프레드 (= 호가 반스프레드) | 0.25bp | |
| 최우선 **레벨** 체결률 | 81.2% | |
| uncross 가 걷어낸 호가에서 난 체결 | 23.5% | |
| 불균형 → 공격 방향 AUC | 0.578 | 0.511 ± 0.003 |
| 호가 품질 상위 3분위 «체결/호가» | 하위의 1.6배 | 평평(0.015) |

- **책은 한 틱이다** → 유효 스프레드는 새 정보가 아니다. 새 정보는 유효가 음수인 19.7% 쪽이고
  정체는 위 23.5% 와 같은 것(= §4-[C]).
- **최우선 «레벨» 81.2%** — v12 의 「37%」는 «엔트리» 를 센 것이라 다른 물건이다.
  최우선 뒤에서 맞은 18.8% 는 **최우선이 묵었을 때**(그때 최우선 나이 중앙 120초).
- **Hendershott 2025(JF) 가 이 방에서도 선다** — 많이 내는 것과 **잘** 내는 것이 따로 기여
  (날짜 FE 회귀 0.138 · 0.155). → 리더보드 «귀속» 열.
- **R2b(불균형 → 15분 뒤 mid)는 화면에 안 올렸다** — 그룹내 +0.188(위약 −0.001)로 살아
  있지만 정체가 «호가 mid 의 되돌림» 이라 거래 신호로 읽히면 안 된다.

### B. 값 진실 층 첫 검정 + 나이 밴드 재보정

정본 **`RESULT_mark_and_age_2026-09-15.md`** · `kbond_mark_test.py`(1분) ·
`data\kbond\kbond_mark_test.json` · `kbond_age_bands.json`.

**①민평은 종가 평가다.** `국고통_민평` 최대 일자가 늘 전 영업일인데 배치 `enrich.load_mp` 는
**같은 날짜로 조인**한다 → 본표 `MPYieldDB`·`QuoteVsMP_bp` 는 그날 **종가**에 붙은 값이다.
서술에는 맞고 **장중·예측에는 선견**이다. 라이브는 전일 민평이라 안전하다. 위험한 건 본표를
쓰는 연구다.

**②마킹 채점**(국고 체결 40,011건) — |체결 − 마크| bp 중앙:

| 민평_전일 | 민평_당일(선견) | 우리 mid −15분 | 우리 mid −60분 |
|---:|---:|---:|---:|
| 2.20 | 0.30 | **0.25** | 0.75 |

짝 비교에서 −15분이 **89.1% 승**(n=21,332), 날짜별로 **530일 중 99%의 날**에 승.
★**그 승리의 대부분은 «오늘 대 어제» 다** — 민평_당일은 체결과 0.30bp 로 **종가 마크로는
훌륭하다.** 그래서 더 센 상대(전일 민평 + 그날 지표물의 우리-책 이동)를 붙였다:
**0.55 대 0.25bp · 결판난 것 중 64.7% 승.** 책은 커브 너머로 종목 고유의 것을 안다, 0.3bp만큼.
⚠직전체결(중앙 0.00bp)은 상대가 못 된다 — 시차 중앙 63초·49%가 60초 이내로 재보고가 섞였다.

**③나이 밴드** — 최우선 나이별 «그 레벨에서 체결»: ~1분 **90.6%** · 1~5분 61.2% ·
5~15분 29.6% · 15~30분 16.7%. **벼랑은 1분이다.** 옛 `AGE_STEPS=(300,900)` 은 한 칸에
90.6%와 61.2%를 같이 담고 있었다 → **(60, 300)**.

### 라이브·화면에 넣은 것

| 곳 | 내용 |
|---|---|
| `kbond_live.Book._book_ctx` | 체결 «순간» 의 책 — `eff`·`qs`·`ab`·`imb`·`amb`. 국고·통안만 |
| `kbond_live` 체결 처리 | 테이프·이벤트에 위 값 · 맞은 호가의 주인에게 `dealers[k]["f"] += 1` |
| `kbond_live.AGE_STEPS` | **(60, 300)** — 근거는 위 ③ |
| `kbond_view.book_info` | 동향 탭 넉 줄(책을 다시 접지 않고 엔진이 그때 잰 값을 모은다) |
| `kbond_view.view()["age_steps"]` | **바램 문턱을 한 곳에서만 정한다**(아래 ★) |
| `kbond_view.curve_today` / `leaderboard` | 지금 불균형 `imb` / 귀속 체결 `f` |
| `kbond_schema` → `openapi.json` → `api-types.d.ts` | `Fill.eff/qs/ab/imb/amb` · `LeaderRow.f` · `CurveTodayRow.imb` · `BookInfo` · `View.book_info` · `View.age_steps` |
| `verify_v4` **[I3][I4][I5]** | 뷰=재계산 · Σ딜러 f ≤ 귀속 수 · **최우선이면 「유효=호가/2」 항등식** |
| `shot_screen.mjs` | 새 화면 한 탭 찍기(+«종목 고르기» 인자) |
| `kbond-web` `Trends.tsx` | 맥박 카드 아래 넉 줄 · 리더보드 «귀속» 열 · 커브 오늘 ▲▼ |
| `kbond-web` `Bonds.tsx`·`kbond.css` | 서버 `age_steps` 를 받아 쓰고 바램 깊이 `.82`/`.60` |

★**React 화면이 문턱 300/900 을 코드에 박아 두고 있었다.** 서버만 고쳤으면 두 화면이 다른
색을 말했을 것이고 **지문 대조는 색을 안 본다** — 09-11 규율의 재발이다. 한 곳으로 모았다.

★**바램 깊이는 문턱과 한 벌이다.** 옛 `.68`/`.42` 는 «15분 넘은 소수» 를 지우려던 값이라
문턱만 좁히면 책 대부분이 가장 옅은 칸에 떨어져 순서도 글자도 죽는다 → `.82`/`.60`.

---

## §3. 새 규율 넷 — 오늘 값을 치른 것

▎**같은 축을 두 표에서 쓸 때는 «같은 함수» 를 지나게 하라.** 체결표에 `BrokerKey` 가 없어
서명 앞부분으로 키를 지었더니 본표와 **겹침 0%**, 「딜러+레벨」 귀속이 통째로 0 이 됐다.
`parse_kbond_logs.broker_key` 를 지나게 하니 99.8%.

▎**★★★귀속 규칙과 설명 변수가 축을 공유하면 그 상관은 검정이 아니다.** 오늘 세 번 나왔다.
①**잠금 동점**(락에서 자기 호가가 없으면 «최신 것» 을 고르는데 최신은 사람 많은 쪽일 확률이
높다) → 부호가 **통째로 뒤집혔다**. ②**맨 «ㅎㅈ» 귀속**(그 딜러의 최근 호가로 방향 결정) →
화면 불균형은 `stated` 만 센다. ③**마킹 검정의 h=0**(과녁이 책 안에 있다) → h=15·60분이 본판.

▎**위약이 0 이 아니면 추정량부터 의심하라.** R2b 위약 −0.108 은 그룹 «간» 성분이 샌 것.

▎**★이기는 쪽이 크게 이기면 상대를 의심하라.** 민평_전일을 89% 로 이겼는데 그 대부분이
«오늘 대 어제» 였다. 더 센 상대(민평+커브이동)를 붙이자 0.3bp·64.7% 로 내려앉았다.
**시시한 상대를 이긴 수를 성과로 적지 말 것.**

▎(계속 유효) 완료 줄이 없는 로그는 실패한 로그다 · 판정을 고쳤으면 그 판정을 쓰는 자리를 전부 세라.

---

## §4. 오너 판정 대기 — 셋

### [C] ★락에서 uncross 가 «체결되는 쪽» 을 지운다 — 고칠 것인가 (제일 값나간다)

**실측**: 귀속 체결 38,746건 중 **23.5%가 uncross 가 걷어낸 호가에서 났다.** 그 레벨이 화면에서
통째로 사라지는 경우는 **0.2%뿐**이고 나머지는 **그 레벨이 반대편 한 쪽만 보인다.** 즉 거의
전부 **락**(간격 0)이고, 화면은 「3.710 사자」를 보여 주는데 실제로는 「3.710 팔자가 맞았다」이다.
걷어낸 호가의 나이 중앙 26초로 스테일해서 지운 것도 아니다(남은 쪽 11초).

**왜 판정이 필요한가.** uncross 는 v2.5 에서 오너가 「진짜 제대로 보고온거 맞아?」로 눌러서
들어온 규칙이고 근거는 코인베이스 책의 «크로스 0» 불변식이었다. 그건 **체결 엔진이 강제하는**
불변식인데 메신저 시장에는 그 엔진이 없어 **락이 진짜 상태로 오래 선다**(v8 실측 하루 907건 =
국고 호가의 40%).

**제안**: 간격이 «있는» 진짜 크로스는 지금처럼 걷고, **간격 0 인 락은 양면을 다 세우고 칸을
«락» 으로 표시한다.** 오너의 「락은 이벤트가 아니다」(09-03)는 이벤트 피드 이야기이지 사다리
표시가 아니다. ⚠불변식 «오퍼 금리 < 비드 금리» 를 손대는 일이라 `verify [C]` 도 같이 고쳐야
한다(락은 위반이 아니라고 말하게). **판정 없이는 손대지 않는다.**

### [B] 프로덕션에 반영할 것인가 — 오늘 안 했다

**장중이라 백엔드를 재기동하지 않았다**(레인 규약 「장중이면 오너 확인 후」).

| | |
|---|---|
| 코드 | 디스크에 있고 커밋·푸시까지 끝났다 |
| `:8301` 백엔드 | **옛 코드가 돈다** — `book_info`·`age_steps` 를 안 낸다 |
| Funnel `/kbond/app` | **새 판이 서빙된다**(out-kbond 를 다시 구웠고 정적이라 재기동 불필요) |
| 그래서 보이는 것 | 새 칸 넷은 **스스로 숨고** 나이 밴드는 **폴백 (60,300)** 으로 이미 새 값이다 |
| Vercel `kbond-web.vercel.app` | **배포 안 했다** |

**내일 아침 자동으로 맞춰진다**(08:00 `KBondAppBuild` → 08:20 `KBondLive`).
오늘 보려면 장 끝난 뒤 `powershell -NoProfile -File restart_kbond.ps1`.
Vercel 은 그 뒤 `cd ..\kbond-web && vercel deploy --prod`(CLI 로그인 되어 있다 `wwoo1116-6173`).

### [A] CD·CP 「3.72/73 팔자」의 뜻 (09-10 부터 그대로)

전제 셋이 실측으로 깨졌다(양방 아님 SELL 18,917 대 BUY 15 · 범위 아님 내림 1,167 ·
어두운 행 아님). 판정은 **이미 책에 올라간 18,142행의 값이 맞나** 다. 뜻을 받기 전에는
짝 19,751행을 손대지 않는다.

### ~~[D] 레인 방향~~ — [OWNER] 「일단 맞다고 가정하고 쭉 갑시다」로 **잠정 채택**(§1).

---

## §5. 지금 상태

| | |
|---|---|
| 코드 | `Projects\apps\kbond` · `origin/kbond-backend` 에 전부 푸시됨 |
| 화면 | `Projects\apps\kbond-web`(원격 없음) · `out`(Vercel판)·`out-kbond`(Funnel판) 새로 구움 |
| 본표 | `kbond_structured_data.parquet` **8,338,580행 · 63열** |
| 다리 · 체결 | `kbond_legs` 515,707행 51열 · `kbond_fills` 86,228행 33열 |
| 연구 산출 | `kbond_dyn_study.json`·`_fills.parquet` · `kbond_mark_test.json` · `kbond_age_bands.json` |
| 예약 | `KBondDailyUpdate` 평일 **15:50 + 08:05** · `KBondLive` **08:20** · `KBondAppBuild` **08:00** |
| 주소 | Funnel `/kbond`=옛 화면 · `/kbond/app`=React · 로컬 `127.0.0.1:8301/app/` · Vercel `kbond-web.vercel.app` |

⚠`main` 과 `kbond-backend` 는 **공통 조상이 없다**. 머지 금지.

---

## §6. 재현 — 검증 배터리 통째

```bash
cd C:\Users\infomax\Projects\apps\kbond

# 1) 동결 리플레이 (정본 검증일 20260903 · ★--at 안 걸면 동결이 아니다 · 기동 60~95초)
set KBOND_REPLAY=20260903&& set KBOND_AT=15:30:00&& ^
  python -X utf8 -m uvicorn kbond_api:app --host 127.0.0.1 --port 8303

# 2) 검증 셋
python verify_v4.py --port 8303 --at 15:30:00 --day 20260903     # [A]~[I5]
python gate_meaning.py                                            # [J][K][L]
python -m pytest -q test_frac.py test_cors_token.py               # 55
python test_issuer.py                                             # 138/138

# 3) 화면 — 지문 대조 + 눈으로 한 장
cd ..\kbond-web && npm run build && python -m http.server 3400 --directory out
chrome --headless=new --remote-debugging-port=9222 --user-data-dir=%TEMP%\kbchrome about:blank
cd ..\kbond
node compare_screens.mjs http://127.0.0.1:8303/ http://127.0.0.1:3400/ http://127.0.0.1:8303
node shot_screen.mjs http://127.0.0.1:3400/ http://127.0.0.1:8303 ob.png 국고 26-7

# 4) 연구
python kbond_dyn_study.py     # 3분 — §11 지표
python kbond_mark_test.py     # 1분 — 마크 채점
```

**09-15 결과**: verify_v4 [A]~[I5]·gate_meaning 전건 통과 · pytest 55 · test_issuer 138/138 ·
지문 대조 다섯 영역 **0건** · 동향 탭과 26-7 사다리 눈으로 확인.

---

## §7. 배관 함정 — 데인 것만 (전부 아직 유효)

1. **`Stop-ScheduledTask` 는 파이썬을 안 죽인다**(conhost 래퍼) → `restart_kbond.ps1`.
2. **기동 되감기 60~95초**(장 끝나갈수록 5분). 안 뜬다고 다시 죽이지 마라.
3. **Miniconda 파이썬이 Funnel 인증서를 거절한다** → 로컬이나 `curl`.
4. **재파싱 중 사전·코드 수정 금지**(워커가 새 코드를 집는다). 원장 동시 읽기도 막힌다.
5. **`kbond_sector.json` 을 안 구우면 유동화가 조용히 회사채로 접힌다.**
6. **지문 대조는 동결로만** — 장중 실황은 폴링 시차로 0건이 안 나온다.
7. **Next `basePath` 는 CSS 안 `url(/fonts/...)` 를 안 고친다** → `build_app.ps1`·`fix_font_prefix.py`.
8. **화면 HTML 은 `no-store`**, 자산은 해시. 열려 있던 탭은 한 번 Ctrl+Shift+R.
9. **★문자열 치환에서 역슬래시가 접힌다 — 오늘도 두 번 터졌다.** 셸을 지나는 패치는 쓰지 말고
   파일 도구로 고쳐라. `replace` 마다 `assert`(둘 다 assert 가 막았다).
10. **윈도우 경로를 문서에 스크립트로 쓰면 `\a` 가 BEL 로 들어간다.** 슬래시를 쓰거나 되읽어 검사.
11. **★본표 `MPYieldDB`·`QuoteVsMP_bp` 는 «그날 종가» 민평이다**(§2-B①). 장중 신호를 만들면 선견이다.

---

## §8. 다음 후보 — 값 순서

### 1. ★[C] 락 수리 — 판정 받으면 제일 먼저, 반나절

체결의 **23.5%가 옳은 쪽에 그려진다.** 할 일: `uncross`/`uncross_best` 를 간격 0 과 간격 있음
으로 가르고, 사다리 칸에 «락» 표시를 넣고, `verify [C]` 를 「락은 위반이 아니다」로 고치고,
전 이력으로 「그 레벨의 어느 쪽이 맞았나」를 다시 센다.
⚠**불변식을 건드리는 일**이다 — 통째 재작성이 이 불변식을 두 번 뒤집은 전례가 있다(v3·v2.5).

### 2. ★★커버리지를 넓힌다 — 이 층의 진짜 한계, 하루

값 진실 층의 한계는 정확도가 아니라 **양면이 없으면 할 말이 없다**는 것이다(15분 기준 53%).
한쪽만 있을 때 무엇을 말할지가 다음 물음이다. 후보: ①단면 + 전일 민평으로 «최소한 이쪽» 을
말하기 ②같은 연물 지표물 이동을 얹은 값을 «대용 mid» 로 내고 그 꼬리표를 붙이기
(§2-B 의 커브이동 상대가 이미 그 계산이다 — 0.55bp 짜리다).
⚠**꼬리표 없이 섞지 말 것.** 화면이 «관측» 과 «대용» 을 같은 색으로 말하면 이 층이 죽는다.

### 3. 마킹을 실제로 붙인다 — 하루

이 층을 쓰는 첫 소비자를 만든다. 히트맵의 «민평 대비» 를 «전일 민평 대비(관측)» 로 이름을
정확히 하고, 종목 화면에 «지금 우리 마크 대 전일 민평» 을 한 줄로 낸다. §2-B 가 그 수의
신뢰구간을 이미 줬다.

### 4. 크레딧 책의 동학 — 며칠

오늘 것은 전부 국고다. 크레딧은 매도 일변도(99.9%)라 양면 불균형이 없다. 물어야 할 것은
«수요 바구니 ↔ 매도 매칭» 이고 재료(`_match`)는 서 있다.

### 5. 딜러 품질 축 확장 — 반나절

지금 품질은 «게시 순간 최우선 여부» 하나다. 나이·양면 여부·재게시 빈도를 넣으면 R3 가
「어떤 호가가 흐름을 가져오나」로 좁혀진다.

### 6. [B1] 판정 대기열 2회차 — 제일 싸다

`python kbond_issuer_todo.py 30`. 2,746종·43,643행. **접을 것이 별로 없다**(09-10 §6).

### 7. 지문 대조를 아침에 자동으로

동결 리플레이로 매일 한 번, 0건이 아니면 로그에. ⚠오늘 배운 대로 **색과 새 칸은 안 덮는다** —
`shot_screen.mjs` 를 같이 걸지 아니면 그건 사람 몫으로 둘지 정할 것.

### ~~나이 밴드 재보정~~ · ~~값 진실 층 첫 검정~~ — 09-15 에 닫았다(§2-B).

---

## §9. 원리적으로 못 하는 것 (건드리지 말 것)

- **범주 콜 약 84,000행** — 발행체가 문면에 없다.
- **사모 유동화 SPC ~15,000행** — 채권 원장에 없다.
- **계열 없는 39.9만행** — 내용 없는 확인.
- **이름이 이미 맞는 것 ~5,000행** — raw 가 정직하다.
- **크레딧 양면 사다리** — 종목 찍고 레벨 있는 QUOTE 가 BUY 0.1%다.
- **취소 관측** — 명시적 철회가 21일에 10행뿐. 삭제는 TTL 뿐이다.

---

## §10. 오늘 커밋

```
kbond (origin/kbond-backend · 전부 푸시함)
  f9e8943  책이 말하는 것 — 체결 귀속이 열어 준 지표 셋 [OWNER 「K-Orderbook+++」]
  067b779  다음 세션 인계문 — 09-15 (1판)
  3cc581c  인계문 상세판 — 레인 방향까지 적는다 · shot_screen.mjs
  db1b006  인계문 §5·§10 해시를 맞춘다
  98dd239  값 진실 층 첫 검정 + 나이 밴드 재보정 [OWNER 「쭉 갑시다」]
  (+ 이 인계문을 후반부까지 반영한 커밋 하나)

kbond-web (원격 없음)
  9ac1326  책이 말하는 것 — 동향 탭 넉 줄 + 리더보드 «귀속» 열
  23f003f  나이 문턱은 서버가 정한다 + 바램 깊이를 같이 맞춘다
```

`main` 은 안 건드렸다. 프로덕션 재기동·Vercel 배포는 §4-[B] 대로 안 했다.
