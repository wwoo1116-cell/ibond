# 다음 세션 인계 — K-Bond, 2026-09-15

## §0. 붙여 넣을 블록

```
K-Bond 레인. 정본 셋을 순서대로 읽어라 —
  Projects\apps\kbond\PROMPT_next_2026-09-15.md   (이 파일 · 오늘 달라진 것 · 레인 방향)
  Projects\apps\kbond\PROMPT_next_2026-09-11.md   (화면 둘 · 지문 대조 · 배관 함정)
  Projects\apps\kbond\PROMPT_next_2026-09-10.md   (레인 전체 · 규율 다섯 · 여전히 유효)

내가 지정하는 것만 한다. 임의로 분석을 시작하지 않는다.
오늘 할 것은 §8 후보에서 지정한다. 오너 판정 넷이 열려 있다(§4) — [C]·[D] 가 새것이다.

규율:
- 백엔드를 만졌으면 반드시 셋 다 — verify_v4 [A]~[I5] · gate_meaning [J][K][L] · pytest.
- 화면을 고쳤으면 동결 리플레이로 지문 대조(node compare_screens.mjs)와
  **눈으로 한 장**(node shot_screen.mjs). 지문 대조는 새 화면에만 있는 칸을 안 본다.
- 재기동은 손으로 하지 말고  powershell -NoProfile -File restart_kbond.ps1
- 책의 «값»을 새로 만들었으면 배치로도 한 번 재서 라이브와 체결 단위로 맞대라.
  둘은 같은 책이 아니다(다리·AtMP·방 중복) — 뜻은 «같은 수» 가 아니라 «같은 규약».
- **귀속 규칙과 설명 변수가 축을 공유하면 그 상관은 검정이 아니다**(§3).
- [H2] 가 빨간불이면 먼저 09-11 §3-3 을 읽어라. 놀랄 일이 아닐 수 있다.
```

---

## §1. ★이 레인이 무엇인지가 오늘 갈렸다 [제안 · 오너 판정 대기 = §4-D]

오너 질문 「so what?」 에 답하면서 정리된 것이다. **아래는 내 읽기이고 아직 판정이 아니다.**

**이 책은 알파 소스가 아니다. 데스크의 «값 진실 층» 이다.**

근거는 오늘 실측 셋이다.

1. **체결 알파가 구조적으로 없다.** 시장 폭 전체가 0.5bp(체결 시점 호가 스프레드의 93.6%)다.
   아무리 잘해야 반 틱이고 5년물 100억에 125만원인데, 그마저 **81%는 이미 최우선에서 난다.**
   실행을 갈고닦을 자리가 없다 — 손잡이는 「얼마에」가 아니라 **「되느냐」**다.
2. **예측도 없다.** 살아남은 유일한 예측력이 불균형 → 공격 방향 AUC 0.578(위약 0.511)인데
   하루 단위로 세 번 중 한 번 뒤집힌다. 거래 신호로 못 쓴다.
3. **대신 허가가 생겼다.** 어제까지 이 화면의 값은 «딜러들이 하는 말» 이었다. 오늘부터는
   **«거래가 실제로 나는 자리»** 다(최우선 레벨 체결률 81.2% · 숨은 비용 없음). 이게 없으면
   이 책으로 마킹도 민평 대비 판단도 커브도 RV 도 못 쓴다. §11 을 지은 값이 여기 있다.

**그래서 다음 한 달을 어디에 쓰나.** 신호 사냥을 접고 이 책을 마킹·민평 대비·RV 가 기대는
층으로 세운다. 그 방향의 첫 걸음이 §8-3 이고, 그건 하루짜리다. 락 수리·나이 밴드·리더보드는
전부 그 방향에서의 잔손질이다.

⚠**오너가 아니라고 하면 §8 의 순서가 바뀐다.** 먼저 물을 것.

---

## §2. 오늘 한 것 — [OWNER] 「K-Orderbook+++」

정본 **`RESULT_book_dynamics_2026-09-15.md`**(근거·한계·라이브 대 배치 대조 전부).
연구 `kbond_dyn_study.py`(3분) · 수치 `data\kbond\kbond_dyn_study.json` ·
체결별 표 `kbond_dyn_study_fills.parquet`(39,889행 × 책 문맥).

§11 이 ++(09-07)에서 «체결은 삭제가 아니라 확인» 을 세우며 남긴 부산물(공격 방향 · 딜러별
체결)이 있어야만 잴 수 있던 지표를 **국고 전 이력 547영업일**(호가 175만 · 체결 39,889)에서
위약 대조로 쟀다.

| | 값 | 위약 |
|---|---:|---:|
| 체결 시점 호가 스프레드 0.5bp 비중 | 93.6% | |
| 유효 반스프레드 (= 호가 반스프레드) | 0.25bp | |
| 최우선 **레벨** 체결률 | 81.2% | |
| uncross 가 걷어낸 호가에서 난 체결 | 23.5% | |
| 불균형 → 공격 방향 AUC | 0.578 | 0.511 ± 0.003 |
| 호가 품질 상위 3분위 «체결/호가» | 하위의 1.6배 | 평평(0.015) |

- **★책은 한 틱이다.** 그래서 «유효 스프레드» 는 새 정보가 아니다. 새 정보는 유효가
  **음수인 19.7%** 쪽이고, 정체는 위 표의 23.5% 와 같은 것이다.
- **★최우선 «레벨» 체결률 81.2%.** v12 의 「37%」와 다른 건 틀린 게 아니라 **엔트리 대 레벨**
  이다. 최우선 뒤에서 맞은 18.8% 는 **최우선이 묵었을 때**다(그때 최우선 나이 중앙 120초,
  맞은 호가 23초). → §4-C 와 §8-2 가 여기서 나온다.
- **★불균형은 «누가 칠지» 를 조금 안다.** 비드 우세 → 사 감 59.5% · 오퍼 우세 39.1%,
  연도별로도 같은 자리(2024·25·26). ⚠하루 단위로는 비드-우세 체결 20건 이상인 183일 중
  **35.5%가 50% 아래**다. 화면 수는 «오늘이 전 이력과 같은 자리인가» 를 보는 것이다.
- **Hendershott 2025(JF)가 이 방에서도 선다.** 많이 내는 것과 **잘** 내는 것이 따로 기여한다
  (날짜 FE 회귀 0.138 · 0.155). → 리더보드 «귀속» 열. 국내 메신저 호가 미시구조는 문헌 공백이라
  이건 그 자체로 내놓을 수 있는 자리다.
- **R2b(불균형 → 15분 뒤 mid)는 화면에 안 올렸다.** 그룹내 +0.188(위약 −0.001)로 살아 있지만
  정체가 **호가 mid 의 되돌림**이라 부호가 R2 와 반대로 읽힌다. 거래 신호로 읽히면 안 된다.

### 라이브에 넣은 것

| 곳 | 내용 |
|---|---|
| `kbond_live.Book._book_ctx` | 체결 «순간» 의 책 — `eff`·`qs`·`ab`·`imb`·`amb`. 연구와 같은 규약 · 국고·통안만 |
| `kbond_live` 체결 처리 | 테이프·이벤트에 위 값 · 맞은 호가의 주인에게 `dealers[k]["f"] += 1` |
| `kbond_view.book_info` | 동향 탭 넉 줄. **책을 다시 접지 않는다**(엔진이 그때 잰 값을 모은다) |
| `kbond_view.curve_today` | 지표·차기 행에 «지금» 불균형 `imb` (화면은 ▲▼, ±0.15 넘을 때만) |
| `kbond_view.leaderboard` | `f` = 귀속 체결 |
| `kbond_schema` → `openapi.json` → `api-types.d.ts` | `Fill.eff/qs/ab/imb/amb` · `LeaderRow.f` · `CurveTodayRow.imb` · `BookInfo` · `View.book_info` |
| `verify_v4` **[I3][I4][I5]** | 뷰=스냅샷 재계산 · Σ딜러 f ≤ 귀속 수 · **최우선이면 「유효=호가/2」 항등식** |
| `shot_screen.mjs` | 새 화면 한 탭을 찍는다(표현 계층은 verify 도 지문 대조도 안 덮는다) |
| `kbond-web` `Trends.tsx` | 맥박 카드 아래 넉 줄 · 리더보드 «귀속» 열 · 커브 오늘 ▲▼ |

### ★화면의 불균형은 «종목을 문면에 적은 체결» 만 센다

라이브 귀속이 배치보다 넓다 — 맨 «ㅎㅈ» 를 그 브로커의 직전 호가에 붙이는 길(`csrc=prev`)이
있고 09-03 재생에선 귀속 108건 중 67건이 그것이다. **배치 체결표에 `BondCode` 가 없어 연구가
한 번도 재 본 적 없는 모집단**이라 뺐다. 빼는 이유는 데이터가 아니라 설계다(§3 둘째).
뺀 수는 화면에 괄호로 같이 보인다(`n_imb_prev`).

---

## §3. 새 규율 셋 — 오늘 값을 치른 것

▎**같은 축을 두 표에서 쓸 때는 «같은 함수» 를 지나게 하라.** 체결표에는 `BrokerKey` 가 없어
서명 앞부분으로 키를 지었더니 본표와 **겹침 0%** 였고 «딜러+레벨» 귀속이 통째로 0 이 됐다.
`parse_kbond_logs.broker_key` 를 지나게 하니 99.8%. 「0%」라는 못 믿을 숫자가 아니었으면
그냥 지나갔을 것이다.

▎**★★★귀속 규칙과 설명 변수가 축을 공유하면 그 상관은 검정이 아니다.** 오늘 두 번 나왔다.
①**잠금 동점** — 락에서 보고자 자신의 호가가 없으면 귀속은 «최신 것» 을 고르는데, 최신은
사람이 많은 쪽일 확률이 높다. 안 빼면 부호가 **통째로 뒤집힌다**(오퍼 우세 → 사 감 67%).
②**맨 «ㅎㅈ» 귀속** — «그 딜러의 가장 최근 호가» 로 방향을 정하는데, 누가 최근에 냈는가는
불균형이 재는 것과 같은 것이다. → 라이브도 `amb` 로 동점을 비우고, 화면은 `stated` 만 센다.

▎**위약이 0 이 아니면 추정량부터 의심하라.** R2b 위약이 −0.108 이었다. 그룹 «간» 성분(종목-일
평균끼리의 상관)이 샌 것이었고, 종목-일 «안에서» 순위를 매기니 위약이 −0.001 로 앉았다.

▎(09-11 것 계속) **완료 줄이 없는 로그는 실패한 로그다** · **판정을 고쳤으면 그 판정을 쓰는
자리를 전부 세라.**

---

## §4. 오너 판정 대기 — 넷

### [A] CD·CP 「3.72/73 팔자」의 뜻 (09-10 부터 그대로)

09-10 §5 를 그대로 읽어라. 전제 셋이 이미 실측으로 깨졌다(양방 아님 SELL 18,917 대 BUY 15 ·
범위 아님 내림 1,167 · 어두운 행 아님). 판정은 「어두운 행을 여는 것」이 아니라 **이미 책에
올라간 18,142행의 값이 맞나** 다. 뜻을 받기 전에는 짝 19,751행을 손대지 않는다.

### [B] 프로덕션에 오늘 반영할 것인가 — 안 했다

**장중이라 백엔드를 재기동하지 않았다**(레인 규약 「장중이면 오너 확인 후」). 지금 상태:

| | |
|---|---|
| 코드 | 디스크에 있고 커밋·푸시까지 끝났다 |
| `:8301` 백엔드 | **옛 코드가 돈다** — `book_info` 를 안 낸다 |
| Funnel `/kbond/app` | **새 판이 이미 서빙된다**(out-kbond 를 다시 구웠고 정적이라 재기동 불필요) |
| 그래서 보이는 것 | 새 칸 넷은 **스스로 숨는다** · 「귀속」 열 빈칸 · ▲▼ 없음 (망가진 게 아니다) |
| Vercel `kbond-web.vercel.app` | **배포 안 했다** — 화면만 새로 가면 볼 것이 없다 |

**내일 아침 자동으로 맞춰진다**(08:00 `KBondAppBuild` → 08:20 `KBondLive`).
오늘 보려면 장 끝난 뒤 `powershell -NoProfile -File restart_kbond.ps1` 하나.
Vercel 은 그 뒤 `cd ..\kbond-web && vercel deploy --prod`(CLI 로그인 되어 있다 — `wwoo1116-6173`).

### [C] ★락에서 uncross 가 «체결되는 쪽» 을 지운다 — 고칠 것인가 (새것)

**실측**: 귀속 체결 38,746건 중 **23.5%가 uncross 가 걷어낸 호가에서 났다.** 그 레벨이 화면에서
통째로 사라지는 경우는 **0.2%뿐**이고, 나머지는 **그 레벨이 반대편 한 쪽만 보인다.** 즉 거의
전부 **락**(간격 0)이고, 화면은 「3.710 사자」를 보여 주는데 실제로 일어난 일은 「3.710 팔자가
맞았다」이다. 걷어낸 호가의 나이 중앙은 26초로 스테일해서 지운 것도 아니다(남은 쪽 11초).

**왜 판정이 필요한가.** uncross 는 v2.5 에서 오너가 「진짜 제대로 보고온거 맞아?」로 눌러서
들어온 규칙이고, 근거는 코인베이스 책의 «단조성·크로스 0» 불변식이었다. 그건 **체결 엔진이
강제하는** 불변식이다. 메신저 시장에는 그 엔진이 없어서 **락이 진짜 상태로 오래 선다**
(v8 실측 하루 907건 = 국고 호가의 40%). 규칙을 세운 근거가 이 시장에 없다는 증거가 오늘 나왔다.

**제안**: 간격이 «있는» 진짜 크로스는 지금처럼 걷고, **간격 0 인 락은 양면을 다 세우고 칸을
«락» 으로 표시한다.** 오너의 「락은 이벤트가 아니다」 판정(09-03)은 이벤트 피드 이야기이지
사다리 표시가 아니다. ⚠불변식 «오퍼 금리 < 비드 금리» 를 손대는 일이라 `verify [C]` 도 같이
고쳐야 한다(락은 위반이 아니라고 말하게). **판정 없이는 손대지 않는다.**

### [D] 레인 방향 — §1 이 맞나

「알파 소스 아님 · 값 진실 층」이 오너 읽기와 같은지. 아니라고 하면 §8 순서가 바뀐다.

---

## §5. 지금 상태

| | |
|---|---|
| 코드 | `Projects\apps\kbond` HEAD **`067b779`** → `origin/kbond-backend` 푸시됨 |
| 화면 | `Projects\apps\kbond-web` HEAD **`9ac1326`**(원격 없음) · `out`(Vercel판)·`out-kbond`(Funnel판) 둘 다 새로 구움 |
| 본표 | `kbond_structured_data.parquet` **8,338,580행 · 63열** |
| 다리 | `kbond_legs.parquet` 515,707행 · 51열 |
| 체결 | `kbond_fills.parquet` 86,228행 · 33열 |
| 연구 산출 | `kbond_dyn_study.json` · `kbond_dyn_study_fills.parquet` (오늘 새로) |
| 예약 | `KBondDailyUpdate` 평일 **15:50 + 08:05** · `KBondLive` **08:20** · `KBondAppBuild` **08:00** |
| 주소 | Funnel `/kbond`=옛 화면 · `/kbond/app`=React · 로컬 `127.0.0.1:8301/app/` · Vercel `kbond-web.vercel.app` |

⚠`main` 과 `kbond-backend` 는 **공통 조상이 없다**. 머지 금지.

---

## §6. 재현 — 검증 배터리 통째

```bash
cd C:\Users\infomax\Projects\apps\kbond

# 1) 동결 리플레이 (정본 검증일 20260903 · ★--at 안 걸면 동결이 아니다)
set KBOND_REPLAY=20260903&& set KBOND_AT=15:30:00&& ^
  python -X utf8 -m uvicorn kbond_api:app --host 127.0.0.1 --port 8303
#    ★기동은 그날 로그를 통째로 되감는 일이라 60~95초 걸린다. 그 전에 죽이지 마라.

# 2) 검증 셋
python verify_v4.py --port 8303 --at 15:30:00 --day 20260903     # [A]~[I5]
python gate_meaning.py                                            # [J][K][L]
python -m pytest -q test_frac.py test_cors_token.py               # 55
python test_issuer.py                                             # 138/138

# 3) 화면 — 지문 대조 + 눈으로 한 장
cd ..\kbond-web && npm run build && python -m http.server 3400 --directory out
chrome --headless=new --remote-debugging-port=9222 --user-data-dir=%TEMP%\kbchrome about:blank
cd ..\kbond
node compare_screens.mjs http://127.0.0.1:8303/ http://127.0.0.1:3400/ http://127.0.0.1:8303
node shot_screen.mjs   http://127.0.0.1:3400/ http://127.0.0.1:8303 dyn.png 동향

# 4) 연구 다시 (3분 · 적재 4s · 접기 96s)
python kbond_dyn_study.py [--since 2024-02-14] [--perm 20]

# 5) 프로덕션 (장중이면 오너 확인 후)
powershell -NoProfile -File restart_kbond.ps1
```

**09-15 결과**: verify_v4 [A]~[I5] 전건 통과 · gate_meaning 전건 통과 · pytest 55 ·
test_issuer 138/138 · compare_screens 다섯 영역 **0건** · 동향 탭 스크린샷 확인.
그날 값 = 귀속 104 · 유효 0.25 = 호가 0.25 · 최우선 82.5%(n=97) · 불균형 21건(+50 은 맨 ㅎㅈ).

---

## §7. 배관 함정 — 데인 것만 (전부 아직 유효)

1. **`Stop-ScheduledTask` 는 파이썬을 안 죽인다**(태스크가 `conhost --headless` 래퍼를 본다).
   포트 주인을 직접 죽여야 한다 → `restart_kbond.ps1`.
2. **기동 되감기 60~95초**(장이 갈수록 길어져 14:43 에 5분). 안 뜬다고 다시 죽이지 마라.
3. **이 PC 의 Miniconda 파이썬은 Funnel 인증서를 거절한다**(CA 묶음이 새 ISRG 체인을 모른다).
   파이썬으로 Funnel 을 부르지 말고 로컬(`127.0.0.1:8301`)이나 `curl` 을 써라.
4. **재파싱 중에는 사전·코드를 고치지 마라**(워커가 새 코드를 집는다). 원장 동시 읽기도 막힌다.
5. **`kbond_sector.json` 을 안 구우면 유동화가 조용히 회사채로 접힌다.**
   새 클론 순서: `kbond_issuer_dict` → `_derive` → `_near` → `kbond_sector`.
6. **지문 대조는 동결로만.** 장중 실황은 두 화면 폴링 주기가 달라 0건이 안 나온다(시차다).
7. **Next 의 `basePath` 는 CSS 안 `url(/fonts/...)` 를 안 고친다** → `fix_font_prefix.py`.
   접두어 빌드는 `build_app.ps1` 이 둘 다 굽는다.
8. **화면 HTML 은 `no-store`**, 자산은 해시가 박혀 오래 캐시한다. 열려 있던 탭은 한 번 Ctrl+Shift+R.
9. **문자열 치환에서 역슬래시가 접힌다**(T25, 두 번 재발). `replace` 마다 `assert`.
10. **윈도우 경로를 문서에 스크립트로 쓰면 `\a` 가 BEL 로 들어간다.** 슬래시를 쓰거나 되읽어 검사.

---

## §8. 다음 후보 — 값 순서 ([D] 가 「맞다」일 때)

### 1. ★[C] 락 수리 — 판정 받으면 제일 먼저, 반나절

체결의 **23.5%가 옳은 쪽에 그려진다.** 할 일: `uncross`/`uncross_best` 를 간격 0 과 간격 있음으로
가르고, 사다리 칸에 «락» 표시를 넣고, `verify [C]` 를 「락은 위반이 아니다」로 고치고, 전 이력으로
「그 레벨의 어느 쪽이 맞았나」를 다시 센다(`kbond_dyn_study.py` 에 칸 하나 더).
⚠**불변식을 건드리는 일이다** — 통째 재작성이 이 불변식을 두 번 뒤집은 전례가 있다(v3·v2.5).

### 2. 나이 밴드 재보정 — 한 시간

사람들이 **120초** 된 최우선을 뚫고 지나가는데 화면은 **300초**까지 «신선» 으로 칠한다.
`AGE_STEPS = (300, 900)` 의 근거는 v8 의 «레벨이 바뀔 확률» 이고, 오늘 것은 «남이 뚫고 가는 시각»
이라 다른 축이다. 둘 중 무엇을 그릴지 정하고 고친다.

### 3. ★★값 진실 층의 첫 걸음 — 「민평이 맞나 우리 책이 맞나」 — 하루

§1 방향이면 이게 가장 값나간다. **재료는 이미 다 있다.**
체결 39,889건마다 `|체결 − 우리 mid|` 대 `|체결 − 전일 민평|` 을 재면 된다(둘 다 표에 있다).
- 우리가 이기면: 이 책이 마킹·민평 대비·RV 가 기댈 층이라는 **증명**이 선다. 그날로 히트맵의
  뜻이 「말 대비」에서 「값 대비」로 바뀐다.
- 민평이 이기면: 화면은 여전히 «지금» 을 말하지만 «값» 은 아니라는 뜻이라 §1 을 접어야 한다.
⚠**정직하게 지게 만들 것** — 우리 mid 는 체결 시각의 책에서, 민평은 그날 전일 민평에서 온다.
시각축이 다르므로 «둘 다 체결 직전 정보만» 쓰게 맞추고, 종목-일 고정효과로도 한 번 본다.

### 4. 크레딧 책의 동학 — 며칠

오늘 것은 전부 국고다. 크레딧은 매도 일변도(99.9%)라 양면 불균형이 없다. 물어야 할 것은
«수요 바구니 ↔ 매도 매칭» 이고 재료(`_match`)는 서 있다. 물음의 꼴: 「바구니가 뜨면 그 종목
오퍼가 며칠 안에 팔리나」.

### 5. 딜러 품질 축 확장 — 반나절

지금 품질은 «게시 순간 최우선 여부» 하나다. 나이·양면 여부·재게시 빈도를 넣으면 R3 가
「어떤 호가가 흐름을 가져오나」로 좁혀지고, 리더보드가 «누구를 부를까» 에 더 곧게 답한다.

### 6. [B1] 판정 대기열 2회차 — 제일 싸다

`python kbond_issuer_todo.py 30`. 2,746종·43,643행이고 상위 12종이 16.8%.
남은 머리는 **접을 것이 별로 없다**(사모 SPC·이미 맞는 이름). 09-10 §6 그대로.

### 7. 지문 대조를 아침에 자동으로

동결 리플레이로 매일 한 번 돌려 0건이 아니면 로그에 남긴다. 「한쪽만 고쳐진 판정」을 다음엔
사람이 아니라 기계가 잡는다.

---

## §9. 원리적으로 못 하는 것 (건드리지 말 것)

- **범주 콜 약 84,000행** — 발행체가 문면에 없다. `SectorCat`·`TenorLo` 로 잡혀 있다.
- **사모 유동화 SPC ~15,000행** — 채권 원장에 없다.
- **계열 없는 39.9만행** — `ㅎㅈ`·`체결` 같은 내용 없는 확인.
- **이름이 이미 맞는 것 ~5,000행** — 효성티앤씨·고려아연. raw 가 정직하다.
- **크레딧 양면 사다리** — 종목 찍고 레벨 있는 QUOTE 가 BUY 0.1%다. 그런 시장이 아니다.
- **취소 관측** — 명시적 철회가 21일에 10행뿐이다. 삭제는 TTL 뿐이다(v8).

---

## §10. 오늘 커밋

```
kbond (origin/kbond-backend · 푸시함)
  067b779  다음 세션 인계문 — 09-15 것 한 장으로
  f9e8943  책이 말하는 것 — 체결 귀속이 열어 준 지표 셋 [OWNER 「K-Orderbook+++」]

kbond-web (원격 없음)
  9ac1326  책이 말하는 것 — 동향 탭 넉 줄 + 리더보드 «귀속» 열
```

`main` 은 안 건드렸다. 프로덕션 재기동·Vercel 배포는 §4-[B] 대로 안 했다.
