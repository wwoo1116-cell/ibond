# 체결 귀속 — 책의 «삭제 이벤트» 를 데이터로 정의한다 (2026-09-07)

[OWNER 2026-09-07] 「K-Orderbook++」 → §11 미결 목록 중 **체결 귀속** 지정.

§11 은 Coinbase level2 를 우리 데이터로 옮기면서 딱 한 칸을 비워 두었다.

| Coinbase | 우리 | 상태 |
|---|---|---|
| 키 `(side, price)` | `(딜러, 방향, 종목)` | 구현됨 (`kbond_live.py:1288`) |
| `new_quantity` 절대값 교체 | 같은 키의 재게시 | 구현됨 |
| **`new_quantity = 0` (명시적 삭제)** | **없다** | **이 문서** |
| `sequence_num` | Timestamp | 약하게 있음 |

지금 체결은 **테이프에만** 적힌다 — `kfills[code] = {t, y}` 로 «당일 마지막 체결»
하나만 남기고(`kbond_live.py:987`), 책은 건드리지 않는다. 즉 **체결된 호가도 TTL
(국고 30분)이 다할 때까지 화면에 계속 서 있다.**

---

## 0. 먼저 — 두 번 데인 시각축

결과를 적기 전에 이것부터. 첫 두 판은 숫자가 나왔고 그럴듯했지만 둘 다 틀렸다.

1. **`pd.Timestamp(naive).timestamp()` 는 KST 로 읽고 `.astype(int64)` 는 UTC 로
   읽는다.** 9시간이 어긋나 위약군이 통째로 «책없음» 이 됐다.
2. **parquet 이 `datetime64[us]` 로 온다**(pandas 3.0 은 해상도를 보존한다).
   `.astype("int64") / 1e9` 는 마이크로초를 초로 착각해 **모든 시각이 1000배 작아졌고**,
   그래서 **TTL 1800초가 실제로는 20.8일로 작동**했다. 「TTL 안의 책」을 잰다고
   적어 놓고 실은 「그날 전체」를 재고 있었다.

둘 다 «위약군이 0» 이라는 **못 믿을 숫자**가 아니었으면 못 잡았을 것이다.
3판부터는 초를 만드는 곳을 `secs()` 하나로 몰고, **체결 시각의 분위가 장중인지**
`assert` 로 막았다.

    def secs(s):
        return s.astype("datetime64[ns]").astype("int64").to_numpy() / 1e9
    assert 8*3600 < np.median(f.ts - secs(f.Date)) < 20*3600

---

## 1. 귀속은 실재한다 — 위약 대조

책 규약 그대로 접는다: 키 `(딜러, 방향, 종목)` 절대값 교체 · TTL 국고 30분.
체결 시점의 책에 «그 딜러가 그 레벨에» 서 있었나를 센다.
위약 = 같은 종목·같은 날의 «다른 체결 시각»(장중 활동 분포를 물려받는 대조군).

국고 체결 49,294건 · 체결시점 책 크기 중앙 18엔트리

| 판정 | 실제 | 위약 |
|---|---:|---:|
| **딜러+레벨** (그 딜러가 그 레벨에 서 있었다) | **66.0%** | 25.1% |
| 레벨만 (남이 그 레벨에 서 있었다) | 30.9% | 36.1% |
| 딜러만 (레벨은 다름) | 1.2% | 4.8% |
| 불일치 | 1.1% | 17.1% |
| 책없음 | 0.3% | 16.9% |

## 2. 양성대조군 — 안 쓴 축으로 검정한다

귀속은 **(딜러, 종목, 레벨) 셋만** 쓴다. **방향은 안 쓴다.**
그런데 체결 문면에는 파서가 따로 읽은 `Position` 이 있다. 귀속이 진짜면 둘이 맞아야 한다.

**일치 99.2%** (n=24,778 · 무작위면 50%)

| FillKind | n | 일치 |
|---|---:|---:|
| `ack_with_quote` | 22,550 | 99.3% |
| `reported` | 766 | 99.5% |
| `reported_then_quote` | 1,462 | 97.9% |

양방향이 같은 레벨에 다 서 있어 애매한 경우는 1.6% 뿐이다.

## 3. ★그래서 체결이 오퍼를 지우나 — **아니다**

§11 이 비워 둔 칸이 이것이다. Coinbase 의 `new_quantity=0` 을 무엇으로 정의할지.
가장 그럴듯한 후보가 «체결» 이었다. **데이터는 반대로 말한다.**

처치 = 체결이 맞은 호가 · 대조 = 같은 순간·같은 종목·같은 방향에 서 있던
«레벨이 다른 남의 호가». 처치군 호가가 훨씬 신선해서(나이 중앙 23초 대 460초)
**나이로 층화**했고, 체결 보고문 자신이 «다음 호가» 로 재계상되지 않게 뺐다.

| 호가 나이 | 처치 n | 재게시% | **같은레벨 \| 재게시%** | 대조 n | 재게시% | **같은레벨 \| 재게시%** |
|---|---:|---:|---:|---:|---:|---:|
| 0~60초 | 23,451 | 53.7% | **85.0%** | 14,077 | 69.6% | 65.4% |
| 60~300초 | 7,289 | 44.5% | **79.7%** | 31,133 | 39.8% | 38.0% |
| 300~900초 | 1,431 | 37.0% | **75.0%** | 44,177 | 21.7% | 26.0% |
| 900~1800초 | 356 | 32.9% | **70.1%** | 31,860 | 13.5% | 23.0% |
| **전체** | 32,527 | 50.7% | **83.6%** | 121,272 | 29.7% | 40.5% |

**모든 나이 구간에서, 체결이 맞은 호가는 딜러가 다시 말할 때 «같은 레벨로 되세운다».**
크레딧도 같은 방향이다(같은 레벨 58.7% 대 대조 13.8% · 단 n=791, 아래 한계).

기전은 문면이 말한다 — 국고 체결의 4.1%, **크레딧 체결의 71.9%가
「거래후 추가 팔자」 꼴**이다. 장외 호가는 **수량이 정해진 주문이 아니라 레벨을
내건 상시 표시**다. 한 번 붙었다고 그 레벨이 사라지지 않는다.

> **판정: 체결은 삭제 이벤트가 아니다. 지우면 있는 시장을 지운다.**
> §11 의 택일 (a)「교체+체결로 지우고 TTL 없음」은 **틀렸다**. 남는 것은
> (b)「교체 + TTL」이고, 그건 이미 서 있는 규약이다. **체결은 «확인» 으로 쓴다.**

## 4. 그래도 남는 값 — 화면은 9분을 침묵한다

귀속된 체결의 **37.0%** 는 그 순간 **최우선** 호가를 맞혔다. 그 뒤 그 호가가
최우선 자리에 남는 시간(딜러가 다시 낼 때까지 · 남이 뺏을 때까지 · TTL 중 최단):

    중앙 552초(9.2분) · 평균 852초 · 49.2%가 10분 이상

지우지는 않되 **«이 레벨은 (n분 전) 실제로 체결됐다»** 를 보여 줘야 하는 이유다.
게다가 §3 이 말하듯 체결된 레벨은 **되세워질 확률이 두 배**라 — 화면에 선 오퍼
중 «진짜» 를 가리는 표시가 된다.

## 5. 부산물 — 장외에 없던 «공격 방향»

귀속된 호가가 오퍼(S)면 **누가 사 갔다**, 비드(B)면 **누가 팔았다**.
장외 대화록에는 없던 축이고, §2 가 99.2% 로 검증한 그 축이다.

    국고 전 이력  사 갔다 16,171 · 팔았다 16,356
    09-03 재생    사 갔다 40 · 팔았다 62

## 6. 무엇을 넣었나

| 곳 | 내용 |
|---|---|
| `kbond_live.py` `_attach_fill` | 종목이 «있는» 체결을 책의 엔트리에 붙인다(v8 은 종목 «없는» 것만 했다). 국고·통안·국주는 레벨 일치+같은 브로커 우선, 크레딧은 (브로커, 라벨). 30분 창 |
| `kbond_live.py` `_carry_hit` | 절대값 교체가 체결 표시를 지우지 않게 — **같은 레벨로 되세운 교체(83.6%)에만** 물려준다 |
| `kbond_live.py` `aggr` | 당일 공격 방향 집계. 테이프·이벤트에 `ag` |
| `kbond_view.py` `ObSide.hit/fhit` | 사다리 칸마다 «실제로 체결된 호가 수»와 마지막 체결 시각 |
| `kbond_view.py` `view().aggr` | `lane=dyn` 응답에 공격 방향 |
| `kbond_schema.py` → `openapi.json` → `api-types.d.ts` | 계약·타입 재생성 |
| `verify_v4.py` **[I]** | 공격 방향이 문면과 맞는지(안 쓴 축) + 서버 집계와 테이프 대사. 85% 미만이면 실패 |

**검증**: `verify_v4.py --port 8303 --at 15:30:00 --day 20260903` **[A]~[I] 전건 통과**.
[I] 는 재생에서 94.4%(n=89), 배치 전 이력에서 99.2%(n=24,778).

## 7. 한계 — 읽는 사람이 알아야 할 것

1. **크레딧 표본이 얇다.** 크레딧 체결 중 `BondName` 이 붙는 건 4.6% 뿐이라
   R4 는 n=791/대조 290 이다. 나머지는 `issuer_guess` 라벨이라 배치에서 재현이 안 된다.
2. **크레딧 체결의 53.3%가 같은 문장의 재게시**(`RepostSeq>0`)다 — 사건이 아니라
   상시 호가다. R4 는 걷어냈지만 **라이브 `_attach_fill` 은 아직 안 걷어낸다**
   (같은 레벨에 다시 붙으므로 `fn` 만 부풀고 표시는 옳다).
3. **«레벨만» 30.9% 는 아직 안 쓴다.** 남의 호가가 맞은 경우인데 누가 공격자인지
   가릴 근거가 약하다 — 같은 브로커가 있으면 그쪽을 쓰고, 없을 때만 남을 쓴다.
4. 체결 **수량**은 책에서 안 뺀다. 「체결 후 잔존 N억」이 명시된 건 국고 0.0%·
   크레딧 3.4% 뿐이라 일반 규칙이 못 된다.
