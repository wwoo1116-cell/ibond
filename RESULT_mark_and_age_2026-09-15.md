# 민평이 맞나 우리 책이 맞나 · 나이 밴드 재보정 (2026-09-15 후반)

[OWNER 2026-09-15] 「so what?」 → 정리된 방향([D] 「일단 맞다고 가정하고 쭉 갑시다」):
**이 책은 알파 소스가 아니라 데스크의 «값 진실 층»** 이다. 그 방향이 서려면 먼저 이게
참이어야 한다 — **우리 책의 mid 가 관행 마크보다 «거래가 나는 자리» 를 잘 말한다.**
아니면 화면은 «지금» 을 말할 뿐 «값» 은 아니고 방향을 접어야 한다.

스크립트 `kbond_mark_test.py`(1분) · 수치 `data\kbond\kbond_mark_test.json` ·
나이 밴드 `data\kbond\kbond_age_bands.json`.
표본 = 국고 체결 **40,011건**(2024-02-14~2026-09-14, `RepostSeq=0`·`DupSeq=0`).

---

## 0. 먼저 — 민평은 종가 평가다 (본표 열 하나가 선견이다)

`국고통_민평` 의 최대 일자는 늘 **전 영업일**이고(오늘 09-15 14시에 최대가 09-14),
라이브 엔진의 `mp_date` 도 전일이다. 즉 **일자 D 로 찍힌 민평은 D 종가의 평가**이고
D 장중에는 존재하지 않는다.

그런데 배치 `enrich_kbond_quotes.load_mp` 는 **같은 날짜로 조인**한다. 그래서
**본표의 `MPYieldDB`·`QuoteVsMP_bp` 는 그날 종가에 붙은 값**이다.

- 서술에는 맞다 — 「그 호가가 그날 평가 대비 어디였나」.
- **예측·장중 문맥에서는 선견이다.** 이 열로 장중 신호를 만들면 그날 종가를 미리 보는 셈이다.
- 라이브 화면은 안전하다(전일 민평을 쓴다). 위험한 곳은 **본표를 쓰는 연구**다.

그래서 이 검정의 상대는 **`민평_전일`**(전 영업일 민평, 라이브가 쓰는 바로 그 값)로 두고,
`민평_당일` 은 **선견 상한 참고**로만 같이 찍었다.

## 1. 검정 설계 — 급소는 h 다

과녁 = 체결 금리. 같은 순간 각 마크가 얼마나 틀렸나(bp).

| 경쟁자 | 그때 손에 있나 | 커버리지 |
|---|---|---:|
| 우리 mid, 체결 **h 분 전** 책 | 있다 | h=0 95% · 15분 53% · 60분 28% |
| `민평_전일` | 있다 | 100% |
| `직전체결`(같은 종목 그날 직전) | 있다 | 66% |
| `민평_당일` | **없다 — 선견** | 100% |
| `민평_전일 + 그날 지표물 이동` | 있다 | 31% |

**h=0 은 기울어져 있다.** 체결의 81%가 최우선 «레벨» 에서 나고 책은 한 틱이라
`|체결 − mid|` 가 구성상 0.25bp 로 나온다. 마크가 좋아서가 아니라 **과녁이 책 안에 있어서**다.
같은 병(귀속 규칙과 설명 변수가 축을 공유)이 오늘 오전에 부호를 뒤집은 전례가 있다.
그래서 **h=15·60분이 본판**이고, h=0 은 「기울어진 칸」이라 이름을 달아 둔다.

## 2. 결과 — 전체

|체결 − 마크| bp

| 마크 | n | 중앙 | 평균 | p90 |
|---|---:|---:|---:|---:|
| 민평_전일 | 40,005 | **2.20** | 2.86 | 5.80 |
| 민평_당일 **(선견)** | 40,009 | 0.30 | 0.68 | 1.80 |
| 직전체결 | 26,514 | 0.00 | 0.27 | 0.50 |
| 우리 mid −0분 (기울어짐) | 38,013 | 0.25 | 0.35 | 0.25 |
| **우리 mid −15분** | 21,335 | **0.25** | 0.54 | 1.00 |
| 우리 mid −60분 | 11,317 | 0.75 | 1.01 | 2.25 |

짝 비교(둘 다 있는 체결만):

| | n | 민평 중앙 | 우리 중앙 | 우리가 이김 |
|---|---:|---:|---:|---:|
| −0분(기울어짐) | 38,010 | 2.20 | 0.25 | 92.9% |
| **−15분** | 21,332 | 2.20 | **0.25** | **89.1%** |
| −60분 | 11,317 | 2.30 | 0.75 | 81.2% |

연물별로도 시간대별로도 같다(연물 84~91% · 시간대 87~90%). 날짜별로 보면 체결 10건 이상인
**530일 중 99%의 날에 우리가 이긴다**(하루 안 승률 중앙 93%). 한 날이 끌고 간 결과가 아니다.

## 3. ★그런데 이 승리의 대부분은 «오늘 대 어제» 다

민평_전일은 **어제 종가**다. `|체결 − 민평_전일|` 은 사실상 하룻밤+장중 이동이고 국고에서
그게 2bp 언저리다. 그러니 **오늘 정보를 쓰는 어떤 마크든 이긴다.** 이 표만으로 「우리 추정이
더 낫다」고 말하면 과장이다.

두 가지가 그걸 말해 준다.

- **민평_당일(선견)은 체결과 0.30bp 다.** 민평은 **종가 마크로는 훌륭하다** — 한 틱 안이다.
  문제는 정확도가 아니라 **하루 묵었다**는 것이다.
- 그래서 진짜 물음은 **«그 종목 호가가 커브 이동 너머로 무엇을 더 아는가»** 다.

### 더 센 상대 — 전일 민평 + 그날 그 연물 지표물의 이동

지표물의 우리-책 mid 가 전일 민평에서 얼마나 움직였는지를 그 종목 전일 민평에 얹는다
(지표물 자신의 체결은 뺀다 — 순환이다). n=12,308.

| | 중앙 | 평균 |
|---|---:|---:|
| 민평_전일 + 커브이동 | 0.55 | 0.77 |
| **우리 mid −15분** | **0.25** | **0.51** |

우리가 이김 **53.8%** · 동률 16.8% · 짐 29.4% → **결판난 것 중 64.7%**.

**판정: 책은 커브 너머로 종목 고유의 것을 안다. 다만 크기는 0.3bp다.**
한 틱이 0.5bp 인 시장에서 반 틱이 안 되는 차이다. RV 레인이 잰 비용 경계(0.713bp)보다 작다 —
**마크가 좋아졌다고 RV 손익이 따라 좋아지지는 않는다.** 그건 따로 검정할 일이다.

### 직전체결은 상대가 못 된다

중앙 0.00bp 로 제일 좋아 보이지만 **직전 체결과의 시차 중앙이 63초이고 49%가 60초 이내**다.
같은 거래의 재보고가 섞여 있다는 뜻이다(`RepostSeq`·`DupSeq` 로 걸러도 남는다 — 두 딜러가
같은 거래를 각자 보고한다). **이 수를 「직전 체결이 최고의 마크」로 읽으면 안 된다.**

## 4. 그래서 [D] 는 서는가 — 선다, 이렇게 좁혀서

**장중 마킹에서 이 책은 손에 잡히는 유일한 대안(어제 민평)보다 2bp 낫고, 만들 수 있는 최선의
대용(민평+커브이동)보다 0.3bp 낫다. 단, 양면 책이 있을 때만이고 15분 기준 체결의 53%다.**

- 민평을 깎아내리는 결과가 아니다. 민평은 **종가에서 한 틱 안**이다.
- 이 층의 값은 «더 똑똑한 추정» 이 아니라 **«하루 묵은 마크를 분 단위로 바꾸는 것»** 이다.
- 커버리지가 이 층의 진짜 한계다. **양면이 없으면 우리는 할 말이 없다.**

## 5. 나이 밴드 재보정 — 벼랑은 5분이 아니라 1분이다

`kbond_dyn_study_fills.parquet` 에서 바로 나온다. 최우선 호가의 «나이» 별로, 그때 난 체결이
**그 최우선 레벨에서** 났나:

| 최우선 나이 | n | 그 레벨에서 체결 |
|---|---:|---:|
| ~1분 | 27,973 | **90.6%** |
| 1~5분 | 4,709 | 61.2% |
| 5~15분 | 2,018 | 29.6% |
| 15~30분 | 953 | 16.7% |

옛 문턱 `AGE_STEPS = (300, 900)`(신선 <5분)은 **한 칸에 90.6% 와 61.2% 를 같이 담고 있었다** —
「이게 시장이다」와 「반반이다」가 같은 색이었다. 근거였던 v8 수치는 «레벨이 바뀔 확률» 이라
축이 다르다(안 바뀌어도 뚫린다).

→ **`AGE_STEPS = (60, 300)`**. 신선 <1분(91%) · 익음 1~5분(61%) · 노후 5분+(30% 아래).

### 딸려 나온 것 둘

▎**화면이 문턱을 손으로 복제하고 있었다.** 옛 화면은 서버 `age_steps` 를 읽는데
**React 화면은 `300/900` 을 코드에 박아** 두었다. 서버만 고쳤으면 두 화면이 다른 색을
말했을 것이고 **지문 대조는 색을 안 본다.** 09-11 의 「판정을 고쳤으면 그 판정을 쓰는 자리를
전부 세라」가 그대로 재발한 자리다. → `View.age_steps` 를 신설해 **한 곳**에서만 정한다.

▎**바램 깊이도 문턱과 한 벌이다.** 옛 깊이(`.68`/`.42`)는 «15분 넘은 소수» 를 지우려고 고른
값이라, 문턱만 좁히면 책의 대부분이 가장 옅은 칸으로 떨어져 **순서도 글자도 같이 죽는다**
(실측: 26-7 의 딜러 13곳 중 11곳이 1분 초과). → `.82`/`.60` 으로 같이 맞췄다.
**바램의 일은 감추는 것이 아니라 신선도를 줄 세우는 것이다.**

## 6. 검증

동결 리플레이 20260903 15:30 — `verify_v4 [A]~[I5]` 전건 통과 · `gate_meaning` 전건 통과 ·
pytest 55 · `test_issuer` 138/138 · 지문 대조 다섯 영역 **0건** ·
`/api/view` 가 `age_steps [60, 300]` 을 내는 것과 26-7 양면 사다리를 **눈으로** 확인
(`shot_screen.mjs` 에 «종목 고르기» 인자를 더했다 — 안 고르면 한쪽 호가만 있는 종목이 잡혀
바램이 안 보이는 그림을 찍는다).

## 7. 한계

1. **커버리지가 결론을 좁힌다.** h=15분에서 53%. 나머지 절반에서 이 층은 할 말이 없다.
2. **체결 보고 시각 ≠ 체결 시각.** 보고가 15~16시에 몰린다(`qs_by_hour`). 「체결 순간의 책」은
   엄밀히는 「보고 순간의 책」이다.
3. **커브이동 상대도 우리 책을 쓴다**(지표물 mid). 우리 책이 아예 없으면 그 상대조차 못 만든다 —
   그러니 §3 의 0.3bp 는 «책 대 책» 이지 «책 대 민평만» 이 아니다.
4. 국고만이다. 크레딧은 매도 일변도라 mid 자체가 드물다.
5. 나이 밴드는 **국고 최우선**에서 잰 것이다. 통안·크레딧에 같은 문턱을 쓰는 것은 가정이다.
