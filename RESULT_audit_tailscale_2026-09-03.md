# Tailscale 공개 전 감사 (2026-09-03)

[OWNER] 「이후 최종적으로 감사하고 Tailscale로 보낼 예정」

결론 먼저: **지금 상태로 밖에 열면 안 된다.** 막는 것은 기술이 아니라 셋이다 —
인증이 없고, 딜러 이름·전화가 그대로 나가고, **이 PC 의 Funnel 이 이미 인터넷에 열려 있다.**
전송량 문제는 이번에 고쳤다(gzip).

---

## 1. 이 PC 의 Tailscale 은 이미 «인터넷» 에 열려 있다

```
# Funnel on:
#     - https://e110430.tailc7b701.ts.net
#     - https://e110430.tailc7b701.ts.net:8443
https://e110430.tailc7b701.ts.net (Funnel on)
|-- /   proxy http://127.0.0.1:8100     <- braveworld v1
|-- /v2 proxy http://127.0.0.1:8200     <- sauron-v2
https://e110430.tailc7b701.ts.net:8443 (Funnel on)
|-- / proxy http://127.0.0.1:8200
```

**Funnel 은 테일넷 전용이 아니라 인터넷 전체 공개다.** 테일넷 IP 는 100.70.247.10.

- K-Bond(:8301)는 아직 붙어 있지 않다. 지금은 `127.0.0.1` 에만 바인딩돼 있다(확인함).
- 「Tailscale 로 보낸다」가 **내 기기끼리**면 `tailscale serve`(Funnel 아님) 또는
  `--host 100.70.247.10` 이고, **남에게 보여준다**면 Funnel 인데 그건 아래 2·3 을 먼저 풀어야 한다.
- ⚠ `tailscale funnel 8301` 한 줄이면 곧바로 인터넷 공개가 된다. 그 한 줄을 조심할 것.

## 2. 인증이 없다

서버는 `/`, `/book.json`, `/feed.json`, `/events`, `/health` 다섯 끝점을 인증 없이 준다.
로컬 전용이라 지금까지 문제가 아니었을 뿐이다. 주소에 닿는 사람은 누구나 전량을 받는다.

경로 처리는 안전하다 — 고정 경로 비교만 하고 파일 경로를 사용자 입력으로 만들지 않는다
(`VIEWER.read_bytes()` 는 상수). 경로 순회는 불가능하다.

## 3. 무엇이 나가는가 (실측, 09-03 14:28 스냅샷)

| 항목 | 값 |
|---|---|
| 전화번호꼴 문자열 | **403개** (예: 02-2009-7048 · 02-3772-2084) |
| 데스크 표시명 | 90개 |
| 딜러 키(전화 국번+번호) | 111개 |
| `dealers` 배열 | 120곳 — 각 데스크의 오늘 활동 전량(건수·매도/매수·체결·최우선 점유) |
| 피드 `raw` | 원문 그대로 — 딜러 서명이 통째로 들어 있다 |

이건 «시세» 가 아니라 **누가 무엇을 얼마에 부르고 있는지의 명부**다. 두 방에서 우리가
받는 것을 그대로 재배포하는 셈이 되므로, 밖에 열 때는 사업적·계약적 판단이 먼저다.
기술로 줄이려면 딜러명을 가명(H-01 식)으로 바꾸는 스위치가 필요한데, 그러면
«누가 어디 서 있나» 화면의 값이 대부분 사라진다 — 오너 결정 사항.

## 4. 전송량 — 고쳤다

| | 전 | 후(gzip) |
|---|---|---|
| `/book.json` | 809 KB | **93 KB** |
| `/` (화면) | 103 KB | **36 KB** |
| `/feed.json?limit=3000` | 1,027 KB | ~129 KB |

SSE 는 책이 바뀔 때마다 스냅샷을 통째로 민다. 장중 ver 이 수백까지 오르므로 압축 없이
밖에 열면 하루 수백 MB 가 나간다. `Handler._send()` 에 gzip 을 넣었다(4KB 초과 + 클라이언트가
`Accept-Encoding: gzip` 을 보낼 때만). 배포·검증 완료.

**남은 전송량 과제**: 압축을 해도 갱신마다 93KB 다. 진짜 해법은 «전체 스냅샷 → 주제별 증분»
인데 프로토콜을 바꾸는 일이라 이번엔 하지 않았다.

## 5. 넣은 것

- `Handler._send()` — gzip + Vary. 세 끝점(`/`, `/book.json`, `/feed.json`)에 적용.
- 기동 로그 경고 — `--host` 가 로컬이 아니면 «인증 없음 · 딜러 이름·전화가 나간다 ·
  Funnel 에 붙이지 말 것» 을 남긴다.

## 6. 밖에 열기 전에 해야 할 것 (순서대로)

1. **범위 결정** — 내 기기끼리(테일넷)인가, 남에게 보여주는가(Funnel). 이게 나머지를 정한다.
2. 테일넷이면: `--host 100.70.247.10` 로 띄우고 끝. 인증은 테일넷 소속이 대신한다.
3. Funnel 이면 최소 셋이 필요하다.
   - 토큰 인증(쿼리 토큰이라도). 지금은 아무것도 없다.
   - 딜러명 가명화 스위치 여부 결정.
   - 접근 로그(누가 언제 받아 갔는지). 지금은 `log_message` 를 통째로 죽여 놨다.
4. 어느 쪽이든 **예약 태스크(KBondLive)의 인자를 바꿔야** 한다. 지금은 인자가 없어
   `127.0.0.1` 로만 뜬다.

## 7. 재현

```
tailscale serve status                      # Funnel 무엇이 열려 있나
tailscale ip -4                             # 테일넷 IP
curl -sH "Accept-Encoding: gzip" -o /dev/null -D - http://127.0.0.1:8301/book.json
python verify_v4.py --port 8301 --day 20260903
```
