# K-Bond 다음 세션 프롬프트 (2026-09-07 마감)

이 파일 한 장만 붙여 넣으면 이어서 할 수 있다. 근거는 같은 폴더의
`RESULT_fill_attribution_2026-09-07.md`(체결 귀속)와
`RESULT_level_coverage_2026-09-07.md`(레벨 커버리지 — 오늘의 본문)에 있다.

---

## 붙여 넣을 프롬프트

```
K-Bond 레인을 이어서 한다.
먼저 C:\Users\infomax\Projects\apps\kbond\PROMPT_next_2026-09-07.md 를 읽어라.
그다음 KBOND_LANE_PROMPT.md §1 «DECIDED» 와 §11(장외 Order Book Dynamics).

이 레인의 규율 셋 — 어기면 오늘 한 게 무너진다.
 1) 내가 지정하는 것만 한다. 임의로 분석을 시작하지 않는다.
 2) «뜻» 을 정할 때는 라벨 대신 «결과검정» 을 쓴다 — 뜻이 참이면 따라 나와야 할
    결과를 찾아, 기저와 견주고, 답을 아는 경우로 배관을 검증한다.
    채점기가 gate_meaning.py 다. 규칙을 고칠 때마다 돌려라.
 3) 고친 뒤에는 verify_v4.py [A]~[I] 와 gate_meaning.py 를 «둘 다» 통과시켜라.

[A] 대기 — CD·CP 「3.72/73 팔자」 양방 표기의 뜻. 오너가 «양방도 범위도 아니다» 라고만
    했다. 94,189행이 여기 걸려 있고 책 경로도 없다. 뜻을 받기 전에는 손대지 않는다.

[B] 다음 후보 셋 (판정 불필요, 크기 순)
    · 미해석 «계열 미상» 24,502행 — 대부분 CP/전단이다.
      [OWNER 2026-09-07] 「고려아연 A1이면 CP긴 한데」 — A1/A2 등급 표기가 CP 표지다.
    · 미해석 «방향 없음» 13,505행 — 종목·민평은 있는데 팔자/사자가 없다.
    · 산금 정본화 — 종별은 특은채로 맞는데 «산금채»와 «산금»이 다른 종목처럼 갈린다
      (크레딧에 «산금» 든 행 215,245). [OWNER] 산금채 = 산업금융채권 = 산업은행채권.
      _ALIAS_OFFICIAL 에 넣고, _SECTOR_RULES 특은채에 «산업금융» 을 더할 것
      (지금 «산업금융채권» 은 회사채로 잘못 잡힌다).

[C] 오늘 못 한 것 — 프로덕션 재기동이 권한 정책에 막혔다. 코드는 다 커밋돼 있고
    평일 08:20 태스크가 새 판을 띄운다. 09-08 아침에 «관심» 면과 묵은 민평 배지가
    실제로 뜨는지 눈으로 확인할 것.
```

---

## 1. 오늘 확정된 것 — 다시 열지 않는다

### ★★★체결은 «삭제» 가 아니라 «확인» 이다
§11 이 비워 둔 칸(Coinbase `new_quantity=0` 을 무엇으로 정의하나)의 답.
- 귀속은 실재한다: 체결 시점 책에 «그 딜러가 그 레벨에» **66.0%**(위약 25.1%)
- **양성대조군 99.2%**(n=24,778) — 귀속에 «방향» 을 안 쓰는데 문면 방향과 맞는다
- **그런데 지우면 틀린다.** 나이를 층화해도 모든 구간에서 맞은 호가는 딜러가 다시 말할 때
  «같은 레벨로 되세운다»(전체 83.6% 대 대조 40.5%). 장외 호가는 수량이 정해진 주문이
  아니라 «레벨을 내건 상시 표시» 다.
- 부산물 = **공격 방향(aggressor)**. 오퍼가 맞았으면 사 간 것, 비드면 판 것.
- 그래도 화면은 침묵한다: 귀속된 체결의 37.0%가 최우선을 맞혔고 그 뒤 최우선 자리에
  중앙 9.2분 남는다 → 그래서 «지우지 않고 표시» 한다(`ObSide.hit`).

### ★★★「민평에 팔자」는 진짜 민평에 팔자다 [OWNER]
- 결과검정: 직전 호가가 «레벨 없음» 일 때 뒤따르는 체결이 민평 ±1bp 에서 **92.0%**
  (기저 40.3%). 배관검증(값 있는 호가는 «그 값» 에서) **98.7%**.
- 09-03 규칙이 요구하던 `MPYield is None` 은 «국고 문형» 이었다.
  **문면에 민평이 적히면 MsgType 은 전 계열에서 언제나 QUOTE**(AXE∩문면민평 = 0건)라,
  민평을 늘 적는 크레딧은 규칙에 안 걸렸다. 그래서 268만 행이 책 밖에 있었다.

### 오너 판정 (2026-09-07)
| 물음 | 판정 |
|---|---|
| 민평만 적고 팔자 | **진짜 민평에 팔자**(0bp) |
| 국고·통안 맨 콜 27만행 | **«관심» 면으로 노출**, 책에는 안 올림 |
| 단위 없는 스프레드 「민+3」·「+10」 | **bp** |
| 「관심」 문구가 붙은 호가 7,705행 | **지금처럼 관심으로**(09-03 유지) |
| 통안 묵은 민평(최대 30일) | **유지하되 화면에 표시** |
| 체결 중복 3,255건 | **표시만**(DupSeq, 집계에서 안 뺌) |
| 죽은 방 «레드본드» | **안 넣는다**(D3 유지) |
| CD·CP 「3.72/73」 | **대기** — 양방도 범위도 아니다 |

---

## 2. 지금 상태

    원장 kbond_structured_data.parquet   8,241,772행 · 63열
    다리 kbond_legs.parquet                494,215행 · 51열
    체결 kbond_fills.parquet                85,141행 · 33열  (DupSeq 신설)
    커브 kbond_mkt_curve.parquet           188,496행  ← 오늘 새로 생김

**범주 콜 제외 레벨 커버리지 아침 52.4% → 93.4%**

| 계열 | 아침 | 지금 |
|---|---:|---:|
| 크레딧/기타 | 35.6% | **98.4%** |
| MBS | 29.4% | **98.4%** |
| 지방/첨가소화 | 27.6% | **95.7%** |
| 국고 | 85.7% | 90.1% |
| 국민주택 | 5.4% | **87.1%** |
| 통안 | 82.2% | 84.7% |
| 국고이자채 | 19.9% | 64.6% |
| CD · CP/전단 | 10.2 · 24.7% | 그대로 (양방 대기) |

커밋 — kbond `e55ff5e`→`79619d5`(6건) · kbond-web `74142fa`→`1b49d48`(3건) ·
ibond `6b941fd` **푸시+배포 완료**(`ibond-theta.vercel.app`).
⚠kbond·kbond-web 은 **원격이 없다**(로컬 커밋만). ibond 는 «빌드된 화면만» 들어가는
public 리포라 소스가 안 올라간다 — 빌드물에 토큰·딜러 실명·전화 0을 확인했다.

---

## 3. 새로 생긴 도구·자료

- **`gate_meaning.py`** — 뜻 채점기. 재파싱 뒤마다 돌린다.
  `[J]` 뜻 관문 92.0%(하한 80) · 배관 98.7%(하한 90) · `[K]` 무단위 민±N **0**(상한 500,
  오늘 bp 로 정해져 0 이 정상이 됐다) · `[L]` 계열별 커버리지
- **`mkt_curve_load.py` → `kbond_mkt_curve.parquet`** — 오너가 준
  `Documents\국고채권포함.xlsx`(시가평가 3사평균 «잔존 구간별» 커브 여섯 ×12구간 ×
  2016-01-04~2026-09-04). 실제로 쓰는 건 **국민주택1종** 하나.
  나머지 넷(국민주택2·3종·서울도시철도·지역개발채·국고채권)은 **붙을 데가 없다**(확인함) —
  보관만. 국고채권 커브는 나중에 리서치에서 국고 커브 대용으로 쓸 수 있다.
- **`verify_v4.py [I]`** — 체결 귀속의 공격 방향이 문면과 맞는지(귀속에 «안 쓴 축»).

### SQL 에서 새로 안 것
- **`kbond.marketvalue`** 는 isin×일자×민평을 18,000종목·2년치 갖고 있는데
  **국민주택만 2025-01-23~24 이틀치**만 적재돼 있다. §10 의 「민평 소스 부재」는
  틀린 진단이었다 — «없다» 가 아니라 «안 채웠다». 채우는 건 데이터 엔지니어 몫[OWNER].
- **`국고통_민평` 의 `KRC0` 65만행 · 317종목은 국고채 이자·원금 분리채(STRIPS)** 다.
  표 이름이 «국고통» 이라 두 계열뿐인 줄 알았는데 세 번째가 있었다.
- `시가평가(daily)`(DB_FI)에 **통안증권 커브**가 2013년부터 있다(아직 안 씀).
- `bond_eval_info` 는 날짜 없는 스냅샷(국민주택 140종). `국고통_장외/장내_시가` 에 국민주택 0행.

---

## 4. 아직 안 보이는 것 — 성격별

| 성격 | 크기 | 상태 |
|---|---:|---|
| 맨 콜(값이 아예 없음) | 국고 207,726 · 통안 59,461 · 국고이자채 10,034 | **자료로 못 고침** → «관심» 면 |
| 잡담·공지 | 182,924 | 미해석이 맞다 |
| **미해석 «계열 미상»** | **24,502** | 대부분 CP/전단. A1/A2 가 표지 |
| 미해석 «방향 없음» | 13,505 | 종목·민평은 있는데 팔자/사자가 없다 |
| CD·CP 양방 | 94,189 | **오너 판정 대기** |
| 국주 은어 | (해결) | 커브로 68,540행 확정 |
| 취소 미관측 | — | **영구.** TTL 가정이 유일한 대안 |
| 양방 한 줄 | 114 | 「민평 팔자 // 오바2 사자」 |

---

## 5. 오늘 데인 것 — 다음 사람은 밟지 말 것

1. **★★parquet 이 `datetime64[us]` 로 온다**(pandas 3.0 이 해상도를 보존).
   `.astype("int64")/1e9` 는 마이크로초를 초로 착각해 **모든 시각이 1000배 작아진다** —
   TTL 1800초가 20.8일로 작동했다. 초를 만드는 곳을 하나로 몰고 `assert` 로 막을 것.
2. **`pd.Timestamp(naive).timestamp()` 는 KST, `.astype(int64)` 는 UTC.** 9시간 어긋난다.
3. **★★줄 인덱스 치환이 다음 줄을 먹는다.** 3줄을 4줄로 바꾸며
   `"ttm": ttm, "cats": cats,` 를 덮어써 기동이 죽었다. **덮어쓸 «모든» 줄을 assert 할 것.**
4. **히어독 안에서 역슬래시가 접힌다.** 정규식·경로는 `chr(92)` 로 만들거나
   조각 파일에 써서 이어 붙일 것. 워러스(`:=`)는 컴프리헨션 iterable 에서 못 쓴다.
5. **★★남의 가드를 내 경로에 그대로 옮기지 말 것.** `~coupon` 을 at-MP 에 걸었더니
   143,233행이 죽었다 — 그 가드는 «문면 정규식으로 승격하는» 다른 경로 몫이고,
   at-MP 는 레벨을 민평에서 가져오니 쿠폰에 오염될 수 없다.
6. **★커브 끝단.** 국민주택 당월물은 잔존이 5.0을 살짝 넘어 보간이 «5~7년» 구간으로
   넘어가며 기준이 튄다(적중 55.2%). 5년에 물리면 97.8%. **뜻이 아니라 처리가 틀렸다.**
7. **★★재파싱은 원자적이 아니다.** MySQL 이 끊겨 enrich 가 죽고 원장이 «파싱본만»
   (파생열 전부 결측) 남는 상태를 지났다. **백업 필수** · 소요 파싱 3분 + enrich 5분 +
   다리 13분 · enrich 는 stdout 버퍼링이라 진행이 안 보인다(프로세스로 감시).
8. **★표현 계층은 verify 도 리플레이 A/B 도 안 덮는다.** 사다리 점 색을 `var(--sr-fg)`
   로 썼는데 이 테마엔 없는 토큰이라(CDS 는 `--color-fg`) 4×4 요소가 투명하게 그려졌다.
9. **커버리지 지표에 안 잡히는 수확이 있다.** 범주 콜 23,676건을 되살렸더니 전체
   커버리지는 91.2%→90.9% 로 «내려갔다» — 레벨이 원래 없는 행이 모집단에 들어왔기
   때문이다. **관문은 «범주 콜 뺀 커버리지» 로 볼 것.**
10. `Stop-ScheduledTask` 는 conhost 로 띄운 python 을 안 죽인다 · 기동 되감기 75~95초 ·
    `AmountEff` 는 미표기를 100억으로 채운다(중복·분포를 잴 땐 «명시된 수량» 만 쓸 것).

---

## 6. 재현

```bash
cd C:\Users\infomax\Projects\apps\kbond
# 재파싱 (약 23분) — 반드시 백업 먼저
python kbond_daily_update.py --force
python gate_meaning.py                       # 뜻 채점

# 리플레이 + 검증
set KBOND_REPLAY=20260903&& set KBOND_AT=16:30:00&& set KBOND_NO_MASK=1&& ^
  python -m uvicorn kbond_api:app --host 127.0.0.1 --port 8303
python verify_v4.py --port 8303 --at 16:30:00 --day 20260903

# 화면
cd ..\kbond-web && pnpm dev                  # :3400 · 설정에서 http://127.0.0.1:8303
pnpm build && rm -rf ..\ibond\public && cp -r out ..\ibond\public
cd ..\ibond && git push origin HEAD && vercel deploy --prod --yes
```
