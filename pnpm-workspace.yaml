# pnpm approve-builds 가 남긴 자리표시자를 실제 값으로 채운다.
# ★그대로 두면 Vercel 의 pnpm install 이 «set this to true or false» 를 불리언으로
#   읽지 못해 깨진다. unrs-resolver 는 eslint 해석기의 네이티브 바이너리다.
allowBuilds:
  sharp: true
  unrs-resolver: true
