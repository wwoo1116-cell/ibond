# 다음 세션 인계 — K-Bond, 2026-09-23 · **단가·검산·0.25bp 격자**

> 09-22 판(`PROMPT_next_2026-09-22.md`)을 **대체한다.** 덧붙이지 않고 한 블록으로
> 다시 썼다(09-11 규율). 그 전 판들은 09-22 §0 의 목록을 그대로 따른다.

---

## §0. 붙여 넣을 블록

```
K-Bond 레인. ★이 레인은 2026-09-15 에 [OWNER] 「락만 고치고 얼린다」로 얼렸다.
09-22 «화면 요청», 09-23 «단가·검산» 으로 두 번 열렸다. 개발을 이어서 하지 마라.
내가 지정하는 것만 한다.

정본 —
  Projects\apps\kbond\PROMPT_next_2026-09-23.md   (이 파일 · 09-23 에 바뀐 것 전부)
  Projects\apps\kbond\PROMPT_next_2026-09-22.md   (가림 두 스위치 · 피드 표 · 원↔bp 감사)
  Projects\apps\kbond\PROMPT_next_2026-09-15.md   (얼림 선언 · 유지보수 목록 · 레인 방향)
  Projects\apps\kbond\PROMPT_next_2026-09-11.md   (화면 둘 · 지문 대조 · 배관 함정)

★09-23 에 [A] 가 닫혔다 — 09-10 부터 열려 있던 「3.72/73 팔자」다.
  답은 «세트호가 = 두 다리의 중간값» 이고, 이유는 **0.25bp 를 세 자리로 적을 수
  없어서** 다(§2). 축약호가가 두세 자리라 딜러는 그 자리를 두 다리로 적는다.

★단가가 화면에 섰다(§3). 규약은 전부 **잠정**이고 화면이 스스로 그렇게 말한다.
  국고·외평은 [OWNER] 지시로 **분기 가정**이라 참값과 24-4 −81원 어긋난다.
  크레딧은 단가를 **안 낸다**(쿠폰이 문면에 4.2%뿐 · §3-C).

★검산은 «맞나» 가 아니라 **«이 단가를 믿어도 되나»** 다(§4). X 는 이유를 같이
  싣는다(`chkw`). 이유 없는 X 는 읽는 사람을 훈련시키지 못한다.

손대게 되면 규율 —
- 백엔드를 만졌으면 반드시 셋 다 — verify_v4 [A]~[I5] · gate_meaning [J][K][L] · pytest.
- 재기동은 **반드시** `powershell -NoProfile -File restart_kbond.ps1`.
- 정적 미리보기는 **:3400 으로만** (CORS 허용 목록에 그 포트뿐).
- 불변식은 «오퍼 금리 <= 비드 금리». 락(간격 0)은 걷지 않는다 — 09-15 판정.
- 귀속 규칙과 설명 변수가 축을 공유하면 그 상관은 검정이 아니다.
- ★본표 `MPYieldDB`·`QuoteVsMP_bp` 는 «그날 종가» 민평이다. 장중·예측 문맥에선 선견이다.
- ★★★**지어낸 값은 빈칸보다 나쁘다** — 빈칸은 모른다고 말하지만 지어낸 수는
  안다고 말한다. 09-23 에 이걸 두 번 어겼다가 두 번 물렸다(§7).
- ★★★**코드를 문자열 앵커로 끼우지 마라.** 괘선 주석의 대시 개수가 달라 replace 가
  조용히 빗나갔고, 호출 줄만 들어가 NameError 로 민평 없이 기동했다(§7).
- ★★★**프레임 시간은 «탭이 앞에 있을 때» 만 재라.** 백그라운드 탭은 Chrome 이
  rAF 를 멈춘다 — 6초 뒤 한 번 깨어난 것을 «6초짜리 프레임» 으로 읽었다(§7).

열려 있는 것 —
  [B] verify_v4 B2 국주 기준 민평 불일치 (09-22 §6-1, 기존 건)
  [C] 동향 탭 딜러 리더보드를 사람 이름 축으로 바꿀지
  [E] won_to_bp 표가 테너 평균이라 짧은 쪽에서 최대 24% 어긋난다 (§5-A)
  [F] 크레딧 단가 — 쿠폰을 어디서 받을지 (§3-C)
  [G] 결제일 T+1 — 휴일표가 없다 (§3-B)
  [H] 통안 민평 수집이 넷을 빠뜨린다 (§5-B · 지금은 커브로 메운다)
  [I] kbond_test.py 의 test1/2/3 가 pytest 에 잡혀 늘 3 errors
  ~~[A] CD·CP 3.72/73~~ 닫힘(§2) · ~~[D] kbond-web 푸시 대상~~ 닫힘(§6)
```

---

## §1. 오늘 무엇이 바뀌었나 — 한 문단

금리만 말하던 화면이 **원**으로도 말하게 됐다. 단가 계산기를 정답지(NICE 32행)에
맞춰 닫고, 피드에 `단가·만기일·잔존·검산` 네 칸을 실었다. 그 과정에서 [A] 가
닫혔다 — 세트호가는 **0.25bp 자리를 적을 수 없어서** 두 다리로 적는 표기법이었고,
격자를 0.25bp 로 열자 중간값이 «아무도 낸 적 없는 값» 이 아니게 됐다. 그리고 칸
넷이 렉을 만들어 그것도 잡았다.

커밋 — kbond `ff9ee02`·`fbda374`·`eaedb41`·`90325e8`·`11d9281`·`3c5e9ec`·`443c2e1`
(`origin/kbond-backend`) · kbond-web `8fb6ce0`·`10854e9` (**`origin/kbond-web` 신설**)
· sauron-v2 `ecc84e0a` (단가 규약 인계).

게이트 — pytest **119 통과** · gate_meaning **전건 통과** · verify_v4 **실패 1건**
(기존 B2. 아침 7건에서 여섯 줄었다).

---

## §2. ★★★[A] 가 닫혔다 — 0.25bp 를 세 자리로 적을 수 없다

[OWNER] 「그 이중호가의 답은 결국 세트호가라고 그 중간값을 내는거야」
→ 「걍 호가를 0.25bp씩 최소단위로 두면 해결되는 문제아닐까?」

국고·통안 레벨 **145,952행**에서 마지막 자리(0.1bp)가 **0 또는 5 인 것이 95.02%**
다(나머지는 0.43~0.79%씩 고르게 흩어진 잡음). 지금 격자는 0.5bp 다. 그런데

```
0.5bp  = 0.005%  -> X.405    세 자리로 써진다
0.25bp = 0.0025% -> X.4075   네 자리가 필요하다
```

축약호가는 **두세 자리**(`405`·`41`)다. **딜러는 0.25bp 자리를 한 수로 적을 방법이
없고**, 그 자리를 말하려고 두 다리로 적는다 — 그게 세트호가다.

설명이 맞아떨어지는 자리가 넷이다.

| 관찰 | 설명 |
|---|---|
| 세트는 늘 0.5bp 짝(99.1% 실측) | 이웃한 두 격자점이라서 |
| 단독 호가가 «사이» 에 선 적 **0%** | 0.25bp 는 적을 수가 없어서 |
| 혼자 말할 땐 자기 쪽 다리 (팔자 앞다리 53.4% · 사자 뒷다리 69.4%, n≈4,000씩) | 적을 수 있는 두 점 중 유리한 쪽 |
| 중간값이 답 | 세트 = «그 사이에 서 있다» 는 표기법 |

### 어떻게 넣었나

- **파서(C1)** — `RE_QUOTE_PAIR_VERB`. 틱 없는 세트 꼴(`405 41 세트 팔자`)을 짝으로
  잡는다. 09월 낱말꼴 872행 기준 **0 → 809행(92.8%)**. 구분자가 넷이다 —
  슬래시·공백 말고 **점**(`구구삼통 725.73`)과 **밑줄**(`62_625`)도 쓴다. 점은
  소수점과 겹치지만 앞이 두세 자리여야 하고 간격 검사가 0.5bp 를 요구해 걸러진다.
  ★순수 덧셈임을 확인했다 — 6,000행 표본에서 `QuoteRaw` 바뀐 행 **0**.
- **엔진(C2)** — `lvl_q`(0.25bp 스냅) + `set_mid`(두 다리 복원 후 가운데).
  국고·통안 갈래에서 세트를 **먼저** 보고, 아니면 지금까지 하던 대로 간다.
  ⚠**`round(...,3)` 25곳을 4자리로 바꾸지 않았다.** 그 반올림은 자리수를 줄이는
    일만 하는 게 아니라 복원값을 0.5bp 격자에 **스냅**하는 일을 겸한다. 통째로
    열면 잡음이 격자로 올라온다.
- **화면** — 0.25bp 자리만 넷째 자리로 적는다. 셋째로 뭉개면 `3.9975` 가 `3.998` 이
  되어 «세트라는 사실» 이 화면에서 사라진다.

실측 — 국고·통안 레벨 중 0.25bp 자리 **538/2,149(25%)**.
`삼통 065 07- → 5.0675` · `24-1 995 00- → 3.9975` · `통당 965 97- → 3.9675`

---

## §3. 단가 — 규약은 전부 «잠정» 이고 화면이 그렇게 말한다

정본은 `kbond_price.py` 와 **`test_price.py`(정답지 32행)** 다. 규약을 고치면 그
32행이 먼저 운다. 같은 규약을 sauron-v2 로도 넘겼다
(`HANDOFF-bond-price-convention-2026-09-23.md`).

```
P = [ Σ CF_k / (1+r/m)^k ] / (1 + r/m × d/D)     앞은 복리, 마지막 토막은 단리
```

### A. 주기는 «발행체 × 이자종류» 가 정한다 — 테너가 아니다

| | 참 규약 | 지금 화면 |
|---|---|---|
| 국고·외평 이표채 | 06M (m=2) | **분기 가정 (m=4)** [OWNER 지시] |
| 통안 이표채 | 03M (m=4) | 그대로 (참값) |
| 통안 할인채(DC) | 단리 365 | 그대로 |

★분기 가정의 값 — 참값 대비 **국고24-4 −81.13원 · 16-8 −37.62원 · 25-6 −0.61원**.
이건 근사가 아니라 «다른 채권을 값매기는» 것이다. `ASSUME_QUARTERLY = False`
한 줄이면 즉시 참값이 된다. `test_price.py` 는 `Spec` 에 m 을 직접 넣어 이 스위치에
안 걸리므로 **참 규약은 그 32행이 계속 지킨다.**

### B. [G] 결제일 — 당일(T)

★고른 게 아니라 «정확히 계산되는 쪽» 이다. T+1 은 «다음 영업일» 이라 **휴일표가
있어야 한다.** 09-23 이 그 함정이다 — 익일결제가 09-24 가 아니라 **09-28** 이다
(추석 연휴). 요일만 세는 T+1 은 그 주에 그냥 틀린다. DB 에 휴일표가 없다.
`SETTLE_MODE = "T+1"` 한 줄이 남아 있고, 화면 머리글이 `단가(T)` 로 말한다.

### C. [F] 크레딧은 단가를 안 낸다

[OWNER] 「크레딧은 일단 단가 제공 X」. 여기까지 온 길이 남겨 둘 값이 있다 —

1. 쿠폰이 문면에 **4.2%만** 적힌다(2026-08 이후 408,928행 실측. 만기는 88.6%).
2. «쿠폰 = 전일 민평» 으로 **지어냈다가** [OWNER] 가 물렸다.
3. 쿠폰이 안 드는 꼴(분기복리 할인)로 갈아탔는데 **긴 쪽에서 무너졌다** —
   검산이 잡았다: 한국도로공사884 잔존 17.5년 **4,394원** · LH 9.2년 6,582원.
4. 짧은 쪽은 쓸 만했지만 «어디까지가 쓸 만한가» 를 우리가 정할 수 없어 잔존으로
   가르지 않고 **통째로 비웠다.** 반쪽짜리 규약은 경계를 모른다는 점에서 없느니만 못하다.

⚠**만기일·잔존은 그대로 싣는다** — 가정이 아니라 문면에 적힌 «자료» 다.

### D. 채움률 (2026-09-23 장 마감 무렵)

| | 단가 |
|---|---|
| 국고 | 2,387 / 2,416 (**98.8%**) |
| 통안 | 107 / 149 (72%) |
| 크레딧 | 0 / 981 (규약상 비움) |

빈칸의 이유는 만기가 아니라 **값이 없어서**다 — 교체 호가(`26-2/26-8`)나 은어
(`통당`·`국고채이자`)처럼 종목이 하나로 안 정해지는 행이다.

---

## §4. 검산 — «맞나» 가 아니라 «믿어도 되나»

[OWNER] 「이걸 믿어도 되는지가 제일 중요함」

처음에는 «데스크의 원 계산기(`won_to_bp` 표)와 맞나» 를 봤는데 X 가 597건(14%)
떴고, 캐 보니 원인이 둘 다 «행의 결함» 이 아니었다(§5). 그래서 뜻을 바꿨다.

```
O  이 단가를 믿어도 된다   제원이 있고 · 만기 전이고 · 민평이 오늘 것이고 · 값이 상식 안
X  믿지 말 것 + 이유(chkw)
빈칸  잴 수 없다
```

⚠**길 B 를 `bp_per_won` 으로 갈아타는 길은 버렸다** — 두 길이 같은 함수가 되어
검산이 **항등식**이 된다. 늘 O 가 뜨는 검산은 없는 것만 못하다.

지금 O 2,594 · X 0 · 빈칸 1,406. (오후 한때 X 11 이었고 이유는 둘 다 진짜였다 —
만기 임박 9일 3건, 단가 상식 밖 8건. 후자가 크레딧 단가를 끄게 만든 그 신호다.)

---

## §5. 오늘 캐다가 나온 «아래» 의 문제 둘

### A. [E] `won_to_bp` 표가 테너 평균이다

실측표는 잔존 일곱 칸(0.125·0.375·0.625·0.875·1.25·1.75·2.5)뿐이고 그 밖은
`1/TTM` 이다. 종목마다 이만큼 어긋난다.

```
잔존 0.16년  참 6.382 대 표 4.819   0.76배   ← 짧을수록 심하다
잔존 1.78년  참 0.609 대 표 0.622   1.02배
잔존 29.5년  참 0.067 대 1/TTM 0.034  0.50배   ← 표 밖 외삽이 2배 틀린다
```

⚠09-22 §7 의 「원↔bp 맞는다」 판정은 **잔존 2.5년 아래 크레딧 모집단**의 것이라
그대로 서지만, **국고 장기물로는 안 넘어간다.** 이미 `kbond_price.bp_per_won`
(그 종목을 두 번 값매겨 잰다)이 답을 들고 있다. 고치면 화면과 `verify_v4` F2a 가
같이 낫는다. 배치 원장의 `chk`·`SpreadBpEst` 가 따라 움직이니 전후를 나란히 재라.

### B. [H] 통안 민평 수집이 넷을 빠뜨린다

살아 있는 통안 20종 중 **16종은 최신 일자 민평이 매 영업일 들어온다**(14영업일
중 9~10일 값이 움직인다 — 이월이 아니라 진짜다). 못 받는 넷이 문제였다.

```
29.09.03통  민평 한 건도 없음   (2026-09-03 발행)
28.07.02통  2026-09-10 (12일)
28.09.03통  2026-09-10 (12일)
29.03.03통  2026-08-26 (27일)
```

여태 `MSB_MP_WIN` 창의 «가장 최근» 값으로 메워, 28.07.02통 이 **12일 묵은 3.827** 을
오늘 값처럼 썼다. 그날 호가(3.975)와 15bp 벌어져 수정가액이 −26원이 됐고, 검산
X 597건 중 **191건이 그 한 종목**이었다.

**지금은 계열 커브에서 붙인다**(`_msb_mp_fill`). 수정가액 **−26.07 → +5.10원**.

★계열을 갈라야 한다 — 이름 끝 `-01`·`-02`·`-03` 이 1년물·2년물·3년물이고 **커브가
셋**이다. `27.07.01통` 이 `27.07.02통` 보다 열흘 내내 꾸준히 14bp 위다(만기 7일 차).
한 줄에 놓고 보간하면 그 계단이 잡음으로 섞여 LOO 오차가 6~7bp 가 된다. 계열
안에서는 28.07.02통이 4.010 으로 그날 호가와 **3.5bp** 차였다.

⚠**이건 메우는 것이지 고치는 게 아니다.** 수집기가 왜 이 넷을 빠뜨리는지는 안
봤다. `29.09.03통`(09-03 발행, 0건)이 제일 좋은 표본이다.
⚠붙인 값은 `mpd` 에 날짜 대신 **`보간`** 이 들어간다. 화면·검산이 그걸 읽는다.

---

## §6. 화면 — 칸 넷, 그리고 그 칸이 만든 렉

`시간 · ☆ · 종류 · 종목명 · **만기일** · **잔존** · 매매 · 브로커 · 회사명 ·
전일민평 · 할인조정 · **단가(T)** · 수량 · 원문 · **검산**`

- **만기일·잔존** — 호가가 아니라 «종목» 의 속성이라 값 없는 행(관심·문의)에도 선다.
  ★1년 안쪽은 «일» 로 적는다. «0.2년» 이라 쓰면 만기가 코앞인 게 안 보이는데 그
  구간은 1원이 4.7bp 다(30년물은 0.06bp).
- **단가(T)** — ★머리글에 결제일을 적는다. 안 적으면 그 숫자는 읽는 사람마다
  달라진다(국고16-8 당일 10006.54 · 익일 10010.90). 수정가액(민평 대비 원)은 칸을
  안 늘리려고 툴팁에 있다.
- **검산** — O/X 글자가 곧 값이다(색만으로 말하지 않는다). X 는 툴팁에 이유.
- 단가 가정은 **하단 캡션**에 적는다 [OWNER 「맨 밑에 블커본드 막무가내 어쩌고 옆에」].
  표 위에 sticky 로 뒀더니 머리글(`.kb-fhead`)과 같은 `top:0` 이라 호가와 겹쳤다.
  문구는 서버(`/health` 의 `px_assume_quarterly`)가 말해 주는 값으로 띄운다 —
  화면이 제 마음대로 적으면 스위치를 되돌렸을 때 알림만 남아 거짓말이 된다.

### ★★★렉 — 원인은 «행이 많아서» 가 아니었다

```js
useEffect(() => { if (stick.current) el.scrollTop = el.scrollHeight; }, [rows]);
const onScroll = () => { ... el.scrollHeight - el.scrollTop - el.clientHeight ... };
```

`scrollHeight`·`clientHeight` 는 읽는 순간 브라우저가 배치를 끝까지 계산해야 답하는
값이다(강제 동기 리플로우). 그 상자에 격자 항목이 **12,000개**(800행 × 15칸)
있었고, ①은 매초(새 호가) ②는 **스크롤 이벤트마다** 그걸 불렀다.

⚠어제까지 800 × 11 = 8,800 이던 것이 **칸 넷으로 12,000** 이 됐다. 렉이 «갑자기»
생긴 게 아니라 한 번의 리플로우가 36% 비싸져 임계를 넘은 것이다.

고침은 CSS 두 줄 — `content-visibility:auto` + `contain-intrinsic-size:auto 22px`.
질문은 그대로인데 답하는 값이 싸진다: 보이는 서른 줄만 실제로 배치하고 나머지
770행은 «22px씩» 으로 센다. 가상 스크롤을 새로 짜지 않고 같은 효과를 냈다.

| 브라우저 실측 | |
|---|---|
| 전 | rAF 90프레임이 45초 안에 안 끝남 (≈0.5초/프레임) |
| 후 | 가만히 p50 **16.7ms**(60fps) · 스크롤 중 p50 24.2ms · 강제 리플로우 **0ms** |

### SSE 도 가볍게 — `/events?lite=1`

책 **1.2MB** 를 매초 밀고 있었는데 새 화면이 그 프레임에서 읽는 건 `feed` 와
`n_msg` **둘뿐**이었다. `now·n_msg·n_seq·feed(60행)` 만 보낸다 — **1,192,937 →
25,568 bytes (46.7배)**. 꼬리가 60행인 근거: 화면은 접속할 때 `/feed.json` 으로
3,000행을 따로 채우고, 메시지가 초당 0.4개라 3분 멎어도 안 빠진다.
⚠**기본(`lite=0`)은 그대로 뒀다** — 옛 화면(`/`)은 프레임 전체를 읽는다.

### 커브(잔존×YTM) 다시 그리기

[OWNER] 「너무 바보 같은데 · 인터렉션도 없고」. `components/Curve.tsx` 로 뺐다.

- ★**눈금은 데이터가 아니라 격자에서** 나온다. 전에는 데이터의 최소·중간·최대라
  `2.99 · 4.38 · 5.77` / `0M · 8.8년 · 17.5년` 이 찍혔다. 읽는 사람은 눈금을 «자» 로
  쓰는데 자에 8.8 이 적혀 있으면 그건 자가 아니다 → `niceTicks`(1·2·2.5·5 ×10^k).
- ★**가장 가까운 점**을 집는다 — 조준이 아니라 «근처» 면 된다. 십자선·말풍선이
  따라오고 화살표 키로도 옮겨진다. 말풍선은 **값이 앞, 이름이 뒤**.
- 범례를 세웠다(색만으로 말하지 않기) · 격자는 실선 헤어라인(점선은 «추정치» 로
  읽힌다) · 집힌 점의 고리는 테두리가 아니라 **바탕색**(마크는 선이 아니라 틈으로 가른다).

### [D] 닫힘 — kbond-web 에 리모트가 생겼다

`ibond.git` 의 **`kbond-web`** 브랜치다. 백엔드는 `kbond-backend` 그대로.
배포는 여전히 `powershell -NoProfile -File build_app.ps1` 뒤
`npx vercel deploy --prod --yes`.

---

## §7. ★★★오늘 틀린 것 — 다음 사람이 같은 길로 안 가게

값보다 이쪽이 더 쓸모 있을 것이다.

1. **지어낸 값을 두 번 넣었다가 두 번 물렸다.**
   ①검산 X 87건이 볼록성 거짓 경보였다(25-11 이 6.1bp 움직일 때 선형 환산이 0.5원
   벌어졌다) → bp/원을 «두 금리의 가운데» 에서 재도록 고쳤다.
   ②크레딧 쿠폰을 «전일 민평과 같다» 로 지어냈다 → [OWNER] 가 바로 물렸다.
   **빈칸은 모른다고 말하지만 지어낸 수는 안다고 말한다.**
2. **남의 수를 다른 모집단에 옮겨 읽고 단정했다.** 09-04 의 50.6/50.4% 를 보고
   「방향 규칙은 동전 던지기」라고 했는데, 세트 행만 골라 직접 재니 팔자 앞다리
   53.4% · 사자 뒷다리 69.4% 로 **규칙이 지지된다.**
3. **그 수를 보고 이번엔 반대로 틀렸다.** 「중간값은 아무도 낸 적 없는 값이니 책에
   넣지 말자」 — «적을 수 있는 값» 과 «있는 값» 을 같은 것으로 본 오독이다(§2).
4. **문자열 앵커로 코드를 끼웠다.** 괘선 주석의 대시 개수가 원본과 달라 `replace`
   가 조용히 빗나갔고, 호출 줄만 들어가 `NameError` 로 **민평 없이 기동**했다
   (국고 2종까지 떨어졌다). 함수 정의 위치를 찾아 줄 단위로 넣어라.
5. **렉의 원인을 두 번 헛짚었다.** 백엔드 CPU(15.3%→0.8%)와 SSE(46.7배)를 고쳤는데
   둘 다 오늘 렉의 원인이 아니었다. 셋째로 브라우저에서 직접 재서 찾았다.
   **고치기 전에 재라 — 그 자리에서.**
6. **백그라운드 탭의 rAF 를 프레임 시간으로 읽었다.** 「첫 렌더가 5.9초」라고
   보고했다가 정정했다. Chrome 은 안 보이는 탭의 rAF 를 멈춘다.

---

## §8. 손댄 파일

```
kbond (origin/kbond-backend)
  kbond_price.py      ★신설 — 단가 규약·제원표·trustcheck
  test_price.py       ★신설 — 정답지 32행 + 함정 시험 다섯
  test_publish.py     ★신설 — 굽는 삯(안 구웠다 / 그런데도 안 멈춘다)
  kbond_live.py       lvl_q·set_mid·_msb_mp_fill·_price_cur·_mp_trust ·
                      _restore_memo · today_files 캐시 · publish 절전 · raw_lite
  kbond_api.py        _snapshot 게으른 굽기 · /events?lite=1 · /health 에 다섯 칸
  kbond_schema.py     FeedRow 에 px·pxs·pxb·pxa·mat·ttm·won·chk·chkw
  parse_kbond_logs.py RE_QUOTE_PAIR_VERB (틱 없는 세트 · 구분자 넷)

kbond-web (origin/kbond-web ★신설)
  src/components/Curve.tsx   ★신설 — 커브를 Credit 에서 빼서 다시 그림
  src/components/Feed.tsx    칸 넷 · 0.25bp 넷째 자리 · 검산 툴팁
  src/app/page.tsx           SSE lite=1 · 하단 캡션에 단가 가정
  src/theme/kbond.css        --kb-cols · content-visibility · 커브 상호작용
  src/lib/api.ts · api-types.d.ts

sauron-v2 (origin/main)
  HANDOFF-bond-price-convention-2026-09-23.md   단가 규약 인계(정답지 32행 포함)
```
