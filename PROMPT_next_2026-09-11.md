# 다음 세션 인계 — 2026-09-11

## §0. 붙여 넣을 블록

```
K-Bond 레인. 정본 둘을 먼저 읽어라 —
  Projects\apps\kbond\PROMPT_next_2026-09-11.md   (이 파일 · 오늘 달라진 것)
  Projects\apps\kbond\PROMPT_next_2026-09-10.md   (레인 전체 · 규율 · 함정 · 여전히 유효)
오늘 할 것은 §4 후보에서 지정한다. [A] 오너 판정 하나가 아직 열려 있다(§3).
백엔드를 만지면 반드시: verify_v4 [A]~[I] · gate_meaning [J][K][L] · pytest.
재기동은 손으로 하지 말고 powershell -NoProfile -File restart_kbond.ps1 을 써라.
```

## §1. 09-10 인계문은 그대로 유효하다

규율 다섯·함정·사전 JSON 굽는 순서·판정 대기열은 `PROMPT_next_2026-09-10.md` 에 있고
바뀐 것이 없다. 여기에는 **09-11 에 달라진 것만** 적는다.

## §2. 오늘 달라진 것

정본은 `RESULT_gov_pills_audit_2026-09-11.md`. 요약하면 둘이다.

1. **분류 줄의 구성** = 국고 · 통안 · 무위험 · 특은채 · 은행채 · 카드채 · 캐피탈 ·
   회사채 · 유동화 · MBS [OWNER]. 히트맵 행과 같은 축이다. 국고·통안은 계열이
   아니라 레인이라 고르면 «만기 버킷» 으로 접힌다. 서버가 `cls_pills` 로 줄의 구성을
   내고, 화면은 그 순서를 그대로 그린다.
2. **배관 감사 열 가지 중 여덟을 고쳤다.** 값이 가장 큰 것은 **16:30 일일 갱신이
   4영업일 연속 안 끝나고 있던 것**이다(파생 표가 원장보다 하루 뒤처져 있었다).
   트리거를 15:50 + 08:05 로 바꿨다.

### 새 규율 하나

▎**완료 줄이 없는 로그는 실패한 로그다.** 배치를 걸면 «시작」이 아니라 «완료」를 세라.
네 번 연속 끊긴 것을 아무도 못 봤다.

## §3. [A] 오너 판정 대기 — 하나 (그대로)

**CD·CP 「3.72/73 팔자」의 뜻.** `PROMPT_next_2026-09-10.md` §5 를 그대로 읽어라 —
전제 셋이 09-10 실측으로 이미 깨졌고(양방 아님·범위 아님·어두운 행 아님), 판정은
「어두운 행을 여는 것」이 아니라 **이미 책에 올라간 18,142행의 값이 맞나** 다.
뜻을 받기 전에는 짝 19,751행을 손대지 않는다.

## §3-2. React 전환 3단계 — 지문 대조는 끝났다

정본 `RESULT_screen_parity_2026-09-11.md`. 「나란히 하루」를 기계가 세도록 바꿨고
(`compare_screens.mjs`), 동결 리플레이에서 **다섯 영역 88줄 0건 차이**다.

대조가 잡은 넷은 전부 «옛 화면이 틀린 쪽» 이었다 — 크레딧 중앙값이 31칸 전부
0.0bp 였던 것(09-07 판정이 서버에만 있었다), 오퍼 표의 «민평» 표기, 새 화면의 종목
구획 셋, 어제 종가 폴백. 넷 다 고쳤다.

**남은 것은 오너 판정 하나다** — 전환할 것인가, 한다면 무엇을 전환하나(Funnel `/kbond`
가 서빙할 화면을 바꿀 것인가, 데스크가 Vercel 주소를 쓸 것인가). 수로 할 말은 끝났다.

⚠장중 실황으로 대조하면 0건이 안 나온다. 두 화면의 폴링 주기가 달라 각자 다른 순간을
그린다 — 시차이지 어긋남이 아니다. 판정은 동결 리플레이로만 한다.

## §4. 다음 후보

1. **[B1] 판정 대기열 2회차** — `python kbond_issuer_todo.py 30`. 09-10 인계문 §6 그대로.
2. **저녁 파생 표를 보장할 것인가** — 15:50 도 PC 가 일찍 꺼지면 끊긴다. 끊겨도 이튿날
   08:05 이 회수하지만 «그날 저녁에 다리·체결을 보는 일» 은 아직 보장이 없다.
   단계를 쪼개 재개 가능하게 만들거나, 파싱만 먼저 끝내고 파생은 따로 거는 길이 있다.
3. ~~백업 파케 정리~~ — 09-11 에 끝냈다(최근 두 벌만 남기고 여섯 벌 삭제, 1.5GiB 회수).
4. ~~탭 숫자와 pill 숫자 맞추기~~ — [OWNER 2026-09-11] **그대로 둔다.** 둘은 진짜로 다른
   것을 센다(탭 = 오늘 호가가 붙은 종목, pill = 전일 민평 대비를 잴 수 있는 종목이고
   그 아래 버킷 합과 같다).
5. **React 전환 3단계 판정** — 수는 끝났다(§3-2). 남은 건 «무엇을 전환하나» 다.

## §5. 손이 닿는 곳

| | |
|---|---|
| 백엔드 | `:8301` · 태스크 `KBondLive`(주중 08:20) · 재기동 `restart_kbond.ps1` |
| 일일 갱신 | 태스크 `KBondDailyUpdate`(주중 **15:50 · 08:05**) · 로그 `data\kbond\kbond_daily_update.log` |
| 공개 | Funnel `https://e110430.tailc7b701.ts.net/kbond` |
| React | `Projects\apps\kbond-web`(원격 없음) · Vercel `kbond-web` · `DEFAULT_API` = 위 Funnel |
| 검증 | `verify_v4.py --port 8301 --day <오늘>` · `gate_meaning.py` · `pytest test_frac.py test_cors_token.py` · `test_issuer.py` |

⚠기동은 그날 로그를 통째로 되감는 일이라 **장이 갈수록 오래 걸린다**(11:22 실측 61초).
재기동 뒤 바로 안 뜬다고 다시 죽이지 마라.
