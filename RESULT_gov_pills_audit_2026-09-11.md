# 분류 줄 앞의 국고·통안 + 배관 감사 — 2026-09-11

## 0. 한 줄

오너 「위에 구성도 국고 통안 무위험 특은 은행 카드 캐피탈 회사 이런식으로」 →
분류 줄을 히트맵 행과 같은 축으로 세웠고, 이어서 배관을 훑어 열 가지를 찾아
여덟을 고쳤다. **값이 가장 큰 것은 UI 가 아니라 「16:30 일일 갱신이 4영업일 연속
안 끝나고 있었다」 였다.**

## 1. 분류 줄 — 구성

```
전에:  [전체] 무위험 특은채 은행채 카드채 캐피탈 회사채 유동화 MBS
지금:  [전체] 국고 통안 무위험 특은채 은행채 카드채 캐피탈 회사채 유동화 MBS
```

국고·통안은 **계열이 아니라 레인**이다. 책이 따로 있고(`snap["ktb"]`·`snap["msb"]`)
등급 축이 없다. 그래서 고르면 등급 대신 **만기 버킷**으로 접어 같은 표로 보여 준다.

- 칸 값은 히트맵의 국고채·통안채 행과 **같은 문**에서 온다(mid − 민평).
  대조 7/7 일치(국고 +9.10 +9.25 +9.75 +9.10 +9.75 · 통안 +8.30 +18.00).
- 없는 축은 만들지 않았다 — 바스켓도 등급 커브도 비워 보낸다.
- 오퍼 면은 종목마다 최우선 매도 하나만. 그 칸 이름이 «오퍼» 다. 양면은 레인 탭 사다리 몫.
- 줄의 구성은 서버가 낸다(`cls_pills`). 버킷에서 뽑아 만들면 국고·통안이 못 들어오고
  건수 0 인 계열이 사라진다 — 줄이 날마다 흔들리면 눈이 자리를 잃는다.

## 2. 감사 — 찾은 열, 고친 여덟

### ★A. 16:30 일일 갱신이 매일 안 끝난다 (고침)

`kbond_daily_update.log` 에 09-07·08·09·10 의 **16:30 자동 실행은 「갱신 시작」만 있고
「완료」가 없다.** 완료된 것은 전부 낮에 손으로 돌린 것이다.

```
2026-09-10 16:30:01 시작 → 16:30:40 원장 기록 → 16:41:57 다리 재생성 → (끊김)
태스크 결과 0x40010004(종료됨) · 실행시간 상한은 PT2H 라 타임아웃이 아니다
kbond_legs.parquet.tmp 4.9MB 가 16:59 에 멈춘 채 고아로 남아 있었다
```

**결과**: 원장은 그날 오후까지 들어갔는데 **파생 표(다리·체결·KIS 커브)는 그날 낮에
멈춰 있었다**(legs·fills mtime 13:43). `gate_meaning` 의 「체결」도 그 뒤처진 표다.

원인은 시간표다 — 16:30 에 시작해 20~28분이 걸리는데 그 전에 PC 가 꺼진다.

고친 것:

- 트리거를 **15:50 + 08:05 두 개**로 바꿨다(주중). 15:50 이 그날 대부분을 담고,
  이튿날 08:05 이 장 끝나고 쌓인 꼬리를 회수한다. 파싱은 지문 대조라 멱등하다.
- 고아 `.tmp` 를 지우고 밀린 것을 한 번 돌려 따라잡았다.

⚠**남은 위험**: 15:50 도 PC 가 일찍 꺼지면 또 끊긴다. 끊겨도 이튿날 08:05 이 회수하지만,
「그날 저녁에 파생 표를 보는 일」은 여전히 보장되지 않는다.

### ★B. React 히트맵이 「묵은 민평」 경고를 버린다 (고침)

서버는 `heat.stale` 을 실어 보내는데 `Heat.tsx` 가 안 썼다. 2026-09-11 실측으로
`stale = True` 이고, 통안 2029-03-03 의 민평 기준일이 **2026-08-26(16일 전)** 이라
통안채 2~3 칸이 **+18.0bp** 로 서 있었다 — 옛 화면은 별표와 각주로 말하는데 React 는
아무 말도 안 했다. 진짜 18bp 움직임으로 읽힌다.

고친 것: React 히트맵에 별표·각주·툴팁을 붙이고, **버킷 줄에도 «묵음» 배지**를 달았다
(두 화면 모두 — 옛 화면의 버킷 줄도 말하지 않고 있었다). 서버는 버킷마다 `stale` 을 낸다.

### C~E. 옛 화면에만 있던 셋을 React 로 (고침)

| | 무엇 | 왜 |
|---|---|---|
| C | 커브 x축 범위 토글(5년·10년·전체, 기본 10년) | 국고 50년물 하나가 축을 48년까지 늘려 나머지 점이 왼쪽에 뭉갰다 |
| D | 히트맵 «등급 지정/무시» 토글 | 등급을 합친 칸은 **서버가 원자료에서 다시 접어** 준다(`종별\|*\|버킷`). 중앙값끼리는 못 합친다 — 등급별 중앙을 다시 중앙 내면 딴 수가 된다 |
| E | 크레딧 메시지 테이프 | 국고·통안 화면엔 있는데 크레딧에만 없었다. 국고·통안 pill 을 고른 동안에는 그 레인 메시지를 쌓는다 |

### F. 태스크 Stop 이 파이썬을 안 죽인다 (고침)

`Stop-ScheduledTask -TaskName KBondLive` 뒤에도 옛 프로세스가 :8301 을 물고 살아 있고,
새 인스턴스는 「주소가 이미 사용 중」으로 **조용히 죽는다**(2026-09-11 실측). 태스크의
실행 파일이 `conhost.exe --headless` 라 엔진이 래퍼만 본다.

고친 것: `restart_kbond.ps1` — 포트 주인을 직접 죽이고 태스크를 다시 시작한 뒤 뜰 때까지
확인한다. ★기동은 그날 로그를 통째로 되감는 일이라 **장이 갈수록 오래 걸린다**
(11:22 실측 61초·메시지 5,392건). 기다림을 3분으로 잡아 뒀다.

### G·H. 작은 것 (고침)

- 죽은 상수 `kbond_live.SECTOR_ORDER` 제거(아무 데서도 안 썼다).
- `RESULT_react_screens_2026-09-04.md` §5 「남은 것」이 뒤처져 있었다 — 셋 중 둘은 이미 됐다.

### I·J. 안 고친 둘 (오너 판단)

- **백업 파케 2.05 GiB**(structured 8벌 · legs·fills 각 2벌). C: 여유 42.9GiB 라 급하지 않아
  손대지 않았다. 지울지 말지는 오너 몫이다.
- **탭 「국고 17」 vs pill 「국고 13」** — 세는 것이 다르다(탭 = 오늘 호가가 있는 종목,
  pill = 전일 민평 대비를 잴 수 있는 종목). pill 의 수는 그 아래 버킷 합과 같다.
  뜻을 맞출지는 판단이 필요해 그대로 뒀다.

## 3. 깨끗한 것 (확인했고 문제 없음)

- `verify_v4 --port 8301 --day 20260911` **[A]~[I] 전건 통과**(고치기 전·후 두 번)
- `gate_meaning` [J][K][L] 전건 통과 · `pytest test_frac test_cors_token` **55 passed**
  · `test_issuer` 138/138
- 오늘 라이브 크레딧 600건 **전부 8계열 안**, 규정 밖 라벨 0 · ABS 표 이름인데 유동화가
  아닌 행 **0**
- 서버 로그의 오류 9,246줄은 **옛 개발 세션(메시지 10,299건짜리 리플레이)** 것이고,
  오늘 기동 이후로는 0

## 4. 이번에 값을 치른 것

▎**「화면이 같은 수를 두 자리에서 다르게 말하면 안 된다」** — 히트맵은 별표로 말하는데
버킷 줄은 말하지 않았다. 서버가 이미 `stale` 을 보내고 있었으므로 이건 «없던 정보» 가
아니라 **버린 정보**였다. 계약에 있는 필드를 화면이 안 쓰면, 그건 조용한 거짓말이 된다.

▎**「완료 줄이 없는 로그는 실패한 로그다」** — 네 번 연속 끊긴 배치를 아무도 못 봤다.
시작 줄만 보면 도는 것처럼 보인다. 다음에 배치를 걸 때는 **완료 줄을 세는 눈**을 같이 건다.

## 5. 파일

| | |
|---|---|
| `kbond_view.py` | `GOVCLS`·`gov_rows/gov_buckets/gov_sel/gov_curve`·`cls_pills`·`_mp_line` · 버킷 `stale` · 히트맵 `종별\|*\|버킷` |
| `kbond_schema.py` | `ClsPill` · `CreditBucket.gov/.stale` · `Heat` 열쇠 설명 |
| `kbond_live.py` | `SECTOR_ORDER` 제거 |
| `kbond_live.html` | 같은 접기를 옛 화면에도 · 버킷 «묵음» 배지 · 레인 메시지 · 커브 부제 |
| `restart_kbond.ps1` | 신설 — 재기동 |
| `openapi.json`·`kbond-api.d.ts` | 재생성 |
| kbond-web `Credit.tsx`·`Heat.tsx`·`page.tsx`·`kbond.css` | pill 구성 · 묵음 · 범위 토글 · 등급 무시 · 메시지 |
