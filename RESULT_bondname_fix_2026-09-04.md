# BondName 앞말 결함 수리 + 원장 교체 (2026-09-04 08:00~08:50)

[OWNER 2026-09-04] 「파서 수리까지 묶어 전체 1회」 — 그대로 실행했다.

## 0. ★앞 세션 진단 정정 둘

`HANDOFF_issuer_to_unparsed_2026-09-03.md` §0·§5.1 의 두 문장이 틀렸다.

**(1) 「라이브 서버가 parquet 을 물고 있어 os.replace 가 막혔다」 — 아니다.**
`kbond_live.py` 에서 원장이 나오는 곳은 13행 주석 한 줄뿐이고, 그 주석이
「이 레인은 `kbond_structured_data.parquet` 을 읽지도 쓰지도 않는다」고
말한다(T24 회피가 설계 의도). 실제 잠금 주체는 **같이 돌던 16:30
`KBondDailyUpdate`** 다. 그 시각 enrich 단계였고 `enrich_kbond_quotes.py:70-71`
이 같은 경로를 **읽으면서 쓴다**. 라이브를 내렸어도 replace 는 안 됐다.
→ 공개 서비스를 내릴 이유가 애초에 없었다.

**(2) 「`.tmp` 는 준비됐다」 — 승격하면 안 되는 물건이었다.**
열은 원장의 상위집합(57 + 2)이라 안전해 보이지만 enrich 열이 전부 비어 있었다:

| 열 | 09-03 원장 | .tmp |
|---|---:|---:|
| QuoteYield | 46.14% | **0.00%** |
| MPBase | 89.78% | **0.00%** |
| MPYieldDB | 37.40% | **0.00%** |

파싱 단계 산출물이 enrich 열 이름만 빈 채 달고 있던 것. 갈아끼웠으면 호가·
스프레드 층이 통째로 날아갔다. **어느 길로 가든 enrich 재실행이 필요했다** —
「묶는다」가 맞았던 진짜 이유는 재파싱 절약이 아니라 이것이다.

## 1. 고친 것 — `RE_BOND_NAME` (§4.1)

`parse_kbond_logs.py` 세 곳. 백업 `parse_kbond_logs.py.bak-bondname`.

- 정규식: 한 토큰 → **최대 3토큰**(`(?:[가-힣A-Za-z]{2,12}\s){0,2}` + 마지막 토큰)
- `NAME_PREFIX_JUNK`(신설 22개) + `_is_prefix_junk()` — **앞말에만** 적용
- 루프: 마지막 불용어 **뒤**만 남긴다(앞만 떼면 `공사채 사자 주금공MBS` 의
  `사자` 가 이름에 남는다)

§4.2 `차유동화` 826행은 오너 「일단 넘긴다」대로 손대지 않았다.

## 2. ★함정 둘 — 실측으로만 잡혔다

**(1) 룩비하인드로 앞을 막으면 첫 글자가 잘린다.** `(?<![\d.])` 를 넣었더니
`24.12.13sk텔레콤84-1` 처럼 **날짜가 이름에 바로 붙는 문면**에서 시작 위치가
한 칸 밀렸다: `sk텔레콤`→`k텔레콤` · `한국지역난방공사`→`국지역난방공사` ·
`평택도시공사`→`택도시공사`. 15행은 아예 이름을 잃었다. 폐기.

**(2) 앞말을 이으면 은어가 딸려 온다.** 2판에서 `bp팔자한국수력원자력72-1` ·
`일만기LG에너지솔루션2-3` · `관심한국서부발전48-2` 처럼 **원래 맞던 이름**이
망가졌다(258건 중 18건). 앞말 전용 엄격 판정으로 잡았다.

## 3. 측정 사슬 (원장 전수 또는 20만행 표본)

| 단계 | 방법 | 결과 |
|---|---|---|
| 플러밍 | 원장 20만행을 현재 `extract()` 에 재투입 | 저장값과 **100.00%** 일치 |
| 1판 | 룩비하인드 포함 | 337건 변경 — 절단 54 · 소실 15 **폐기** |
| 2판 | 룩비하인드 제거 | 258건 — 회복 240 · 과잉흡수 18 |
| census | `core` 전수 821만행 | 앞말 부착 9,347행 / 214종 |
| 3판 | 앞말 엄격 판정 | 8,627행 / 166종 — 잔여 꼬리는 1건짜리 대화체 |
| 최종 A/B | 20만행 | 240건 **전부 앞말 회복** · 소실 0 · 절단 0 |

⚠ census 를 `Message` 로 재면 `부국` 7,245 같은 허수가 낀다(브로커 꼬리표).
반드시 `split_broker` → `RE_ROUTE_TAG` → `RE_TB_PREFIX` 를 거친 `core` 로 잴 것.

## 4. 원장 교체 결과

`python kbond_daily_update.py --force` 1회 · **1,182초** · exit 0

| | 09-03 16:36 판 | 09-04 08:16 판 |
|---|---:|---:|
| 행 | 8,214,240 | 8,214,400 |
| 열 | 57 | **59** |
| IssuerCanon | 없음 | 21.77% (raw 1,708,423 · power5 79,983) |
| QuoteYield | 46.14% | 46.30% |
| MPBase | 89.78% | 89.84% |

회복된 발행체(신규 원장 행수): `CJENM` 4,595 · `cjenm` 844 · `JB우리캐피탈`
25,719 · `BNK캐피탈` 28,824 · `주금공MBS` 4,017 · `메리츠캐피탈` 18,397 ·
`IBK캐피탈` 19,149 · `한국서부발전` 17,196 · `LS일렉트릭` 341 · `sk텔레콤` 1,118.
잔여 일반명사 머리말 `캐피탈` 902 → **78** · `ENM` → 188.

백업: `kbond_structured_data.parquet.bak-20260903`. 잔재 `.tmp` 둘 삭제.
legs·fills·kis 전부 08:31 재생성.

## 5. 검증

- `test_issuer.py` 48/48 · `test_frac.py` exit 0
- `verify_v4.py --port 8301 --day 20260904 --at 08:30:00` 전건 통과
  (다만 장 초반이라 원본 3건 — 사실상 빈 검증)
- **`replay_verify.py 20260903 15:00:00 8302` 전건 통과** ← 이쪽이 진짜 검증
  원본 12,706건 · 책 11,007행 · 엄격 True · **가림 False(원문 대조)**
  [A] 115/115 · [B] 22/22 · [C2] 26/26 · [D] 3000/3000 · [E] 64/64 ·
  [F] 64/64 · [F2] |chk| ≤ 1bp 90.0% · [G] 딜러 10/10 · 크로스 위반 0

## 6. ★라이브도 같은 파서를 쓴다

`kbond_live.py:55` 가 `parse_kbond_logs` 에서 `extract` 를 가져온다. 라이브는
08:20 예약 기동으로 **이미 새 파서로 돌고 있다**(PID 23548 · health 200).
배치만 고쳤다고 생각하면 안 된다 — 파서를 만지면 공개 화면이 같이 바뀐다.
그래서 5절의 리플레이 검증을 배치가 아니라 **라이브 코드 경로**로 돌렸다.

## 7. 남은 것

1. `차유동화` 826행(§4.2) — 발행체명 안에 숫자(`신보2023제1차`)라 한글 뭉치
   규칙이 뒤만 집는다. 오너 보류.
2. 158쌍 발행체 사전 — 검정은 끝났고 아직 안 넣었다(오너 「나머지는 원문 일단」).
3. 앞말 잔여 꼬리 약 60행 — `지역전월`(5) · `민평팔거나`(3) 등. 대부분 원래도
   쓰레기였던 대화체 행이라 방치 가능.
4. 미해석 레인 P1·P3 은 아직 손대지 않았다. `parse_kbond_logs.py` 는
   **2026-09-04 08:10 판**이니 stale 사본으로 편집하지 말 것.
