# 파이썬
__pycache__/
*.pyc

# 손으로 뜬 백업 — 이제 git 이 그 일을 한다
*.bak
*.bak-*

# 실행 산출물
*.log
*.out
.pytest_cache/
.impeccable/

# 원장·파생물은 여기 없다(Projects\data\kbond). 실수로 들어오면 막는다
*.parquet
*.csv
*.bak.html

# ★[OWNER 2026-09-09] 벤더 원장(인포맥스·KOFIA)에서 구운 사전은 리포에 안 넣는다.
#   유료 자료를 배포하는 셈이 되고, 코드만 있으면 언제든 다시 굽는다:
#       python kbond_issuer_dict.py     # 표제어 9,872 · 이형태
#       python kbond_issuer_derive.py   # 민평 검정층
#       python kbond_issuer_near.py     # 근접층
#       python kbond_sector.py          # 계열 대조표
kbond_issuer_dict.json
kbond_issuer_derived.json
kbond_issuer_near.json
kbond_sector.json
issuer_ratings.json
