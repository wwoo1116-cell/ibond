# 책이 말하는 것 — 체결 귀속이 열어 준 §11 지표 1차 (2026-09-15)

[OWNER 2026-09-15] 「K-Orderbook+++」 → §11 다음 증분. 「++」(09-07)가 «체결은 삭제가 아니라
확인» 을 세우고 그 부산물로 공격 방향·딜러별 체결을 남겼다. 그 둘이 있어야만 잴 수 있던 지표
셋 — 유효 스프레드 · 불균형 → 공격 방향 · 딜러 호가 경쟁 → 체결 점유 — 을 **국고 전 이력**
에서 위약 대조로 쟀고, 살아남은 것을 동향 탭에 올렸다.

스크립트 `kbond_dyn_study.py` (이번엔 리포에 둔다 — v12 의 연구 스크립트가 남지 않아 이번에
그 수를 다시 못 맞춘 자리가 있다) · 수치 `data\kbond\kbond_dyn_study.json` · 체결별 표
`kbond_dyn_study_fills.parquet`.

표본: 2024-02-14 ~ 2026-09-14 **547 영업일** · 국고 QUOTE(종목·방향·레벨 있음) **1,754,785**
(방 가로지르는 20초 중복 85,666 제외) · 국고 체결(종목·레벨 있고 `RepostSeq=0`·`DupSeq=0`)
**39,889** · 종목-일 26,587 · 딜러-일 124,246.

책 접기 규약은 라이브와 한 벌 — 키 (딜러, 방향, 종목) 절대값 교체 · TTL 1800초 · uncross ·
오퍼 최우선 = 오퍼 중 최고 금리 · 귀속 = 같은 종목·레벨 일치·같은 브로커 우선·30분 창 ·
수량 표기 없음 = 100억. 라이브 `Book._book_ctx` 가 같은 규약으로 체결 순간에 같은 값을 잰다(§7).

---

## 0. 먼저 — 첫 판이 틀렸던 자리 셋

숫자를 적기 전에 이것부터. 첫 실행은 그럴듯한 수를 냈고 셋이 틀렸다.

1. **브로커 키가 안 맞물렸다.** 체결표에는 `BrokerKey` 가 없고 `Broker`(서명 원문)만 있다.
   서명 앞부분을 키로 썼더니 본표 `BrokerKey`(전화번호 정규화)와 **겹침 0%** — «딜러+레벨»
   귀속이 통째로 0 이 되고 R3 가 전부 0 이 됐다. `parse_kbond_logs.broker_key` 를 체결표에도
   지나게 하니 겹침 **99.8%**. 같은 축을 두 표에서 쓸 때는 **같은 함수**를 지나게 할 것.
2. **★잠금 동점이 부호를 뒤집었다.** 같은 레벨에 오퍼와 비드가 «다» 서 있는데(락 — 국고
   호가의 40%) 보고자 자신의 호가가 없으면 귀속 규칙은 «최신 것» 을 고른다. 최신 호가는 사람이
   많은 쪽일 확률이 높다. 그래서 첫 판 R2 는 **「오퍼 우세면 사 감 67% · 비드 우세면 30%」**
   — 지금의 정반대였다. 동점(귀속의 18.7%)을 빼면 **비드 우세 → 사 감 59.5% · 오퍼 우세 →
   39.1%** 로 돌아선다. 양성대조군도 81.3% → 93.8% 로 선다.
   → **라이브도 같은 규칙으로 동점을 비운다**(`_book_ctx` 의 `amb`).
3. **R2b 의 위약이 −0.108 이었다.** 그룹 «간» 성분(종목-일 평균끼리의 상관)이 새 들어온 것이라,
   순위를 종목-일 «안에서» 매기고 그룹 평균을 뺀 그룹내 상관으로 바꿨다(위약 −0.001 ± 0.002).
   **한 판에 두 층이 섞여 있으면 위약이 먼저 말한다** — 위약이 0 이 아니면 추정량을 의심할 것.

---

## 1. R0 — 귀속 재현 (v12 와 같은 수인가)

| | 이번(배치 재구현) | v12 (09-07) |
|---|---:|---:|
| 책에 붙은 체결 | 97.1% | — |
| 그중 **딜러+레벨** | **68.5%** | 66.0% |
| 잠금 동점 | 18.7% | — |
| 양성대조(문면 방향 = 공격 방향의 반대) | 81.3% · **동점 빼면 93.8%** (n=22,220) | 99.2% |
| 사 감 · 팜 | 19,070 · 19,676 | 16,171 · 16,356 |

v12 규약을 따로 짠 스크립트가 같은 자리에 온다. 양성대조가 99.2 에서 93.8 로 내려온 몫은
v12 가 잠금 동점을 어떻게 다뤘는지 스크립트가 남지 않아 못 맞췄다 — 그래서 이번 것은 리포에 둔다.

## 2. R1 — 유효 스프레드 · 최우선 체결률

| | 값 |
|---|---:|
| 체결 시점 호가 스프레드 | **0.5bp 가 93.6%** · 1.0bp 4.4% · 1.5bp+ 1.7% · 잠금 0.3% · 평균 0.587bp |
| 유효 반스프레드 | 중앙 **0.25bp** (= 호가 반스프레드) · 평균 0.177bp · n=30,303 |
| 유효 분포 | 0.25bp **75.4%** · **음수 19.7%** · 0.5bp 2.7% · 0.75bp+ 2.1% |
| 최우선 «레벨» 에서 난 체결 | **81.2%** (n=35,654) |
| 크로스로 걷힌 호가가 맞음 | **23.5%** |
| 시간대별 호가 스프레드 중앙 | 09~16시 전부 0.5bp |

**국고 장외 책은 한 틱이다.** 스프레드가 0.5bp 로 굳어 있어 유효 스프레드는 새 정보가 아니라
«호가 반스프레드 그대로» 다 — 최우선에서 맞으면 정확히 0.25bp(verify [I5] 가 이 항등식을 센다).

새 정보는 **음수 19.7%** 쪽이고, 그 정체는 **크로스한 호가가 맞은 것(23.5%)** 이다. 남의
반대편을 뚫고 들어온 호가는 uncross 가 책에서 걷어내지만 데이터는 그 호가가 곧 체결된다고
말한다. v8 이 크로스 이벤트를 「사실상 치는 호가」라 부른 것이 맞았다 — **국고 체결의 넷 중
하나는 «치는 호가», 넷 중 셋은 «서 있는 최우선»** 이다.

⚠v12 의 「귀속된 체결의 37.0% 가 최우선을 맞혔다」와 다른 수다. v12 는 최우선 «엔트리»(같은
레벨의 여러 딜러 중 tie-break 로 뽑힌 하나)를, 여기서는 최우선 «레벨» 을 센다. 트레이더에게
뜻이 있는 것은 레벨이다. 최우선 뒤에서 맞은 18.8% 는 최우선과 정확히 한 틱 떨어져 있고, 그때
최우선은 나이 중앙 120초 · 맞은 호가는 23초 — **최우선이 묵었을 때 그 뒤가 맞는다.**

## 3. R2 — 직전 불균형 → 공격 방향

불균형 = (비드 딜러 수 − 오퍼 딜러 수) / 합. **맞은 호가 자신은 뺀다**(안 빼면 «오퍼가 있으니
오퍼가 맞았다» 항등식) · 양면이 다 선 책만 · 잠금 동점 제외 → n=23,246.

| 직전 불균형 | n | 사 감(오퍼 맞음) % |
|---|---:|---:|
| 오퍼 우세 (≤ −0.5) | 3,576 | **39.1** |
| 오퍼 약우세 | 4,371 | 43.0 |
| 균형 | 5,990 | 49.7 |
| 비드 약우세 | 5,956 | 54.0 |
| 비드 우세 (≥ 0.5) | 3,353 | **59.5** |

AUC **0.578**(딜러 수) · 0.569(수량) · 그룹내 0.594 · 딜러+레벨 귀속만 0.557 —
**위약(같은 종목-일 안에서 공격 방향 뒤섞기, 20회) 0.511 ± 0.003 · 최대 0.517.** 기저 49.3%.
연도별로도 같은 자리다(비드 우세 → 사 감 2024 56.2% · 2025 56.6% · 2026 54.5% /
오퍼 우세 42.3 · 40.3 · 41.3%).

**책은 «누가 칠지» 를 조금 안다.** 비드가 두터우면 다음 체결은 오퍼를 산다 — 장내 호가창의 큐
불균형 문헌과 같은 방향이다. 다만 크기는 39 ↔ 60% 다. 방향 예측기가 아니라 «지금 사려는 쪽이
많다» 는 읽기 도구다. ⚠하루 단위로는 흔들린다: 비드-우세 체결이 20건 이상인 183일에서 사 감
비율 중앙 54.5% · p10 38.7 · p90 69.5 이고 **35.5% 의 날은 50% 아래**다.

## 4. R2b — 불균형 → 15분 뒤 mid

호가 사건마다(양면 책, n=1,184,181) 불균형과 15분 뒤 mid 변화(금리 bp).

| | Spearman |
|---|---:|
| **그룹내**(종목-일 안에서 순위·평균 제거) | **+0.188** (위약 −0.001 ± 0.002) |
| 풀링 | +0.068 |
| 그룹간(종목-일 평균끼리, n≥30 인 6,645 그룹) | **−0.373** |

| 직전 불균형 | 금리↓ % | 금리↑ % |
|---|---:|---:|
| 오퍼 우세 | **27.5** | 17.8 |
| 균형 | 17.5 | 16.9 |
| 비드 우세 | 19.6 | **24.8** |

부호를 똑바로 읽어야 한다. **그룹내 «+»** 는 비드가 두터우면 15분 뒤 mid 의 **금리가 오른다**
는 뜻이다. R2 와 반대로 보이지만 다른 물건이다 — mid 는 호가의 중간이라, 두터운 비드가 만든
낮은 최우선 비드가 걷히면(TTL·uncross·재호가) mid 가 되돌아온다. **호가 mid 의 되돌림**이고
중앙값은 전 구간 0.0bp(한 틱 시장)다. **거래 신호로 읽지 말 것.** 그룹간 «−» 는 하루 단위의
수준 효과다 — 비드가 두터운 종목-일은 금리가 내려간 날이다. 화면에 안 올린 이유가 이것이다.

## 5. R3 — 딜러 호가 경쟁 → 체결 점유 (Hendershott 2025 재현)

딜러-일 81,409(호가 5건 이상). 품질 = 그 딜러 호가 중 «게시 순간 최우선 이상» 이었던 비율,
그날 안에서 3분위.

| 최우선 게시 비율 | 딜러-일 | 호가 | 귀속 체결 | **체결/호가** | 위약 |
|---|---:|---:|---:|---:|---:|
| 상 | 27,133 | 349,797 | 6,677 | **0.0191** | 0.0151 |
| 중 | 26,955 | 706,413 | 11,094 | 0.0157 | 0.0150 |
| 하 | 27,315 | 604,026 | 7,208 | **0.0119** | 0.0150 |

위약 = 그날 귀속 체결을 호가 수에 비례해 무작위 재배정(20회) → 세 칸이 0.015 로 평평해진다.
실측은 상이 하의 **1.6배**. 날짜 고정효과 회귀
`log(1+체결) = 0.138(±0.002)·log(1+호가) + 0.155(±0.011)·최우선비율`.
호가 점유 ↔ 체결 점유 Spearman **+0.211**.

**«좋은 호가를 많이 내는 딜러가 주문흐름을 가져간다»** 가 이 메신저 방에서도 선다. 많이 내는
것(호가 수)과 잘 내는 것(최우선 게시)이 **따로** 기여한다. 리더보드에 «귀속» 열을 둔 이유다.

## 6. R4 — 호가/체결

전체 **44 대 1** · 체결 있는 종목-일 중앙 36.8 · 종목-일의 **31.6%** 에만 체결이 있다 ·
호가 상위 10% 종목-일이 체결의 **62.4%**.

⚠체결 «보고» 시각은 15시 8,709 · 16시 13,551 로 장 막판에 몰린다. 체결 순간의 책이란 엄밀히는
«보고 순간의 책» 이고, 귀속 창 30분이 그 사이를 잇는다.

---

## 7. 라이브에 넣은 것

| 곳 | 내용 |
|---|---|
| `kbond_live.py` `Book._book_ctx` | 체결 순간의 책 — `eff`·`qs`·`ab`·`imb`·`amb`. 연구와 같은 규약. 국고·통안만 |
| `kbond_live.py` 체결 처리 | 테이프·이벤트에 위 값 · 맞은 호가의 주인에게 `dealers[k]["f"] += 1` |
| `kbond_view.py` `book_info` | 오늘 귀속 체결의 유효 반스프레드 중앙 · 호가 반스프레드 중앙 · 최우선 체결률 · 불균형 3구간별 사 감 % |
| `kbond_view.py` `curve_today` | 지표·차기 행에 «지금» 불균형 `imb` (화면은 ▲▼) |
| `kbond_view.py` `leaderboard` | `f` 귀속 체결 |
| `kbond_schema.py` → `openapi.json` → `api-types.d.ts` | `Fill.eff/qs/ab/imb/amb` · `LeaderRow.f` · `CurveTodayRow.imb` · `BookInfo` · `View.book_info` |
| `verify_v4.py` **[I3][I4][I5]** | 뷰 = 스냅샷 재계산 · Σ딜러 f ≤ 귀속 수(상위 120 만 실림) · **최우선에서 맞았으면 유효 = 호가/2 항등식** |
| `kbond-web` `Trends.tsx` | 맥박 카드 아래 네 칸 · 리더보드 «귀속» 열 · 커브 오늘 딜러 칸 ▲▼ |

### ★화면의 불균형은 «종목을 문면에 적은 체결» 만 센다

라이브 귀속은 배치보다 넓다 — **맨 «ㅎㅈ»** 를 그 브로커의 직전 호가에 붙이는 길(`csrc=prev`)이
있고, 09-03 재생에서 귀속 108건 중 67건이 그것이다. 배치 체결표에는 `BondCode` 가 없어 **연구가
한 번도 재 본 적 없는 모집단**이다.

빼는 이유는 데이터가 아니라 **설계**다. `prev` 는 «그 딜러의 가장 최근 호가» 로 방향을 정하는데,
누가 최근에 호가를 냈는가는 불균형이 재는 것과 **같은 것**이다. 귀속 규칙과 설명 변수가 축을
공유하면 그 상관은 검정이 아니다. 09-03 재생도 그쪽으로 기운다 — `prev` 만 보면 비드 우세에서
사 감 31.2%(n=32)로 §3 과 반대다. `stated` 만 남기면 n 은 작아지지만(그날 21건) 연구가 검증한
모집단과 같은 것을 센다. 화면은 뺀 수를 괄호로 같이 보여 준다(`n_imb_prev`).

### 라이브 대 배치 — 체결 단위 대조 (20260903)

겹치는 34건(라이브 국고 테이프 × 배치 체결 ≤15:30):

| | 일치 |
|---|---:|
| 공격 방향 `ag` | 31/34 (91.2%) |
| 최우선 여부 `ab` | 90.6% |
| 유효 반스프레드 `eff` (±0.01bp) | 88.9% |
| 불균형 `imb` (둘 다 값 있는 21건, ±0.01) | 85.7% |
| 잠금 동점 `amb` | 겹치는 곳 전부 같음 |

**두 책은 같은 책이 아니다** — 라이브는 다리(교체)를 펴 넣고 «민평에» AXE 를 책에 올리며 방
가로지르는 중복을 문구로 지운다. 배치는 본표 QUOTE 행만 본다. 그래서 어긋난 3건은 «어느 호가에
붙였나» 가 갈린 것이고, 값이 틀린 것이 아니다. **이 대조의 뜻은 «같은 수» 가 아니라 «같은
규약이 같은 자리에 온다» 이다.** 라이브만 있는 66건은 전부 배치에 없는 모집단(prev 62 등)이다.

### 검증

동결 리플레이 20260903 15:30 — `verify_v4 [A]~[I5]` **전건 통과** · `gate_meaning` 전건 통과 ·
pytest 55 · `test_issuer` 138/138 · 두 화면 지문 대조 **다섯 영역 0건** · 동향 탭 스크린샷 확인.

그날 값: 귀속 104 · 유효 0.25 = 호가 0.25 · 최우선 82.5%(n=97) · 불균형 21건(+50 은 맨 ㅎㅈ) ·
딜러 귀속 합 73.

## 8. 한계 — 읽는 사람이 알아야 할 것

1. **크레딧은 없다.** 매도 일변도(99.9%)라 양면 불균형이 정의되지 않는다. 크레딧 책의 동학은
   수요 바구니 ↔ 매도 매칭 쪽에서 따로 물어야 한다.
2. 불균형은 **딜러 수** 기준이다. 수량 기준(AUC 0.569)도 같은 방향이지만 수량의 대부분이
   «표기 없음 = 100억» 기본값이라 딜러 수가 정직하다.
3. R2b 의 mid 는 **호가 mid** 다. 체결가 기준 15분 뒤 변화는 체결이 드물어(종목-일의 31.6%)
   못 쟀다.
4. **하루 표본은 작다.** 화면의 구간별 % 는 날마다 흔들린다(§3 의 날짜 분포). 화면 수는
   «오늘이 전 이력과 같은 자리에 있나» 를 보는 것이지 예측기가 아니다.
5. R3 의 «품질» 은 게시 순간의 최우선 여부 하나다. 나이·재게시 빈도·양면 여부는 안 넣었다.
6. 전부 **«보이는 것만의 책»** 이다 — 두 방이 시장 전체가 아니다(§11 의 정직한 제약 셋 중 둘째).
